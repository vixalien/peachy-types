
/// <reference path="./GLib-2.0.d.ts" />
/// <reference path="./GObject-2.0.d.ts" />
/// <reference path="./freetype2-2.0.d.ts" />

/**
 * Type Definitions for GJS (https://gjs.guide/), generated by [GirGen](https://github.com/aylur/girgen)
 * If you found a bug, create a bug report on [GirGen's Repository](https://github.com/aylur/girgen/issues/new)
 */
declare module "gi://HarfBuzz?version=0.0" {
    import type GLib from "gi://GLib?version=2.0"
    import type GObject from "gi://GObject?version=2.0"
    import type freetype2 from "gi://freetype2?version=2.0"

    /**
     * Do **not** import this at runtime.
     * This namespace is only exported for module augmentation.
     */
    export namespace GI {
        

        namespace HarfBuzz {
            

            interface aat_layout_feature_selector_info_tStruct {
                readonly $gtype: GObject.GType<aat_layout_feature_selector_info_t>
                new (fields?: {
                    name_id?: never
                    enable?: never
                    disable?: never
                }): aat_layout_feature_selector_info_t
            }

            interface aat_layout_feature_selector_info_t {
                /**
                 * The selector's name identifier
                 */
                name_id: never
                /**
                 * The value to turn the selector on
                 */
                enable: never
                /**
                 * The value to turn the selector off
                 */
                disable: never
            }

            interface $Exports {
                aat_layout_feature_selector_info_t: aat_layout_feature_selector_info_tStruct
            }
            

            interface blob_tStruct {
                readonly $gtype: GObject.GType<blob_t>
                [Symbol.hasInstance](instance: unknown): instance is blob_t
            }

            interface blob_t {
            }

            interface $Exports {
                blob_t: blob_tStruct
            }
            

            interface buffer_tStruct {
                readonly $gtype: GObject.GType<buffer_t>
                [Symbol.hasInstance](instance: unknown): instance is buffer_t
            }

            interface buffer_t {
            }

            interface $Exports {
                buffer_t: buffer_tStruct
            }
            

            interface color_line_tStruct {
                readonly $gtype: GObject.GType<color_line_t>
                new (fields?: {
                    data?: never
                    get_color_stops?: never
                    get_color_stops_user_data?: never
                    get_extend?: never
                    get_extend_user_data?: never
                    reserved0?: never
                    reserved1?: never
                    reserved2?: never
                    reserved3?: never
                    reserved5?: never
                    reserved6?: never
                    reserved7?: never
                    reserved8?: never
                }): color_line_t
            }

            interface color_line_t {
                
                data: never
                
                get_color_stops: never
                
                get_color_stops_user_data: never
                
                get_extend: never
                
                get_extend_user_data: never
                
                reserved0: never
                
                reserved1: never
                
                reserved2: never
                
                reserved3: never
                
                reserved5: never
                
                reserved6: never
                
                reserved7: never
                
                reserved8: never
            }

            interface $Exports {
                color_line_t: color_line_tStruct
            }
            

            interface color_stop_tStruct {
                readonly $gtype: GObject.GType<color_stop_t>
                new (fields?: {
                    offset?: number
                    is_foreground?: never
                    color?: never
                }): color_stop_t
            }

            interface color_stop_t {
                /**
                 * the offset of the color stop
                 */
                offset: number
                /**
                 * whether the color is the foreground
                 */
                is_foreground: never
                /**
                 * the color, unpremultiplied
                 */
                color: never
            }

            interface $Exports {
                color_stop_t: color_stop_tStruct
            }
            

            interface draw_funcs_tStruct {
                readonly $gtype: GObject.GType<draw_funcs_t>
                [Symbol.hasInstance](instance: unknown): instance is draw_funcs_t
            }

            interface draw_funcs_t {
            }

            interface $Exports {
                draw_funcs_t: draw_funcs_tStruct
            }
            

            interface draw_state_tStruct {
                readonly $gtype: GObject.GType<draw_state_t>
                new (fields?: {
                    path_open?: never
                    path_start_x?: number
                    path_start_y?: number
                    current_x?: number
                    current_y?: number
                }): draw_state_t
            }

            interface draw_state_t {
                /**
                 * Whether there is an open path
                 */
                path_open: never
                /**
                 * X component of the start of current path
                 */
                path_start_x: number
                /**
                 * Y component of the start of current path
                 */
                path_start_y: number
                /**
                 * X component of current point
                 */
                current_x: number
                /**
                 * Y component of current point
                 */
                current_y: number
            }

            interface $Exports {
                draw_state_t: draw_state_tStruct
            }
            

            interface face_tStruct {
                readonly $gtype: GObject.GType<face_t>
                [Symbol.hasInstance](instance: unknown): instance is face_t
            }

            interface face_t {
            }

            interface $Exports {
                face_t: face_tStruct
            }
            

            interface feature_tStruct {
                readonly $gtype: GObject.GType<feature_t>
                new (fields?: {
                    tag?: never
                    value?: number
                    start?: number
                    end?: number
                }): feature_t
            }

            interface feature_t {
                /**
                 * The #hb_tag_t tag of the feature
                 */
                tag: never
                /**
                 * The value of the feature. 0 disables the feature, non-zero (usually
                 * 1) enables the feature.  For features implemented as lookup type 3 (like
                 * 'salt') the `value` is a one based index into the alternates.
                 */
                value: number
                /**
                 * the cluster to start applying this feature setting (inclusive).
                 */
                start: number
                /**
                 * the cluster to end applying this feature setting (exclusive).
                 */
                end: number
                /**
                 * Converts a #hb_feature_t into a `NULL`-terminated string in the format
                 * understood by hb_feature_from_string(). The client in responsible for
                 * allocating big enough size for `buf`, 128 bytes is more than enough.
                 *
                 * Note that the feature value will be omitted if it is '1', but the
                 * string won't include any whitespace.
                 * @since 0.9.5
                 * @returns , output string
                 */
                _string(): string[]
            }

            interface $Exports {
                feature_t: feature_tStruct
            }
            

            interface font_extents_tStruct {
                readonly $gtype: GObject.GType<font_extents_t>
                new (fields?: {
                    ascender?: never
                    descender?: never
                    line_gap?: never
                }): font_extents_t
            }

            interface font_extents_t {
                /**
                 * The height of typographic ascenders.
                 */
                ascender: never
                /**
                 * The depth of typographic descenders.
                 */
                descender: never
                /**
                 * The suggested line-spacing gap.
                 */
                line_gap: never
            }

            interface $Exports {
                font_extents_t: font_extents_tStruct
            }
            

            interface font_funcs_tStruct {
                readonly $gtype: GObject.GType<font_funcs_t>
                [Symbol.hasInstance](instance: unknown): instance is font_funcs_t
            }

            interface font_funcs_t {
            }

            interface $Exports {
                font_funcs_t: font_funcs_tStruct
            }
            

            interface font_tStruct {
                readonly $gtype: GObject.GType<font_t>
                [Symbol.hasInstance](instance: unknown): instance is font_t
            }

            interface font_t {
            }

            interface $Exports {
                font_t: font_tStruct
            }
            

            interface glyph_extents_tStruct {
                readonly $gtype: GObject.GType<glyph_extents_t>
                new (fields?: {
                    x_bearing?: never
                    y_bearing?: never
                    width?: never
                    height?: never
                }): glyph_extents_t
            }

            interface glyph_extents_t {
                /**
                 * Distance from the x-origin to the left extremum of the glyph.
                 */
                x_bearing: never
                /**
                 * Distance from the top extremum of the glyph to the y-origin.
                 */
                y_bearing: never
                /**
                 * Distance from the left extremum of the glyph to the right extremum.
                 */
                width: never
                /**
                 * Distance from the top extremum of the glyph to the bottom extremum.
                 */
                height: never
            }

            interface $Exports {
                glyph_extents_t: glyph_extents_tStruct
            }
            

            interface glyph_info_tStruct {
                readonly $gtype: GObject.GType<glyph_info_t>
                new (fields?: {
                    codepoint?: never
                    cluster?: number
                }): glyph_info_t
            }

            interface glyph_info_t {
                /**
                 * either a Unicode code point (before shaping) or a glyph index
                 *             (after shaping).
                 */
                codepoint: never
                /**
                 * the index of the character in the original text that corresponds
                 *           to this #hb_glyph_info_t, or whatever the client passes to
                 *           hb_buffer_add(). More than one #hb_glyph_info_t can have the same
                 *           `cluster` value, if they resulted from the same character (e.g. one
                 *           to many glyph substitution), and when more than one character gets
                 *           merged in the same glyph (e.g. many to one glyph substitution) the
                 *           #hb_glyph_info_t will have the smallest cluster value of them.
                 *           By default some characters are merged into the same cluster
                 *           (e.g. combining marks have the same cluster as their bases)
                 *           even if they are separate glyphs, hb_buffer_set_cluster_level()
                 *           allow selecting more fine-grained cluster handling.
                 */
                cluster: number
            }

            interface $Exports {
                glyph_info_t: glyph_info_tStruct
            }
            

            interface glyph_position_tStruct {
                readonly $gtype: GObject.GType<glyph_position_t>
                new (fields?: {
                    x_advance?: never
                    y_advance?: never
                    x_offset?: never
                    y_offset?: never
                }): glyph_position_t
            }

            interface glyph_position_t {
                /**
                 * how much the line advances after drawing this glyph when setting
                 *             text in horizontal direction.
                 */
                x_advance: never
                /**
                 * how much the line advances after drawing this glyph when setting
                 *             text in vertical direction.
                 */
                y_advance: never
                /**
                 * how much the glyph moves on the X-axis before drawing it, this
                 *            should not affect how much the line advances.
                 */
                x_offset: never
                /**
                 * how much the glyph moves on the Y-axis before drawing it, this
                 *            should not affect how much the line advances.
                 */
                y_offset: never
            }

            interface $Exports {
                glyph_position_t: glyph_position_tStruct
            }
            

            interface language_tStruct {
                readonly $gtype: GObject.GType<language_t>
                new (fields?: {
                }): language_t
            }

            interface language_t {
                /**
                 * Converts an #hb_language_t to a string.
                 * @since 0.9.2
                 * @returns  A `NULL`-terminated string representing the `language`. Must not be freed by the caller.
                 */
                _string(): string
            }

            interface $Exports {
                language_t: language_tStruct
            }
            

            interface map_tStruct {
                readonly $gtype: GObject.GType<map_t>
                [Symbol.hasInstance](instance: unknown): instance is map_t
            }

            interface map_t {
            }

            interface $Exports {
                map_t: map_tStruct
            }
            

            interface ot_color_layer_tStruct {
                readonly $gtype: GObject.GType<ot_color_layer_t>
                new (fields?: {
                    glyph?: never
                    color_index?: number
                }): ot_color_layer_t
            }

            interface ot_color_layer_t {
                /**
                 * the glyph ID of the layer
                 */
                glyph: never
                /**
                 * the palette color index of the layer
                 */
                color_index: number
            }

            interface $Exports {
                ot_color_layer_t: ot_color_layer_tStruct
            }
            

            interface ot_math_glyph_part_tStruct {
                readonly $gtype: GObject.GType<ot_math_glyph_part_t>
                new (fields?: {
                    glyph?: never
                    start_connector_length?: never
                    end_connector_length?: never
                    full_advance?: never
                    flags?: never
                }): ot_math_glyph_part_t
            }

            interface ot_math_glyph_part_t {
                /**
                 * The glyph index of the variant part
                 */
                glyph: never
                /**
                 * The length of the connector on the starting side of the variant part
                 */
                start_connector_length: never
                /**
                 * The length of the connector on the ending side of the variant part
                 */
                end_connector_length: never
                /**
                 * The total advance of the part
                 */
                full_advance: never
                /**
                 * #hb_ot_math_glyph_part_flags_t flags for the part
                 */
                flags: never
            }

            interface $Exports {
                ot_math_glyph_part_t: ot_math_glyph_part_tStruct
            }
            

            interface ot_math_glyph_variant_tStruct {
                readonly $gtype: GObject.GType<ot_math_glyph_variant_t>
                new (fields?: {
                    glyph?: never
                    advance?: never
                }): ot_math_glyph_variant_t
            }

            interface ot_math_glyph_variant_t {
                /**
                 * The glyph index of the variant
                 */
                glyph: never
                /**
                 * The advance width of the variant
                 */
                advance: never
            }

            interface $Exports {
                ot_math_glyph_variant_t: ot_math_glyph_variant_tStruct
            }
            

            interface ot_math_kern_entry_tStruct {
                readonly $gtype: GObject.GType<ot_math_kern_entry_t>
                new (fields?: {
                    max_correction_height?: never
                    kern_value?: never
                }): ot_math_kern_entry_t
            }

            interface ot_math_kern_entry_t {
                /**
                 * The maximum height at which this entry should be used
                 */
                max_correction_height: never
                /**
                 * The kern value of the entry
                 */
                kern_value: never
            }

            interface $Exports {
                ot_math_kern_entry_t: ot_math_kern_entry_tStruct
            }
            

            interface ot_name_entry_tStruct {
                readonly $gtype: GObject.GType<ot_name_entry_t>
                new (fields?: {
                    name_id?: never
                    language?: never
                }): ot_name_entry_t
            }

            interface ot_name_entry_t {
                /**
                 * name ID
                 */
                name_id: never
                /**
                 * language
                 */
                language: never
            }

            interface $Exports {
                ot_name_entry_t: ot_name_entry_tStruct
            }
            

            interface ot_var_axis_info_tStruct {
                readonly $gtype: GObject.GType<ot_var_axis_info_t>
                new (fields?: {
                    axis_index?: number
                    tag?: never
                    name_id?: never
                    flags?: never
                    min_value?: number
                    default_value?: number
                    max_value?: number
                }): ot_var_axis_info_t
            }

            interface ot_var_axis_info_t {
                /**
                 * Index of the axis in the variation-axis array
                 */
                axis_index: number
                /**
                 * The #hb_tag_t tag identifying the design variation of the axis
                 */
                tag: never
                /**
                 * The `name` table Name ID that provides display names for the axis
                 */
                name_id: never
                /**
                 * The #hb_ot_var_axis_flags_t flags for the axis
                 */
                flags: never
                /**
                 * The minimum value on the variation axis that the font covers
                 */
                min_value: number
                /**
                 * The position on the variation axis corresponding to the font's defaults
                 */
                default_value: number
                /**
                 * The maximum value on the variation axis that the font covers
                 */
                max_value: number
            }

            interface $Exports {
                ot_var_axis_info_t: ot_var_axis_info_tStruct
            }
            

            interface ot_var_axis_tStruct {
                readonly $gtype: GObject.GType<ot_var_axis_t>
                new (fields?: {
                    tag?: never
                    name_id?: never
                    min_value?: number
                    default_value?: number
                    max_value?: number
                }): ot_var_axis_t
            }

            interface ot_var_axis_t {
                /**
                 * axis tag
                 */
                tag: never
                /**
                 * axis name identifier
                 */
                name_id: never
                /**
                 * minimum value of the axis
                 */
                min_value: number
                /**
                 * default value of the axis
                 */
                default_value: number
                /**
                 * maximum value of the axis
                 */
                max_value: number
            }

            interface $Exports {
                ot_var_axis_t: ot_var_axis_tStruct
            }
            

            interface paint_funcs_tStruct {
                readonly $gtype: GObject.GType<paint_funcs_t>
                [Symbol.hasInstance](instance: unknown): instance is paint_funcs_t
            }

            interface paint_funcs_t {
            }

            interface $Exports {
                paint_funcs_t: paint_funcs_tStruct
            }
            

            interface segment_properties_tStruct {
                readonly $gtype: GObject.GType<segment_properties_t>
                new (fields?: {
                    direction?: never
                    script?: never
                    language?: never
                }): segment_properties_t
            }

            interface segment_properties_t {
                /**
                 * the #hb_direction_t of the buffer, see hb_buffer_set_direction().
                 */
                direction: never
                /**
                 * the #hb_script_t of the buffer, see hb_buffer_set_script().
                 */
                script: never
                /**
                 * the #hb_language_t of the buffer, see hb_buffer_set_language().
                 */
                language: never
            }

            interface $Exports {
                segment_properties_t: segment_properties_tStruct
            }
            

            interface set_tStruct {
                readonly $gtype: GObject.GType<set_t>
                [Symbol.hasInstance](instance: unknown): instance is set_t
            }

            interface set_t {
            }

            interface $Exports {
                set_t: set_tStruct
            }
            

            interface shape_plan_tStruct {
                readonly $gtype: GObject.GType<shape_plan_t>
                [Symbol.hasInstance](instance: unknown): instance is shape_plan_t
            }

            interface shape_plan_t {
            }

            interface $Exports {
                shape_plan_t: shape_plan_tStruct
            }
            

            interface unicode_funcs_tStruct {
                readonly $gtype: GObject.GType<unicode_funcs_t>
                [Symbol.hasInstance](instance: unknown): instance is unicode_funcs_t
            }

            interface unicode_funcs_t {
            }

            interface $Exports {
                unicode_funcs_t: unicode_funcs_tStruct
            }
            

            interface user_data_key_tStruct {
                readonly $gtype: GObject.GType<user_data_key_t>
                new (fields?: {
                }): user_data_key_t
            }

            interface user_data_key_t {
            }

            interface $Exports {
                user_data_key_t: user_data_key_tStruct
            }
            

            interface variation_tStruct {
                readonly $gtype: GObject.GType<variation_t>
                new (fields?: {
                    tag?: never
                    value?: number
                }): variation_t
            }

            interface variation_t {
                /**
                 * The #hb_tag_t tag of the variation-axis name
                 */
                tag: never
                /**
                 * The value of the variation axis
                 */
                value: number
                /**
                 * Converts an #hb_variation_t into a `NULL`-terminated string in the format
                 * understood by hb_variation_from_string(). The client in responsible for
                 * allocating big enough size for `buf`, 128 bytes is more than enough.
                 *
                 * Note that the string won't include any whitespace.
                 * @since 1.4.2
                 * @returns , output string
                 */
                _string(): string[]
            }

            interface $Exports {
                variation_t: variation_tStruct
            }
            

            interface var_int_tStruct {
                readonly $gtype: GObject.GType<var_int_t>
                [Symbol.hasInstance](instance: unknown): instance is var_int_t
            }

            interface var_int_t {
                
                u32: number
                
                i32: number
                
                u16: number[]
                
                i16: number[]
                
                u8: Uint8Array
                
                i8: Uint8Array
            }

            interface $Exports {
                var_int_t: var_int_tStruct
            }
            

            interface var_num_tStruct {
                readonly $gtype: GObject.GType<var_num_t>
                [Symbol.hasInstance](instance: unknown): instance is var_num_t
            }

            interface var_num_t {
                
                f: number
                
                u32: number
                
                i32: number
                
                u16: number[]
                
                i16: number[]
                
                u8: Uint8Array
                
                i8: Uint8Array
            }

            interface $Exports {
                var_num_t: var_num_tStruct
            }
            
            interface aat_layout_feature_selector_tEnum {
                readonly $gtype: GObject.GType<aat_layout_feature_selector_t>
                /**
                 * Initial, unset feature selector
                 */
                readonly "INVALID": 65535
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_ALL_TYPOGRAPHIC
                 */
                readonly "ALL_TYPE_FEATURES_ON": 0
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_ALL_TYPOGRAPHIC
                 */
                readonly "ALL_TYPE_FEATURES_OFF": 1
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_LIGATURES
                 */
                readonly "REQUIRED_LIGATURES_ON": 0
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_LIGATURES
                 */
                readonly "REQUIRED_LIGATURES_OFF": 1
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_LIGATURES
                 */
                readonly "COMMON_LIGATURES_ON": 2
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_LIGATURES
                 */
                readonly "COMMON_LIGATURES_OFF": 3
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_LIGATURES
                 */
                readonly "RARE_LIGATURES_ON": 4
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_LIGATURES
                 */
                readonly "RARE_LIGATURES_OFF": 5
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_LIGATURES
                 */
                readonly "LOGOS_ON": 6
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_LIGATURES
                 */
                readonly "LOGOS_OFF": 7
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_LIGATURES
                 */
                readonly "REBUS_PICTURES_ON": 8
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_LIGATURES
                 */
                readonly "REBUS_PICTURES_OFF": 9
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_LIGATURES
                 */
                readonly "DIPHTHONG_LIGATURES_ON": 10
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_LIGATURES
                 */
                readonly "DIPHTHONG_LIGATURES_OFF": 11
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_LIGATURES
                 */
                readonly "SQUARED_LIGATURES_ON": 12
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_LIGATURES
                 */
                readonly "SQUARED_LIGATURES_OFF": 13
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_LIGATURES
                 */
                readonly "ABBREV_SQUARED_LIGATURES_ON": 14
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_LIGATURES
                 */
                readonly "ABBREV_SQUARED_LIGATURES_OFF": 15
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_LIGATURES
                 */
                readonly "SYMBOL_LIGATURES_ON": 16
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_LIGATURES
                 */
                readonly "SYMBOL_LIGATURES_OFF": 17
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_LIGATURES
                 */
                readonly "CONTEXTUAL_LIGATURES_ON": 18
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_LIGATURES
                 */
                readonly "CONTEXTUAL_LIGATURES_OFF": 19
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_LIGATURES
                 */
                readonly "HISTORICAL_LIGATURES_ON": 20
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_LIGATURES
                 */
                readonly "HISTORICAL_LIGATURES_OFF": 21
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_LIGATURES
                 */
                readonly "UNCONNECTED": 0
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_LIGATURES
                 */
                readonly "PARTIALLY_CONNECTED": 1
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_LIGATURES
                 */
                readonly "CURSIVE": 2
                /**
                 * Deprecated
                 */
                readonly "UPPER_AND_LOWER_CASE": 0
                /**
                 * Deprecated
                 */
                readonly "ALL_CAPS": 1
                /**
                 * Deprecated
                 */
                readonly "ALL_LOWER_CASE": 2
                /**
                 * Deprecated
                 */
                readonly "SMALL_CAPS": 3
                /**
                 * Deprecated
                 */
                readonly "INITIAL_CAPS": 4
                /**
                 * Deprecated
                 */
                readonly "INITIAL_CAPS_AND_SMALL_CAPS": 5
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_VERTICAL_SUBSTITUTION
                 */
                readonly "SUBSTITUTE_VERTICAL_FORMS_ON": 0
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_VERTICAL_SUBSTITUTION
                 */
                readonly "SUBSTITUTE_VERTICAL_FORMS_OFF": 1
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_LINGUISTIC_REARRANGEMENT
                 */
                readonly "LINGUISTIC_REARRANGEMENT_ON": 0
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_LINGUISTIC_REARRANGEMENT
                 */
                readonly "LINGUISTIC_REARRANGEMENT_OFF": 1
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_NUMBER_SPACING
                 */
                readonly "MONOSPACED_NUMBERS": 0
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_NUMBER_SPACING
                 */
                readonly "PROPORTIONAL_NUMBERS": 1
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_NUMBER_SPACING
                 */
                readonly "THIRD_WIDTH_NUMBERS": 2
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_NUMBER_SPACING
                 */
                readonly "QUARTER_WIDTH_NUMBERS": 3
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_SMART_SWASH_TYPE
                 */
                readonly "WORD_INITIAL_SWASHES_ON": 0
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_SMART_SWASH_TYPE
                 */
                readonly "WORD_INITIAL_SWASHES_OFF": 1
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_SMART_SWASH_TYPE
                 */
                readonly "WORD_FINAL_SWASHES_ON": 2
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_SMART_SWASH_TYPE
                 */
                readonly "WORD_FINAL_SWASHES_OFF": 3
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_SMART_SWASH_TYPE
                 */
                readonly "LINE_INITIAL_SWASHES_ON": 4
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_SMART_SWASH_TYPE
                 */
                readonly "LINE_INITIAL_SWASHES_OFF": 5
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_SMART_SWASH_TYPE
                 */
                readonly "LINE_FINAL_SWASHES_ON": 6
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_SMART_SWASH_TYPE
                 */
                readonly "LINE_FINAL_SWASHES_OFF": 7
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_SMART_SWASH_TYPE
                 */
                readonly "NON_FINAL_SWASHES_ON": 8
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_SMART_SWASH_TYPE
                 */
                readonly "NON_FINAL_SWASHES_OFF": 9
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_DIACRITICS_TYPE
                 */
                readonly "SHOW_DIACRITICS": 0
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_DIACRITICS_TYPE
                 */
                readonly "HIDE_DIACRITICS": 1
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_DIACRITICS_TYPE
                 */
                readonly "DECOMPOSE_DIACRITICS": 2
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_VERTICAL_POSITION
                 */
                readonly "NORMAL_POSITION": 0
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_VERTICAL_POSITION
                 */
                readonly "SUPERIORS": 1
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_VERTICAL_POSITION
                 */
                readonly "INFERIORS": 2
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_VERTICAL_POSITION
                 */
                readonly "ORDINALS": 3
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_VERTICAL_POSITION
                 */
                readonly "SCIENTIFIC_INFERIORS": 4
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_FRACTIONS
                 */
                readonly "NO_FRACTIONS": 0
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_FRACTIONS
                 */
                readonly "VERTICAL_FRACTIONS": 1
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_FRACTIONS
                 */
                readonly "DIAGONAL_FRACTIONS": 2
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_OVERLAPPING_CHARACTERS_TYPE
                 */
                readonly "PREVENT_OVERLAP_ON": 0
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_OVERLAPPING_CHARACTERS_TYPE
                 */
                readonly "PREVENT_OVERLAP_OFF": 1
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_TYPOGRAPHIC_EXTRAS
                 */
                readonly "HYPHENS_TO_EM_DASH_ON": 0
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_TYPOGRAPHIC_EXTRAS
                 */
                readonly "HYPHENS_TO_EM_DASH_OFF": 1
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_TYPOGRAPHIC_EXTRAS
                 */
                readonly "HYPHEN_TO_EN_DASH_ON": 2
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_TYPOGRAPHIC_EXTRAS
                 */
                readonly "HYPHEN_TO_EN_DASH_OFF": 3
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_TYPOGRAPHIC_EXTRAS
                 */
                readonly "SLASHED_ZERO_ON": 4
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_TYPOGRAPHIC_EXTRAS
                 */
                readonly "SLASHED_ZERO_OFF": 5
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_TYPOGRAPHIC_EXTRAS
                 */
                readonly "FORM_INTERROBANG_ON": 6
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_TYPOGRAPHIC_EXTRAS
                 */
                readonly "FORM_INTERROBANG_OFF": 7
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_TYPOGRAPHIC_EXTRAS
                 */
                readonly "SMART_QUOTES_ON": 8
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_TYPOGRAPHIC_EXTRAS
                 */
                readonly "SMART_QUOTES_OFF": 9
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_TYPOGRAPHIC_EXTRAS
                 */
                readonly "PERIODS_TO_ELLIPSIS_ON": 10
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_TYPOGRAPHIC_EXTRAS
                 */
                readonly "PERIODS_TO_ELLIPSIS_OFF": 11
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_MATHEMATICAL_EXTRAS
                 */
                readonly "HYPHEN_TO_MINUS_ON": 0
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_MATHEMATICAL_EXTRAS
                 */
                readonly "HYPHEN_TO_MINUS_OFF": 1
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_MATHEMATICAL_EXTRAS
                 */
                readonly "ASTERISK_TO_MULTIPLY_ON": 2
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_MATHEMATICAL_EXTRAS
                 */
                readonly "ASTERISK_TO_MULTIPLY_OFF": 3
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_MATHEMATICAL_EXTRAS
                 */
                readonly "SLASH_TO_DIVIDE_ON": 4
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_MATHEMATICAL_EXTRAS
                 */
                readonly "SLASH_TO_DIVIDE_OFF": 5
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_MATHEMATICAL_EXTRAS
                 */
                readonly "INEQUALITY_LIGATURES_ON": 6
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_MATHEMATICAL_EXTRAS
                 */
                readonly "INEQUALITY_LIGATURES_OFF": 7
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_MATHEMATICAL_EXTRAS
                 */
                readonly "EXPONENTS_ON": 8
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_MATHEMATICAL_EXTRAS
                 */
                readonly "EXPONENTS_OFF": 9
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_MATHEMATICAL_EXTRAS
                 */
                readonly "MATHEMATICAL_GREEK_ON": 10
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_MATHEMATICAL_EXTRAS
                 */
                readonly "MATHEMATICAL_GREEK_OFF": 11
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_ORNAMENT_SETS_TYPE
                 */
                readonly "NO_ORNAMENTS": 0
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_ORNAMENT_SETS_TYPE
                 */
                readonly "DINGBATS": 1
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_ORNAMENT_SETS_TYPE
                 */
                readonly "PI_CHARACTERS": 2
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_ORNAMENT_SETS_TYPE
                 */
                readonly "FLEURONS": 3
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_ORNAMENT_SETS_TYPE
                 */
                readonly "DECORATIVE_BORDERS": 4
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_ORNAMENT_SETS_TYPE
                 */
                readonly "INTERNATIONAL_SYMBOLS": 5
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_ORNAMENT_SETS_TYPE
                 */
                readonly "MATH_SYMBOLS": 6
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_CHARACTER_ALTERNATIVES
                 */
                readonly "NO_ALTERNATES": 0
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_DESIGN_COMPLEXITY_TYPE
                 */
                readonly "DESIGN_LEVEL1": 0
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_DESIGN_COMPLEXITY_TYPE
                 */
                readonly "DESIGN_LEVEL2": 1
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_DESIGN_COMPLEXITY_TYPE
                 */
                readonly "DESIGN_LEVEL3": 2
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_DESIGN_COMPLEXITY_TYPE
                 */
                readonly "DESIGN_LEVEL4": 3
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_DESIGN_COMPLEXITY_TYPE
                 */
                readonly "DESIGN_LEVEL5": 4
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_STYLE_OPTIONS
                 */
                readonly "NO_STYLE_OPTIONS": 0
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_STYLE_OPTIONS
                 */
                readonly "DISPLAY_TEXT": 1
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_STYLE_OPTIONS
                 */
                readonly "ENGRAVED_TEXT": 2
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_STYLE_OPTIONS
                 */
                readonly "ILLUMINATED_CAPS": 3
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_STYLE_OPTIONS
                 */
                readonly "TITLING_CAPS": 4
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_STYLE_OPTIONS
                 */
                readonly "TALL_CAPS": 5
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_CHARACTER_SHAPE
                 */
                readonly "TRADITIONAL_CHARACTERS": 0
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_CHARACTER_SHAPE
                 */
                readonly "SIMPLIFIED_CHARACTERS": 1
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_CHARACTER_SHAPE
                 */
                readonly "JIS1978_CHARACTERS": 2
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_CHARACTER_SHAPE
                 */
                readonly "JIS1983_CHARACTERS": 3
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_CHARACTER_SHAPE
                 */
                readonly "JIS1990_CHARACTERS": 4
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_CHARACTER_SHAPE
                 */
                readonly "TRADITIONAL_ALT_ONE": 5
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_CHARACTER_SHAPE
                 */
                readonly "TRADITIONAL_ALT_TWO": 6
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_CHARACTER_SHAPE
                 */
                readonly "TRADITIONAL_ALT_THREE": 7
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_CHARACTER_SHAPE
                 */
                readonly "TRADITIONAL_ALT_FOUR": 8
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_CHARACTER_SHAPE
                 */
                readonly "TRADITIONAL_ALT_FIVE": 9
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_CHARACTER_SHAPE
                 */
                readonly "EXPERT_CHARACTERS": 10
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_CHARACTER_SHAPE
                 */
                readonly "JIS2004_CHARACTERS": 11
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_CHARACTER_SHAPE
                 */
                readonly "HOJO_CHARACTERS": 12
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_CHARACTER_SHAPE
                 */
                readonly "NLCCHARACTERS": 13
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_CHARACTER_SHAPE
                 */
                readonly "TRADITIONAL_NAMES_CHARACTERS": 14
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_NUMBER_CASE
                 */
                readonly "LOWER_CASE_NUMBERS": 0
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_NUMBER_CASE
                 */
                readonly "UPPER_CASE_NUMBERS": 1
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_TEXT_SPACING
                 */
                readonly "PROPORTIONAL_TEXT": 0
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_TEXT_SPACING
                 */
                readonly "MONOSPACED_TEXT": 1
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_TEXT_SPACING
                 */
                readonly "HALF_WIDTH_TEXT": 2
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_TEXT_SPACING
                 */
                readonly "THIRD_WIDTH_TEXT": 3
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_TEXT_SPACING
                 */
                readonly "QUARTER_WIDTH_TEXT": 4
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_TEXT_SPACING
                 */
                readonly "ALT_PROPORTIONAL_TEXT": 5
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_TEXT_SPACING
                 */
                readonly "ALT_HALF_WIDTH_TEXT": 6
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_TRANSLITERATION
                 */
                readonly "NO_TRANSLITERATION": 0
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_TRANSLITERATION
                 */
                readonly "HANJA_TO_HANGUL": 1
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_TRANSLITERATION
                 */
                readonly "HIRAGANA_TO_KATAKANA": 2
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_TRANSLITERATION
                 */
                readonly "KATAKANA_TO_HIRAGANA": 3
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_TRANSLITERATION
                 */
                readonly "KANA_TO_ROMANIZATION": 4
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_TRANSLITERATION
                 */
                readonly "ROMANIZATION_TO_HIRAGANA": 5
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_TRANSLITERATION
                 */
                readonly "ROMANIZATION_TO_KATAKANA": 6
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_TRANSLITERATION
                 */
                readonly "HANJA_TO_HANGUL_ALT_ONE": 7
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_TRANSLITERATION
                 */
                readonly "HANJA_TO_HANGUL_ALT_TWO": 8
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_TRANSLITERATION
                 */
                readonly "HANJA_TO_HANGUL_ALT_THREE": 9
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_ANNOTATION_TYPE
                 */
                readonly "NO_ANNOTATION": 0
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_ANNOTATION_TYPE
                 */
                readonly "BOX_ANNOTATION": 1
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_ANNOTATION_TYPE
                 */
                readonly "ROUNDED_BOX_ANNOTATION": 2
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_ANNOTATION_TYPE
                 */
                readonly "CIRCLE_ANNOTATION": 3
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_ANNOTATION_TYPE
                 */
                readonly "INVERTED_CIRCLE_ANNOTATION": 4
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_ANNOTATION_TYPE
                 */
                readonly "PARENTHESIS_ANNOTATION": 5
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_ANNOTATION_TYPE
                 */
                readonly "PERIOD_ANNOTATION": 6
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_ANNOTATION_TYPE
                 */
                readonly "ROMAN_NUMERAL_ANNOTATION": 7
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_ANNOTATION_TYPE
                 */
                readonly "DIAMOND_ANNOTATION": 8
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_ANNOTATION_TYPE
                 */
                readonly "INVERTED_BOX_ANNOTATION": 9
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_ANNOTATION_TYPE
                 */
                readonly "INVERTED_ROUNDED_BOX_ANNOTATION": 10
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_KANA_SPACING_TYPE
                 */
                readonly "FULL_WIDTH_KANA": 0
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_KANA_SPACING_TYPE
                 */
                readonly "PROPORTIONAL_KANA": 1
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_IDEOGRAPHIC_SPACING_TYPE
                 */
                readonly "FULL_WIDTH_IDEOGRAPHS": 0
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_IDEOGRAPHIC_SPACING_TYPE
                 */
                readonly "PROPORTIONAL_IDEOGRAPHS": 1
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_IDEOGRAPHIC_SPACING_TYPE
                 */
                readonly "HALF_WIDTH_IDEOGRAPHS": 2
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_UNICODE_DECOMPOSITION_TYPE
                 */
                readonly "CANONICAL_COMPOSITION_ON": 0
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_UNICODE_DECOMPOSITION_TYPE
                 */
                readonly "CANONICAL_COMPOSITION_OFF": 1
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_UNICODE_DECOMPOSITION_TYPE
                 */
                readonly "COMPATIBILITY_COMPOSITION_ON": 2
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_UNICODE_DECOMPOSITION_TYPE
                 */
                readonly "COMPATIBILITY_COMPOSITION_OFF": 3
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_UNICODE_DECOMPOSITION_TYPE
                 */
                readonly "TRANSCODING_COMPOSITION_ON": 4
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_UNICODE_DECOMPOSITION_TYPE
                 */
                readonly "TRANSCODING_COMPOSITION_OFF": 5
                /**
                 * Deprecated; use #HB_AAT_LAYOUT_FEATURE_SELECTOR_RUBY_KANA_OFF instead
                 */
                readonly "NO_RUBY_KANA": 0
                /**
                 * Deprecated; use #HB_AAT_LAYOUT_FEATURE_SELECTOR_RUBY_KANA_ON instead
                 */
                readonly "RUBY_KANA": 1
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_RUBY_KANA
                 */
                readonly "RUBY_KANA_ON": 2
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_RUBY_KANA
                 */
                readonly "RUBY_KANA_OFF": 3
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_CJK_SYMBOL_ALTERNATIVES_TYPE
                 */
                readonly "NO_CJK_SYMBOL_ALTERNATIVES": 0
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_CJK_SYMBOL_ALTERNATIVES_TYPE
                 */
                readonly "CJK_SYMBOL_ALT_ONE": 1
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_CJK_SYMBOL_ALTERNATIVES_TYPE
                 */
                readonly "CJK_SYMBOL_ALT_TWO": 2
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_CJK_SYMBOL_ALTERNATIVES_TYPE
                 */
                readonly "CJK_SYMBOL_ALT_THREE": 3
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_CJK_SYMBOL_ALTERNATIVES_TYPE
                 */
                readonly "CJK_SYMBOL_ALT_FOUR": 4
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_CJK_SYMBOL_ALTERNATIVES_TYPE
                 */
                readonly "CJK_SYMBOL_ALT_FIVE": 5
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_IDEOGRAPHIC_ALTERNATIVES_TYPE
                 */
                readonly "NO_IDEOGRAPHIC_ALTERNATIVES": 0
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_IDEOGRAPHIC_ALTERNATIVES_TYPE
                 */
                readonly "IDEOGRAPHIC_ALT_ONE": 1
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_IDEOGRAPHIC_ALTERNATIVES_TYPE
                 */
                readonly "IDEOGRAPHIC_ALT_TWO": 2
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_IDEOGRAPHIC_ALTERNATIVES_TYPE
                 */
                readonly "IDEOGRAPHIC_ALT_THREE": 3
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_IDEOGRAPHIC_ALTERNATIVES_TYPE
                 */
                readonly "IDEOGRAPHIC_ALT_FOUR": 4
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_IDEOGRAPHIC_ALTERNATIVES_TYPE
                 */
                readonly "IDEOGRAPHIC_ALT_FIVE": 5
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_CJK_VERTICAL_ROMAN_PLACEMENT_TYPE
                 */
                readonly "CJK_VERTICAL_ROMAN_CENTERED": 0
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_CJK_VERTICAL_ROMAN_PLACEMENT_TYPE
                 */
                readonly "CJK_VERTICAL_ROMAN_HBASELINE": 1
                /**
                 * Deprecated; use #HB_AAT_LAYOUT_FEATURE_SELECTOR_CJK_ITALIC_ROMAN_OFF instead
                 */
                readonly "NO_CJK_ITALIC_ROMAN": 0
                /**
                 * Deprecated; use #HB_AAT_LAYOUT_FEATURE_SELECTOR_CJK_ITALIC_ROMAN_ON instead
                 */
                readonly "CJK_ITALIC_ROMAN": 1
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_ITALIC_CJK_ROMAN
                 */
                readonly "CJK_ITALIC_ROMAN_ON": 2
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_ITALIC_CJK_ROMAN
                 */
                readonly "CJK_ITALIC_ROMAN_OFF": 3
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_CASE_SENSITIVE_LAYOUT
                 */
                readonly "CASE_SENSITIVE_LAYOUT_ON": 0
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_CASE_SENSITIVE_LAYOUT
                 */
                readonly "CASE_SENSITIVE_LAYOUT_OFF": 1
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_CASE_SENSITIVE_LAYOUT
                 */
                readonly "CASE_SENSITIVE_SPACING_ON": 2
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_CASE_SENSITIVE_LAYOUT
                 */
                readonly "CASE_SENSITIVE_SPACING_OFF": 3
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_ALTERNATE_KANA
                 */
                readonly "ALTERNATE_HORIZ_KANA_ON": 0
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_ALTERNATE_KANA
                 */
                readonly "ALTERNATE_HORIZ_KANA_OFF": 1
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_ALTERNATE_KANA
                 */
                readonly "ALTERNATE_VERT_KANA_ON": 2
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_ALTERNATE_KANA
                 */
                readonly "ALTERNATE_VERT_KANA_OFF": 3
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_STYLISTIC_ALTERNATIVES
                 */
                readonly "NO_STYLISTIC_ALTERNATES": 0
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_STYLISTIC_ALTERNATIVES
                 */
                readonly "STYLISTIC_ALT_ONE_ON": 2
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_STYLISTIC_ALTERNATIVES
                 */
                readonly "STYLISTIC_ALT_ONE_OFF": 3
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_STYLISTIC_ALTERNATIVES
                 */
                readonly "STYLISTIC_ALT_TWO_ON": 4
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_STYLISTIC_ALTERNATIVES
                 */
                readonly "STYLISTIC_ALT_TWO_OFF": 5
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_STYLISTIC_ALTERNATIVES
                 */
                readonly "STYLISTIC_ALT_THREE_ON": 6
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_STYLISTIC_ALTERNATIVES
                 */
                readonly "STYLISTIC_ALT_THREE_OFF": 7
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_STYLISTIC_ALTERNATIVES
                 */
                readonly "STYLISTIC_ALT_FOUR_ON": 8
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_STYLISTIC_ALTERNATIVES
                 */
                readonly "STYLISTIC_ALT_FOUR_OFF": 9
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_STYLISTIC_ALTERNATIVES
                 */
                readonly "STYLISTIC_ALT_FIVE_ON": 10
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_STYLISTIC_ALTERNATIVES
                 */
                readonly "STYLISTIC_ALT_FIVE_OFF": 11
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_STYLISTIC_ALTERNATIVES
                 */
                readonly "STYLISTIC_ALT_SIX_ON": 12
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_STYLISTIC_ALTERNATIVES
                 */
                readonly "STYLISTIC_ALT_SIX_OFF": 13
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_STYLISTIC_ALTERNATIVES
                 */
                readonly "STYLISTIC_ALT_SEVEN_ON": 14
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_STYLISTIC_ALTERNATIVES
                 */
                readonly "STYLISTIC_ALT_SEVEN_OFF": 15
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_STYLISTIC_ALTERNATIVES
                 */
                readonly "STYLISTIC_ALT_EIGHT_ON": 16
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_STYLISTIC_ALTERNATIVES
                 */
                readonly "STYLISTIC_ALT_EIGHT_OFF": 17
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_STYLISTIC_ALTERNATIVES
                 */
                readonly "STYLISTIC_ALT_NINE_ON": 18
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_STYLISTIC_ALTERNATIVES
                 */
                readonly "STYLISTIC_ALT_NINE_OFF": 19
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_STYLISTIC_ALTERNATIVES
                 */
                readonly "STYLISTIC_ALT_TEN_ON": 20
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_STYLISTIC_ALTERNATIVES
                 */
                readonly "STYLISTIC_ALT_TEN_OFF": 21
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_STYLISTIC_ALTERNATIVES
                 */
                readonly "STYLISTIC_ALT_ELEVEN_ON": 22
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_STYLISTIC_ALTERNATIVES
                 */
                readonly "STYLISTIC_ALT_ELEVEN_OFF": 23
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_STYLISTIC_ALTERNATIVES
                 */
                readonly "STYLISTIC_ALT_TWELVE_ON": 24
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_STYLISTIC_ALTERNATIVES
                 */
                readonly "STYLISTIC_ALT_TWELVE_OFF": 25
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_STYLISTIC_ALTERNATIVES
                 */
                readonly "STYLISTIC_ALT_THIRTEEN_ON": 26
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_STYLISTIC_ALTERNATIVES
                 */
                readonly "STYLISTIC_ALT_THIRTEEN_OFF": 27
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_STYLISTIC_ALTERNATIVES
                 */
                readonly "STYLISTIC_ALT_FOURTEEN_ON": 28
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_STYLISTIC_ALTERNATIVES
                 */
                readonly "STYLISTIC_ALT_FOURTEEN_OFF": 29
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_STYLISTIC_ALTERNATIVES
                 */
                readonly "STYLISTIC_ALT_FIFTEEN_ON": 30
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_STYLISTIC_ALTERNATIVES
                 */
                readonly "STYLISTIC_ALT_FIFTEEN_OFF": 31
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_STYLISTIC_ALTERNATIVES
                 */
                readonly "STYLISTIC_ALT_SIXTEEN_ON": 32
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_STYLISTIC_ALTERNATIVES
                 */
                readonly "STYLISTIC_ALT_SIXTEEN_OFF": 33
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_STYLISTIC_ALTERNATIVES
                 */
                readonly "STYLISTIC_ALT_SEVENTEEN_ON": 34
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_STYLISTIC_ALTERNATIVES
                 */
                readonly "STYLISTIC_ALT_SEVENTEEN_OFF": 35
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_STYLISTIC_ALTERNATIVES
                 */
                readonly "STYLISTIC_ALT_EIGHTEEN_ON": 36
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_STYLISTIC_ALTERNATIVES
                 */
                readonly "STYLISTIC_ALT_EIGHTEEN_OFF": 37
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_STYLISTIC_ALTERNATIVES
                 */
                readonly "STYLISTIC_ALT_NINETEEN_ON": 38
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_STYLISTIC_ALTERNATIVES
                 */
                readonly "STYLISTIC_ALT_NINETEEN_OFF": 39
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_STYLISTIC_ALTERNATIVES
                 */
                readonly "STYLISTIC_ALT_TWENTY_ON": 40
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_STYLISTIC_ALTERNATIVES
                 */
                readonly "STYLISTIC_ALT_TWENTY_OFF": 41
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_CONTEXTUAL_ALTERNATIVES
                 */
                readonly "CONTEXTUAL_ALTERNATES_ON": 0
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_CONTEXTUAL_ALTERNATIVES
                 */
                readonly "CONTEXTUAL_ALTERNATES_OFF": 1
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_CONTEXTUAL_ALTERNATIVES
                 */
                readonly "SWASH_ALTERNATES_ON": 2
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_CONTEXTUAL_ALTERNATIVES
                 */
                readonly "SWASH_ALTERNATES_OFF": 3
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_CONTEXTUAL_ALTERNATIVES
                 */
                readonly "CONTEXTUAL_SWASH_ALTERNATES_ON": 4
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_CONTEXTUAL_ALTERNATIVES
                 */
                readonly "CONTEXTUAL_SWASH_ALTERNATES_OFF": 5
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_LOWER_CASE
                 */
                readonly "DEFAULT_LOWER_CASE": 0
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_LOWER_CASE
                 */
                readonly "LOWER_CASE_SMALL_CAPS": 1
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_LOWER_CASE
                 */
                readonly "LOWER_CASE_PETITE_CAPS": 2
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_UPPER_CASE
                 */
                readonly "DEFAULT_UPPER_CASE": 0
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_UPPER_CASE
                 */
                readonly "UPPER_CASE_SMALL_CAPS": 1
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_UPPER_CASE
                 */
                readonly "UPPER_CASE_PETITE_CAPS": 2
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_CJK_ROMAN_SPACING_TYPE
                 */
                readonly "HALF_WIDTH_CJK_ROMAN": 0
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_CJK_ROMAN_SPACING_TYPE
                 */
                readonly "PROPORTIONAL_CJK_ROMAN": 1
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_CJK_ROMAN_SPACING_TYPE
                 */
                readonly "DEFAULT_CJK_ROMAN": 2
                /**
                 * for #HB_AAT_LAYOUT_FEATURE_TYPE_CJK_ROMAN_SPACING_TYPE
                 */
                readonly "FULL_WIDTH_CJK_ROMAN": 3
            }
            type aat_layout_feature_selector_t = aat_layout_feature_selector_tEnum[Exclude<keyof aat_layout_feature_selector_tEnum, "$gtype">]
            interface $Exports {
                /**
                 * The selectors defined for specifying AAT feature settings.
                 * @since 2.2.0
                 */
                aat_layout_feature_selector_t: aat_layout_feature_selector_tEnum
            }
            
            interface aat_layout_feature_type_tEnum {
                readonly $gtype: GObject.GType<aat_layout_feature_type_t>
                /**
                 * Initial, unset feature type
                 */
                readonly "INVALID": 65535
                /**
                 * [All Typographic Features](https://developer.apple.com/fonts/TrueType-Reference-Manual/RM09/AppendixF.html#Type0)
                 */
                readonly "ALL_TYPOGRAPHIC": 0
                /**
                 * [Ligatures](https://developer.apple.com/fonts/TrueType-Reference-Manual/RM09/AppendixF.html#Type1)
                 */
                readonly "LIGATURES": 1
                /**
                 * [Cursive Connection](https://developer.apple.com/fonts/TrueType-Reference-Manual/RM09/AppendixF.html#Type2)
                 */
                readonly "CURSIVE_CONNECTION": 2
                /**
                 * [Letter Case](https://developer.apple.com/fonts/TrueType-Reference-Manual/RM09/AppendixF.html#Type3)
                 */
                readonly "LETTER_CASE": 3
                /**
                 * [Vertical Substitution](https://developer.apple.com/fonts/TrueType-Reference-Manual/RM09/AppendixF.html#Type4)
                 */
                readonly "VERTICAL_SUBSTITUTION": 4
                /**
                 * [Linguistic Rearrangement](https://developer.apple.com/fonts/TrueType-Reference-Manual/RM09/AppendixF.html#Type5)
                 */
                readonly "LINGUISTIC_REARRANGEMENT": 5
                /**
                 * [Number Spacing](https://developer.apple.com/fonts/TrueType-Reference-Manual/RM09/AppendixF.html#Type6)
                 */
                readonly "NUMBER_SPACING": 6
                /**
                 * [Smart Swash](https://developer.apple.com/fonts/TrueType-Reference-Manual/RM09/AppendixF.html#Type8)
                 */
                readonly "SMART_SWASH_TYPE": 8
                /**
                 * [Diacritics](https://developer.apple.com/fonts/TrueType-Reference-Manual/RM09/AppendixF.html#Type9)
                 */
                readonly "DIACRITICS_TYPE": 9
                /**
                 * [Vertical Position](https://developer.apple.com/fonts/TrueType-Reference-Manual/RM09/AppendixF.html#Type10)
                 */
                readonly "VERTICAL_POSITION": 10
                /**
                 * [Fractions](https://developer.apple.com/fonts/TrueType-Reference-Manual/RM09/AppendixF.html#Type11)
                 */
                readonly "FRACTIONS": 11
                /**
                 * [Overlapping Characters](https://developer.apple.com/fonts/TrueType-Reference-Manual/RM09/AppendixF.html#Type13)
                 */
                readonly "OVERLAPPING_CHARACTERS_TYPE": 13
                /**
                 * [Typographic Extras](https://developer.apple.com/fonts/TrueType-Reference-Manual/RM09/AppendixF.html#Type14)
                 */
                readonly "TYPOGRAPHIC_EXTRAS": 14
                /**
                 * [Mathematical Extras](https://developer.apple.com/fonts/TrueType-Reference-Manual/RM09/AppendixF.html#Type15)
                 */
                readonly "MATHEMATICAL_EXTRAS": 15
                /**
                 * [Ornament Sets](https://developer.apple.com/fonts/TrueType-Reference-Manual/RM09/AppendixF.html#Type16)
                 */
                readonly "ORNAMENT_SETS_TYPE": 16
                /**
                 * [Character Alternatives](https://developer.apple.com/fonts/TrueType-Reference-Manual/RM09/AppendixF.html#Type17)
                 */
                readonly "CHARACTER_ALTERNATIVES": 17
                /**
                 * [Design Complexity](https://developer.apple.com/fonts/TrueType-Reference-Manual/RM09/AppendixF.html#Type18)
                 */
                readonly "DESIGN_COMPLEXITY_TYPE": 18
                /**
                 * [Style Options](https://developer.apple.com/fonts/TrueType-Reference-Manual/RM09/AppendixF.html#Type19)
                 */
                readonly "STYLE_OPTIONS": 19
                /**
                 * [Character Shape](https://developer.apple.com/fonts/TrueType-Reference-Manual/RM09/AppendixF.html#Type20)
                 */
                readonly "CHARACTER_SHAPE": 20
                /**
                 * [Number Case](https://developer.apple.com/fonts/TrueType-Reference-Manual/RM09/AppendixF.html#Type21)
                 */
                readonly "NUMBER_CASE": 21
                /**
                 * [Text Spacing](https://developer.apple.com/fonts/TrueType-Reference-Manual/RM09/AppendixF.html#Type22)
                 */
                readonly "TEXT_SPACING": 22
                /**
                 * [Transliteration](https://developer.apple.com/fonts/TrueType-Reference-Manual/RM09/AppendixF.html#Type23)
                 */
                readonly "TRANSLITERATION": 23
                /**
                 * [Annotation](https://developer.apple.com/fonts/TrueType-Reference-Manual/RM09/AppendixF.html#Type24)
                 */
                readonly "ANNOTATION_TYPE": 24
                /**
                 * [Kana Spacing](https://developer.apple.com/fonts/TrueType-Reference-Manual/RM09/AppendixF.html#Type25)
                 */
                readonly "KANA_SPACING_TYPE": 25
                /**
                 * [Ideographic Spacing](https://developer.apple.com/fonts/TrueType-Reference-Manual/RM09/AppendixF.html#Type26)
                 */
                readonly "IDEOGRAPHIC_SPACING_TYPE": 26
                /**
                 * [Unicode Decomposition](https://developer.apple.com/fonts/TrueType-Reference-Manual/RM09/AppendixF.html#Type27)
                 */
                readonly "UNICODE_DECOMPOSITION_TYPE": 27
                /**
                 * [Ruby Kana](https://developer.apple.com/fonts/TrueType-Reference-Manual/RM09/AppendixF.html#Type28)
                 */
                readonly "RUBY_KANA": 28
                /**
                 * [CJK Symbol Alternatives](https://developer.apple.com/fonts/TrueType-Reference-Manual/RM09/AppendixF.html#Type29)
                 */
                readonly "CJK_SYMBOL_ALTERNATIVES_TYPE": 29
                /**
                 * [Ideographic Alternatives](https://developer.apple.com/fonts/TrueType-Reference-Manual/RM09/AppendixF.html#Type30)
                 */
                readonly "IDEOGRAPHIC_ALTERNATIVES_TYPE": 30
                /**
                 * [CJK Vertical Roman Placement](https://developer.apple.com/fonts/TrueType-Reference-Manual/RM09/AppendixF.html#Type31)
                 */
                readonly "CJK_VERTICAL_ROMAN_PLACEMENT_TYPE": 31
                /**
                 * [Italic CJK Roman](https://developer.apple.com/fonts/TrueType-Reference-Manual/RM09/AppendixF.html#Type32)
                 */
                readonly "ITALIC_CJK_ROMAN": 32
                /**
                 * [Case Sensitive Layout](https://developer.apple.com/fonts/TrueType-Reference-Manual/RM09/AppendixF.html#Type33)
                 */
                readonly "CASE_SENSITIVE_LAYOUT": 33
                /**
                 * [Alternate Kana](https://developer.apple.com/fonts/TrueType-Reference-Manual/RM09/AppendixF.html#Type34)
                 */
                readonly "ALTERNATE_KANA": 34
                /**
                 * [Stylistic Alternatives](https://developer.apple.com/fonts/TrueType-Reference-Manual/RM09/AppendixF.html#Type35)
                 */
                readonly "STYLISTIC_ALTERNATIVES": 35
                /**
                 * [Contextual Alternatives](https://developer.apple.com/fonts/TrueType-Reference-Manual/RM09/AppendixF.html#Type36)
                 */
                readonly "CONTEXTUAL_ALTERNATIVES": 36
                /**
                 * [Lower Case](https://developer.apple.com/fonts/TrueType-Reference-Manual/RM09/AppendixF.html#Type37)
                 */
                readonly "LOWER_CASE": 37
                /**
                 * [Upper Case](https://developer.apple.com/fonts/TrueType-Reference-Manual/RM09/AppendixF.html#Type38)
                 */
                readonly "UPPER_CASE": 38
                /**
                 * [Language Tag](https://developer.apple.com/fonts/TrueType-Reference-Manual/RM09/AppendixF.html#Type39)
                 */
                readonly "LANGUAGE_TAG_TYPE": 39
                /**
                 * [CJK Roman Spacing](https://developer.apple.com/fonts/TrueType-Reference-Manual/RM09/AppendixF.html#Type103)
                 */
                readonly "CJK_ROMAN_SPACING_TYPE": 103
            }
            type aat_layout_feature_type_t = aat_layout_feature_type_tEnum[Exclude<keyof aat_layout_feature_type_tEnum, "$gtype">]
            interface $Exports {
                /**
                 * The possible feature types defined for AAT shaping, from Apple [Font Feature Registry](https://developer.apple.com/fonts/TrueType-Reference-Manual/RM09/AppendixF.html).
                 * @since 2.2.0
                 */
                aat_layout_feature_type_t: aat_layout_feature_type_tEnum
            }
            
            interface buffer_cluster_level_tEnum {
                readonly $gtype: GObject.GType<buffer_cluster_level_t>
                /**
                 * Return cluster values grouped by graphemes into
                 *   monotone order.
                 */
                readonly "MONOTONE_GRAPHEMES": 0
                /**
                 * Return cluster values grouped into monotone order.
                 */
                readonly "MONOTONE_CHARACTERS": 1
                /**
                 * Don't group cluster values.
                 */
                readonly "CHARACTERS": 2
                /**
                 * Only group clusters, but don't enforce monotone order.
                 */
                readonly "GRAPHEMES": 3
                /**
                 * Default cluster level,
                 *   equal to `HB_BUFFER_CLUSTER_LEVEL_MONOTONE_GRAPHEMES`.
                 */
                readonly "DEFAULT": 0
            }
            type buffer_cluster_level_t = buffer_cluster_level_tEnum[Exclude<keyof buffer_cluster_level_tEnum, "$gtype">]
            interface $Exports {
                /**
                 * Data type for holding HarfBuzz's clustering behavior options. The cluster level
                 * dictates one aspect of how HarfBuzz will treat non-base characters
                 * during shaping.
                 *
                 * In `HB_BUFFER_CLUSTER_LEVEL_MONOTONE_GRAPHEMES`, non-base
                 * characters are merged into the cluster of the base character that precedes them.
                 * There is also cluster merging every time the clusters will otherwise become non-monotone.
                 *
                 * In `HB_BUFFER_CLUSTER_LEVEL_MONOTONE_CHARACTERS`, non-base characters are initially
                 * assigned their own cluster values, which are not merged into preceding base
                 * clusters. This allows HarfBuzz to perform additional operations like reorder
                 * sequences of adjacent marks. The output is still monotone, but the cluster
                 * values are more granular.
                 *
                 * In `HB_BUFFER_CLUSTER_LEVEL_CHARACTERS`, non-base characters are assigned their
                 * own cluster values, which are not merged into preceding base clusters. Moreover,
                 * the cluster values are not merged into monotone order. This is the most granular
                 * cluster level, and it is useful for clients that need to know the exact cluster
                 * values of each character, but is harder to use for clients, since clusters
                 * might appear in any order.
                 *
                 * In `HB_BUFFER_CLUSTER_LEVEL_GRAPHEMES`, non-base characters are merged into the
                 * cluster of the base character that precedes them. This is similar to the Unicode
                 * Grapheme Cluster algorithm, but it is not exactly the same. The output is
                 * not forced to be monotone. This is useful for clients that want to use HarfBuzz
                 * as a cheap implementation of the Unicode Grapheme Cluster algorithm.
                 *
                 *  `HB_BUFFER_CLUSTER_LEVEL_MONOTONE_GRAPHEMES` is the default, because it maintains
                 * backward compatibility with older versions of HarfBuzz. New client programs that
                 * do not need to maintain such backward compatibility are recommended to use
                 *  `HB_BUFFER_CLUSTER_LEVEL_MONOTONE_CHARACTERS` instead of the default.
                 * @since 0.9.42
                 */
                buffer_cluster_level_t: buffer_cluster_level_tEnum
            }
            
            interface buffer_content_type_tEnum {
                readonly $gtype: GObject.GType<buffer_content_type_t>
                /**
                 * Initial value for new buffer.
                 */
                readonly "INVALID": 0
                /**
                 * The buffer contains input characters (before shaping).
                 */
                readonly "UNICODE": 1
                /**
                 * The buffer contains output glyphs (after shaping).
                 */
                readonly "GLYPHS": 2
            }
            type buffer_content_type_t = buffer_content_type_tEnum[Exclude<keyof buffer_content_type_tEnum, "$gtype">]
            interface $Exports {
                /**
                 * The type of #hb_buffer_t contents.
                 */
                buffer_content_type_t: buffer_content_type_tEnum
            }
            
            interface buffer_serialize_format_tEnum {
                readonly $gtype: GObject.GType<buffer_serialize_format_t>
                /**
                 * a human-readable, plain text format.
                 */
                readonly "TEXT": 1413830740
                /**
                 * a machine-readable JSON format.
                 */
                readonly "JSON": 1246973774
                /**
                 * invalid format.
                 */
                readonly "INVALID": 0
            }
            type buffer_serialize_format_t = buffer_serialize_format_tEnum[Exclude<keyof buffer_serialize_format_tEnum, "$gtype">]
            interface $Exports {
                /**
                 * The buffer serialization and de-serialization format used in
                 * hb_buffer_serialize_glyphs() and hb_buffer_deserialize_glyphs().
                 * @since 0.9.2
                 */
                buffer_serialize_format_t: buffer_serialize_format_tEnum
            }
            
            interface direction_tEnum {
                readonly $gtype: GObject.GType<direction_t>
                /**
                 * Initial, unset direction.
                 */
                readonly "INVALID": 0
                /**
                 * Text is set horizontally from left to right.
                 */
                readonly "LTR": 4
                /**
                 * Text is set horizontally from right to left.
                 */
                readonly "RTL": 5
                /**
                 * Text is set vertically from top to bottom.
                 */
                readonly "TTB": 6
                /**
                 * Text is set vertically from bottom to top.
                 */
                readonly "BTT": 7
            }
            type direction_t = direction_tEnum[Exclude<keyof direction_tEnum, "$gtype">]
            interface $Exports {
                /**
                 * The direction of a text segment or buffer.
                 *
                 * A segment can also be tested for horizontal or vertical
                 * orientation (irrespective of specific direction) with
                 * HB_DIRECTION_IS_HORIZONTAL() or HB_DIRECTION_IS_VERTICAL().
                 */
                direction_t: direction_tEnum
            }
            
            interface draw_line_cap_tEnum {
                readonly $gtype: GObject.GType<draw_line_cap_t>
                /**
                 * No cap; the line ends exactly at
                 *   its endpoint.
                 */
                readonly "BUTT": 0
                /**
                 * Square cap; the line is extended
                 *   past its endpoint by half the local stroke width.  Useful
                 *   for composing closed shapes from line segments (e.g. a
                 *   rectangle made from four lines).
                 */
                readonly "SQUARE": 1
            }
            type draw_line_cap_t = draw_line_cap_tEnum[Exclude<keyof draw_line_cap_tEnum, "$gtype">]
            interface $Exports {
                /**
                 * End-cap shape for hb_draw_line().
                 * @since 14.2.0
                 */
                draw_line_cap_t: draw_line_cap_tEnum
            }
            
            interface memory_mode_tEnum {
                readonly $gtype: GObject.GType<memory_mode_t>
                /**
                 * HarfBuzz immediately makes a copy of the data.
                 */
                readonly "DUPLICATE": 0
                /**
                 * HarfBuzz client will never modify the data,
                 *     and HarfBuzz will never modify the data.
                 */
                readonly "READONLY": 1
                /**
                 * HarfBuzz client made a copy of the data solely
                 *     for HarfBuzz, so HarfBuzz may modify the data.
                 */
                readonly "WRITABLE": 2
                /**
                 * See above
                 */
                readonly "READONLY_MAY_MAKE_WRITABLE": 3
            }
            type memory_mode_t = memory_mode_tEnum[Exclude<keyof memory_mode_tEnum, "$gtype">]
            interface $Exports {
                /**
                 * Data type holding the memory modes available to
                 * client programs.
                 *
                 * Regarding these various memory-modes:
                 *
                 * - In no case shall the HarfBuzz client modify memory
                 *   that is passed to HarfBuzz in a blob.  If there is
                 *   any such possibility, `HB_MEMORY_MODE_DUPLICATE` should be used
                 *   such that HarfBuzz makes a copy immediately,
                 *
                 * - Use `HB_MEMORY_MODE_READONLY` otherwise, unless you really really
                 *   really know what you are doing,
                 *
                 * - `HB_MEMORY_MODE_WRITABLE` is appropriate if you really made a
                 *   copy of data solely for the purpose of passing to
                 *   HarfBuzz and doing that just once (no reuse!),
                 *
                 * - If the font is mmap()ed, it's okay to use
                 *   `HB_MEMORY_MODE_READONLY_MAY_MAKE_WRITABLE`, however, using that mode
                 *   correctly is very tricky.  Use `HB_MEMORY_MODE_READONLY` instead.
                 */
                memory_mode_t: memory_mode_tEnum
            }
            
            interface ot_bits_tag_tEnum {
                readonly $gtype: GObject.GType<ot_bits_tag_t>
                /**
                 * `fsType` of the `OS/2` table.
                 */
                readonly "FS_TYPE": 1718842480
                /**
                 * `fsSelection` of the `OS/2` table.
                 */
                readonly "FS_SELECTION": 1718842220
                /**
                 * `macStyle` of the `head` table.
                 */
                readonly "MAC_STYLE": 1835234164
                /**
                 * `isFixedPitch` of the `post` table.
                 */
                readonly "IS_FIXED_PITCH": 1719169140
                /**
                 * `ulUnicodeRange1` of the `OS/2` table.
                 */
                readonly "UNICODE_RANGE_1": 1970433585
                /**
                 * `ulUnicodeRange2` of the `OS/2` table.
                 */
                readonly "UNICODE_RANGE_2": 1970433586
                /**
                 * `ulUnicodeRange3` of the `OS/2` table.
                 */
                readonly "UNICODE_RANGE_3": 1970433587
                /**
                 * `ulUnicodeRange4` of the `OS/2` table.
                 */
                readonly "UNICODE_RANGE_4": 1970433588
                /**
                 * `ulCodePageRange1` of the `OS/2` table.
                 */
                readonly "CODE_PAGE_RANGE_1": 1668313649
                /**
                 * `ulCodePageRange2` of the `OS/2` table.
                 */
                readonly "CODE_PAGE_RANGE_2": 1668313650
            }
            type ot_bits_tag_t = ot_bits_tag_tEnum[Exclude<keyof ot_bits_tag_tEnum, "$gtype">]
            interface $Exports {
                /**
                 * Bit fields that can be fetched with hb_ot_fetch_bits().
                 * @since 14.3.0
                 */
                ot_bits_tag_t: ot_bits_tag_tEnum
            }
            
            interface ot_layout_baseline_tag_tEnum {
                readonly $gtype: GObject.GType<ot_layout_baseline_tag_t>
                /**
                 * The baseline used by alphabetic scripts such as Latin, Cyrillic and Greek.
                 * In vertical writing mode, the alphabetic baseline for characters rotated 90 degrees clockwise.
                 * (This would not apply to alphabetic characters that remain upright in vertical writing mode, since these
                 * characters are not rotated.)
                 */
                readonly "ROMAN": 1919905134
                /**
                 * The hanging baseline. In horizontal direction, this is the horizontal
                 * line from which syllables seem, to hang in Tibetan and other similar scripts. In vertical writing mode,
                 * for Tibetan (or some other similar script) characters rotated 90 degrees clockwise.
                 */
                readonly "HANGING": 1751215719
                /**
                 * Ideographic character face bottom or left edge,
                 * if the direction is horizontal or vertical, respectively.
                 */
                readonly "IDEO_FACE_BOTTOM_OR_LEFT": 1768121954
                /**
                 * Ideographic character face top or right edge,
                 * if the direction is horizontal or vertical, respectively.
                 */
                readonly "IDEO_FACE_TOP_OR_RIGHT": 1768121972
                /**
                 * The center of the ideographic character face. Since: 4.0.0
                 */
                readonly "IDEO_FACE_CENTRAL": 1231251043
                /**
                 * Ideographic em-box bottom or left edge,
                 * if the direction is horizontal or vertical, respectively.
                 */
                readonly "IDEO_EMBOX_BOTTOM_OR_LEFT": 1768187247
                /**
                 * Ideographic em-box top or right edge baseline,
                 */
                readonly "IDEO_EMBOX_TOP_OR_RIGHT": 1768191088
                /**
                 * The center of the ideographic em-box. Since: 4.0.0
                 * if the direction is horizontal or vertical, respectively.
                 */
                readonly "IDEO_EMBOX_CENTRAL": 1231315813
                /**
                 * The baseline about which mathematical characters are centered.
                 * In vertical writing mode when mathematical characters rotated 90 degrees clockwise, are centered.
                 */
                readonly "MATH": 1835103336
            }
            type ot_layout_baseline_tag_t = ot_layout_baseline_tag_tEnum[Exclude<keyof ot_layout_baseline_tag_tEnum, "$gtype">]
            interface $Exports {
                /**
                 * Baseline tags from [Baseline Tags](https://docs.microsoft.com/en-us/typography/opentype/spec/baselinetags) registry.
                 * @since 2.6.0
                 */
                ot_layout_baseline_tag_t: ot_layout_baseline_tag_tEnum
            }
            
            interface ot_layout_glyph_class_tEnum {
                readonly $gtype: GObject.GType<ot_layout_glyph_class_t>
                /**
                 * Glyphs not matching the other classifications
                 */
                readonly "UNCLASSIFIED": 0
                /**
                 * Spacing, single characters, capable of accepting marks
                 */
                readonly "BASE_GLYPH": 1
                /**
                 * Glyphs that represent ligation of multiple characters
                 */
                readonly "LIGATURE": 2
                /**
                 * Non-spacing, combining glyphs that represent marks
                 */
                readonly "MARK": 3
                /**
                 * Spacing glyphs that represent part of a single character
                 */
                readonly "COMPONENT": 4
            }
            type ot_layout_glyph_class_t = ot_layout_glyph_class_tEnum[Exclude<keyof ot_layout_glyph_class_tEnum, "$gtype">]
            interface $Exports {
                /**
                 * The GDEF classes defined for glyphs.
                 */
                ot_layout_glyph_class_t: ot_layout_glyph_class_tEnum
            }
            
            interface ot_math_constant_tEnum {
                readonly $gtype: GObject.GType<ot_math_constant_t>
                /**
                 * scriptPercentScaleDown
                 */
                readonly "SCRIPT_PERCENT_SCALE_DOWN": 0
                /**
                 * scriptScriptPercentScaleDown
                 */
                readonly "SCRIPT_SCRIPT_PERCENT_SCALE_DOWN": 1
                /**
                 * delimitedSubFormulaMinHeight
                 */
                readonly "DELIMITED_SUB_FORMULA_MIN_HEIGHT": 2
                /**
                 * displayOperatorMinHeight
                 */
                readonly "DISPLAY_OPERATOR_MIN_HEIGHT": 3
                /**
                 * mathLeading
                 */
                readonly "MATH_LEADING": 4
                /**
                 * axisHeight
                 */
                readonly "AXIS_HEIGHT": 5
                /**
                 * accentBaseHeight
                 */
                readonly "ACCENT_BASE_HEIGHT": 6
                /**
                 * flattenedAccentBaseHeight
                 */
                readonly "FLATTENED_ACCENT_BASE_HEIGHT": 7
                /**
                 * subscriptShiftDown
                 */
                readonly "SUBSCRIPT_SHIFT_DOWN": 8
                /**
                 * subscriptTopMax
                 */
                readonly "SUBSCRIPT_TOP_MAX": 9
                /**
                 * subscriptBaselineDropMin
                 */
                readonly "SUBSCRIPT_BASELINE_DROP_MIN": 10
                /**
                 * superscriptShiftUp
                 */
                readonly "SUPERSCRIPT_SHIFT_UP": 11
                /**
                 * superscriptShiftUpCramped
                 */
                readonly "SUPERSCRIPT_SHIFT_UP_CRAMPED": 12
                /**
                 * superscriptBottomMin
                 */
                readonly "SUPERSCRIPT_BOTTOM_MIN": 13
                /**
                 * superscriptBaselineDropMax
                 */
                readonly "SUPERSCRIPT_BASELINE_DROP_MAX": 14
                /**
                 * subSuperscriptGapMin
                 */
                readonly "SUB_SUPERSCRIPT_GAP_MIN": 15
                /**
                 * superscriptBottomMaxWithSubscript
                 */
                readonly "SUPERSCRIPT_BOTTOM_MAX_WITH_SUBSCRIPT": 16
                /**
                 * spaceAfterScript
                 */
                readonly "SPACE_AFTER_SCRIPT": 17
                /**
                 * upperLimitGapMin
                 */
                readonly "UPPER_LIMIT_GAP_MIN": 18
                /**
                 * upperLimitBaselineRiseMin
                 */
                readonly "UPPER_LIMIT_BASELINE_RISE_MIN": 19
                /**
                 * lowerLimitGapMin
                 */
                readonly "LOWER_LIMIT_GAP_MIN": 20
                /**
                 * lowerLimitBaselineDropMin
                 */
                readonly "LOWER_LIMIT_BASELINE_DROP_MIN": 21
                /**
                 * stackTopShiftUp
                 */
                readonly "STACK_TOP_SHIFT_UP": 22
                /**
                 * stackTopDisplayStyleShiftUp
                 */
                readonly "STACK_TOP_DISPLAY_STYLE_SHIFT_UP": 23
                /**
                 * stackBottomShiftDown
                 */
                readonly "STACK_BOTTOM_SHIFT_DOWN": 24
                /**
                 * stackBottomDisplayStyleShiftDown
                 */
                readonly "STACK_BOTTOM_DISPLAY_STYLE_SHIFT_DOWN": 25
                /**
                 * stackGapMin
                 */
                readonly "STACK_GAP_MIN": 26
                /**
                 * stackDisplayStyleGapMin
                 */
                readonly "STACK_DISPLAY_STYLE_GAP_MIN": 27
                /**
                 * stretchStackTopShiftUp
                 */
                readonly "STRETCH_STACK_TOP_SHIFT_UP": 28
                /**
                 * stretchStackBottomShiftDown
                 */
                readonly "STRETCH_STACK_BOTTOM_SHIFT_DOWN": 29
                /**
                 * stretchStackGapAboveMin
                 */
                readonly "STRETCH_STACK_GAP_ABOVE_MIN": 30
                /**
                 * stretchStackGapBelowMin
                 */
                readonly "STRETCH_STACK_GAP_BELOW_MIN": 31
                /**
                 * fractionNumeratorShiftUp
                 */
                readonly "FRACTION_NUMERATOR_SHIFT_UP": 32
                /**
                 * fractionNumeratorDisplayStyleShiftUp
                 */
                readonly "FRACTION_NUMERATOR_DISPLAY_STYLE_SHIFT_UP": 33
                /**
                 * fractionDenominatorShiftDown
                 */
                readonly "FRACTION_DENOMINATOR_SHIFT_DOWN": 34
                /**
                 * fractionDenominatorDisplayStyleShiftDown
                 */
                readonly "FRACTION_DENOMINATOR_DISPLAY_STYLE_SHIFT_DOWN": 35
                /**
                 * fractionNumeratorGapMin
                 */
                readonly "FRACTION_NUMERATOR_GAP_MIN": 36
                /**
                 * fractionNumDisplayStyleGapMin
                 */
                readonly "FRACTION_NUM_DISPLAY_STYLE_GAP_MIN": 37
                /**
                 * fractionRuleThickness
                 */
                readonly "FRACTION_RULE_THICKNESS": 38
                /**
                 * fractionDenominatorGapMin
                 */
                readonly "FRACTION_DENOMINATOR_GAP_MIN": 39
                /**
                 * fractionDenomDisplayStyleGapMin
                 */
                readonly "FRACTION_DENOM_DISPLAY_STYLE_GAP_MIN": 40
                /**
                 * skewedFractionHorizontalGap
                 */
                readonly "SKEWED_FRACTION_HORIZONTAL_GAP": 41
                /**
                 * skewedFractionVerticalGap
                 */
                readonly "SKEWED_FRACTION_VERTICAL_GAP": 42
                /**
                 * overbarVerticalGap
                 */
                readonly "OVERBAR_VERTICAL_GAP": 43
                /**
                 * overbarRuleThickness
                 */
                readonly "OVERBAR_RULE_THICKNESS": 44
                /**
                 * overbarExtraAscender
                 */
                readonly "OVERBAR_EXTRA_ASCENDER": 45
                /**
                 * underbarVerticalGap
                 */
                readonly "UNDERBAR_VERTICAL_GAP": 46
                /**
                 * underbarRuleThickness
                 */
                readonly "UNDERBAR_RULE_THICKNESS": 47
                /**
                 * underbarExtraDescender
                 */
                readonly "UNDERBAR_EXTRA_DESCENDER": 48
                /**
                 * radicalVerticalGap
                 */
                readonly "RADICAL_VERTICAL_GAP": 49
                /**
                 * radicalDisplayStyleVerticalGap
                 */
                readonly "RADICAL_DISPLAY_STYLE_VERTICAL_GAP": 50
                /**
                 * radicalRuleThickness
                 */
                readonly "RADICAL_RULE_THICKNESS": 51
                /**
                 * radicalExtraAscender
                 */
                readonly "RADICAL_EXTRA_ASCENDER": 52
                /**
                 * radicalKernBeforeDegree
                 */
                readonly "RADICAL_KERN_BEFORE_DEGREE": 53
                /**
                 * radicalKernAfterDegree
                 */
                readonly "RADICAL_KERN_AFTER_DEGREE": 54
                /**
                 * radicalDegreeBottomRaisePercent
                 */
                readonly "RADICAL_DEGREE_BOTTOM_RAISE_PERCENT": 55
            }
            type ot_math_constant_t = ot_math_constant_tEnum[Exclude<keyof ot_math_constant_tEnum, "$gtype">]
            interface $Exports {
                /**
                 * The 'MATH' table constants, refer to
                 * [OpenType documentation](https://docs.microsoft.com/en-us/typography/opentype/spec/math#mathconstants-table)
                 * For more explanations.
                 * @since 1.3.3
                 */
                ot_math_constant_t: ot_math_constant_tEnum
            }
            
            interface ot_math_kern_tEnum {
                readonly $gtype: GObject.GType<ot_math_kern_t>
                /**
                 * The top right corner of the glyph.
                 */
                readonly "TOP_RIGHT": 0
                /**
                 * The top left corner of the glyph.
                 */
                readonly "TOP_LEFT": 1
                /**
                 * The bottom right corner of the glyph.
                 */
                readonly "BOTTOM_RIGHT": 2
                /**
                 * The bottom left corner of the glyph.
                 */
                readonly "BOTTOM_LEFT": 3
            }
            type ot_math_kern_t = ot_math_kern_tEnum[Exclude<keyof ot_math_kern_tEnum, "$gtype">]
            interface $Exports {
                /**
                 * The math kerning-table types defined for the four corners
                 * of a glyph.
                 * @since 1.3.3
                 */
                ot_math_kern_t: ot_math_kern_tEnum
            }
            
            interface ot_meta_tag_tEnum {
                readonly $gtype: GObject.GType<ot_meta_tag_t>
                /**
                 * Design languages. Text, using only
                 * Basic Latin (ASCII) characters. Indicates languages and/or scripts
                 * for the user audiences that the font was primarily designed for.
                 */
                readonly "DESIGN_LANGUAGES": 1684827751
                /**
                 * Supported languages. Text, using
                 * only Basic Latin (ASCII) characters. Indicates languages and/or scripts
                 * that the font is declared to be capable of supporting.
                 */
                readonly "SUPPORTED_LANGUAGES": 1936485991
            }
            type ot_meta_tag_t = ot_meta_tag_tEnum[Exclude<keyof ot_meta_tag_tEnum, "$gtype">]
            interface $Exports {
                /**
                 * Known metadata tags from https://docs.microsoft.com/en-us/typography/opentype/spec/meta
                 * @since 2.6.0
                 */
                ot_meta_tag_t: ot_meta_tag_tEnum
            }
            
            interface ot_metrics_tag_tEnum {
                readonly $gtype: GObject.GType<ot_metrics_tag_t>
                /**
                 * horizontal ascender.
                 */
                readonly "HORIZONTAL_ASCENDER": 1751216995
                /**
                 * horizontal descender.
                 */
                readonly "HORIZONTAL_DESCENDER": 1751413603
                /**
                 * horizontal line gap.
                 */
                readonly "HORIZONTAL_LINE_GAP": 1751934832
                /**
                 * horizontal clipping ascent.
                 */
                readonly "HORIZONTAL_CLIPPING_ASCENT": 1751346273
                /**
                 * horizontal clipping descent.
                 */
                readonly "HORIZONTAL_CLIPPING_DESCENT": 1751346276
                /**
                 * vertical ascender.
                 */
                readonly "VERTICAL_ASCENDER": 1986098019
                /**
                 * vertical descender.
                 */
                readonly "VERTICAL_DESCENDER": 1986294627
                /**
                 * vertical line gap.
                 */
                readonly "VERTICAL_LINE_GAP": 1986815856
                /**
                 * horizontal caret rise.
                 */
                readonly "HORIZONTAL_CARET_RISE": 1751347827
                /**
                 * horizontal caret run.
                 */
                readonly "HORIZONTAL_CARET_RUN": 1751347822
                /**
                 * horizontal caret offset.
                 */
                readonly "HORIZONTAL_CARET_OFFSET": 1751347046
                /**
                 * vertical caret rise.
                 */
                readonly "VERTICAL_CARET_RISE": 1986228851
                /**
                 * vertical caret run.
                 */
                readonly "VERTICAL_CARET_RUN": 1986228846
                /**
                 * vertical caret offset.
                 */
                readonly "VERTICAL_CARET_OFFSET": 1986228070
                /**
                 * x height.
                 */
                readonly "X_HEIGHT": 2020108148
                /**
                 * cap height.
                 */
                readonly "CAP_HEIGHT": 1668311156
                /**
                 * subscript em x size.
                 */
                readonly "SUBSCRIPT_EM_X_SIZE": 1935833203
                /**
                 * subscript em y size.
                 */
                readonly "SUBSCRIPT_EM_Y_SIZE": 1935833459
                /**
                 * subscript em x offset.
                 */
                readonly "SUBSCRIPT_EM_X_OFFSET": 1935833199
                /**
                 * subscript em y offset.
                 */
                readonly "SUBSCRIPT_EM_Y_OFFSET": 1935833455
                /**
                 * superscript em x size.
                 */
                readonly "SUPERSCRIPT_EM_X_SIZE": 1936750707
                /**
                 * superscript em y size.
                 */
                readonly "SUPERSCRIPT_EM_Y_SIZE": 1936750963
                /**
                 * superscript em x offset.
                 */
                readonly "SUPERSCRIPT_EM_X_OFFSET": 1936750703
                /**
                 * superscript em y offset.
                 */
                readonly "SUPERSCRIPT_EM_Y_OFFSET": 1936750959
                /**
                 * strikeout size.
                 */
                readonly "STRIKEOUT_SIZE": 1937011315
                /**
                 * strikeout offset.
                 */
                readonly "STRIKEOUT_OFFSET": 1937011311
                /**
                 * underline size.
                 */
                readonly "UNDERLINE_SIZE": 1970168947
                /**
                 * underline offset.
                 */
                readonly "UNDERLINE_OFFSET": 1970168943
            }
            type ot_metrics_tag_t = ot_metrics_tag_tEnum[Exclude<keyof ot_metrics_tag_tEnum, "$gtype">]
            interface $Exports {
                /**
                 * Metric tags corresponding to [MVAR Value
                 * Tags](https://docs.microsoft.com/en-us/typography/opentype/spec/mvar#value-tags)
                 * @since 2.6.0
                 */
                ot_metrics_tag_t: ot_metrics_tag_tEnum
            }
            
            interface ot_name_id_predefined_tEnum {
                readonly $gtype: GObject.GType<ot_name_id_predefined_t>
                /**
                 * Copyright notice
                 */
                readonly "COPYRIGHT": 0
                /**
                 * Font Family name
                 */
                readonly "FONT_FAMILY": 1
                /**
                 * Font Subfamily name
                 */
                readonly "FONT_SUBFAMILY": 2
                /**
                 * Unique font identifier
                 */
                readonly "UNIQUE_ID": 3
                /**
                 * Full font name that reflects
                 * all family and relevant subfamily descriptors
                 */
                readonly "FULL_NAME": 4
                /**
                 * Version string
                 */
                readonly "VERSION_STRING": 5
                /**
                 * PostScript name for the font
                 */
                readonly "POSTSCRIPT_NAME": 6
                /**
                 * Trademark
                 */
                readonly "TRADEMARK": 7
                /**
                 * Manufacturer Name
                 */
                readonly "MANUFACTURER": 8
                /**
                 * Designer
                 */
                readonly "DESIGNER": 9
                /**
                 * Description
                 */
                readonly "DESCRIPTION": 10
                /**
                 * URL of font vendor
                 */
                readonly "VENDOR_URL": 11
                /**
                 * URL of typeface designer
                 */
                readonly "DESIGNER_URL": 12
                /**
                 * License Description
                 */
                readonly "LICENSE": 13
                /**
                 * URL where additional licensing
                 * information can be found
                 */
                readonly "LICENSE_URL": 14
                /**
                 * Typographic Family name
                 */
                readonly "TYPOGRAPHIC_FAMILY": 16
                /**
                 * Typographic Subfamily name
                 */
                readonly "TYPOGRAPHIC_SUBFAMILY": 17
                /**
                 * Compatible Full Name for MacOS
                 */
                readonly "MAC_FULL_NAME": 18
                /**
                 * Sample text
                 */
                readonly "SAMPLE_TEXT": 19
                /**
                 * PostScript CID findfont name
                 */
                readonly "CID_FINDFONT_NAME": 20
                /**
                 * WWS Family Name
                 */
                readonly "WWS_FAMILY": 21
                /**
                 * WWS Subfamily Name
                 */
                readonly "WWS_SUBFAMILY": 22
                /**
                 * Light Background Palette
                 */
                readonly "LIGHT_BACKGROUND": 23
                /**
                 * Dark Background Palette
                 */
                readonly "DARK_BACKGROUND": 24
                /**
                 * Variations PostScript Name Prefix
                 */
                readonly "VARIATIONS_PS_PREFIX": 25
                /**
                 * Value to represent a nonexistent name ID.
                 */
                readonly "INVALID": 65535
            }
            type ot_name_id_predefined_t = ot_name_id_predefined_tEnum[Exclude<keyof ot_name_id_predefined_tEnum, "$gtype">]
            interface $Exports {
                /**
                 * An enum type representing the pre-defined name IDs.
                 *
                 * For more information on these fields, see the
                 * [OpenType spec](https://docs.microsoft.com/en-us/typography/opentype/spec/name#name-ids).
                 * @since 7.0.0
                 */
                ot_name_id_predefined_t: ot_name_id_predefined_tEnum
            }
            
            interface ot_number_tag_tEnum {
                readonly $gtype: GObject.GType<ot_number_tag_t>
                /**
                 * `xMin` of the `head` table.
                 */
                readonly "X_MIN": 2020436334
                /**
                 * `yMin` of the `head` table.
                 */
                readonly "Y_MIN": 2037213550
                /**
                 * `xMax` of the `head` table.
                 */
                readonly "X_MAX": 2020434296
                /**
                 * `yMax` of the `head` table.
                 */
                readonly "Y_MAX": 2037211512
            }
            type ot_number_tag_t = ot_number_tag_tEnum[Exclude<keyof ot_number_tag_tEnum, "$gtype">]
            interface $Exports {
                /**
                 * Numbers that can be fetched with hb_ot_fetch_number().
                 * @since 14.3.0
                 */
                ot_number_tag_t: ot_number_tag_tEnum
            }
            
            interface paint_composite_mode_tEnum {
                readonly $gtype: GObject.GType<paint_composite_mode_t>
                /**
                 * clear destination layer (bounded)
                 */
                readonly "CLEAR": 0
                /**
                 * replace destination layer (bounded)
                 */
                readonly "SRC": 1
                /**
                 * ignore the source
                 */
                readonly "DEST": 2
                /**
                 * draw source layer on top of destination layer
                 * (bounded)
                 */
                readonly "SRC_OVER": 3
                /**
                 * draw destination on top of source
                 */
                readonly "DEST_OVER": 4
                /**
                 * draw source where there was destination content
                 * (unbounded)
                 */
                readonly "SRC_IN": 5
                /**
                 * leave destination only where there was
                 * source content (unbounded)
                 */
                readonly "DEST_IN": 6
                /**
                 * draw source where there was no destination
                 * content (unbounded)
                 */
                readonly "SRC_OUT": 7
                /**
                 * leave destination only where there was no
                 * source content
                 */
                readonly "DEST_OUT": 8
                /**
                 * draw source on top of destination content and
                 * only there
                 */
                readonly "SRC_ATOP": 9
                /**
                 * leave destination on top of source content
                 * and only there (unbounded)
                 */
                readonly "DEST_ATOP": 10
                /**
                 * source and destination are shown where there is only
                 * one of them
                 */
                readonly "XOR": 11
                /**
                 * source and destination layers are accumulated
                 */
                readonly "PLUS": 12
                /**
                 * source and destination are complemented and
                 * multiplied. This causes the result to be at least as light as the lighter
                 * inputs.
                 */
                readonly "SCREEN": 13
                /**
                 * multiplies or screens, depending on the
                 * lightness of the destination color.
                 */
                readonly "OVERLAY": 14
                /**
                 * replaces the destination with the source if it
                 * is darker, otherwise keeps the source.
                 */
                readonly "DARKEN": 15
                /**
                 * replaces the destination with the source if it
                 * is lighter, otherwise keeps the source.
                 */
                readonly "LIGHTEN": 16
                /**
                 * brightens the destination color to reflect
                 * the source color.
                 */
                readonly "COLOR_DODGE": 17
                /**
                 * darkens the destination color to reflect
                 * the source color.
                 */
                readonly "COLOR_BURN": 18
                /**
                 * Multiplies or screens, dependent on source
                 * color.
                 */
                readonly "HARD_LIGHT": 19
                /**
                 * Darkens or lightens, dependent on source
                 * color.
                 */
                readonly "SOFT_LIGHT": 20
                /**
                 * Takes the difference of the source and
                 * destination color.
                 */
                readonly "DIFFERENCE": 21
                /**
                 * Produces an effect similar to difference, but
                 * with lower contrast.
                 */
                readonly "EXCLUSION": 22
                /**
                 * source and destination layers are multiplied.
                 * This causes the result to be at least as dark as the darker inputs.
                 */
                readonly "MULTIPLY": 23
                /**
                 * Creates a color with the hue of the source
                 * and the saturation and luminosity of the target.
                 */
                readonly "HSL_HUE": 24
                /**
                 * Creates a color with the saturation
                 * of the source and the hue and luminosity of the target. Painting with
                 * this mode onto a gray area produces no change.
                 */
                readonly "HSL_SATURATION": 25
                /**
                 * Creates a color with the hue and saturation
                 * of the source and the luminosity of the target. This preserves the gray
                 * levels of the target and is useful for coloring monochrome images or
                 * tinting color images.
                 */
                readonly "HSL_COLOR": 26
                /**
                 * Creates a color with the luminosity of
                 * the source and the hue and saturation of the target. This produces an
                 * inverse effect to `HB_PAINT_COMPOSITE_MODE_HSL_COLOR`.
                 */
                readonly "HSL_LUMINOSITY": 27
            }
            type paint_composite_mode_t = paint_composite_mode_tEnum[Exclude<keyof paint_composite_mode_tEnum, "$gtype">]
            interface $Exports {
                /**
                 * The values of this enumeration describe the compositing modes
                 * that can be used when combining temporary redirected drawing
                 * with the backdrop.
                 *
                 * See the OpenType spec [COLR](https://learn.microsoft.com/en-us/typography/opentype/spec/colr)
                 * section for details.
                 * @since 7.0.0
                 */
                paint_composite_mode_t: paint_composite_mode_tEnum
            }
            
            interface paint_extend_tEnum {
                readonly $gtype: GObject.GType<paint_extend_t>
                /**
                 * Outside the defined interval,
                 *   the color of the closest color stop is used.
                 */
                readonly "PAD": 0
                /**
                 * The color line is repeated over
                 *   repeated multiples of the defined interval
                 */
                readonly "REPEAT": 1
                /**
                 * The color line is repeated over
                 *      repeated intervals, as for the repeat mode.
                 *      However, in each repeated interval, the ordering of
                 *      color stops is the reverse of the adjacent interval.
                 */
                readonly "REFLECT": 2
            }
            type paint_extend_t = paint_extend_tEnum[Exclude<keyof paint_extend_tEnum, "$gtype">]
            interface $Exports {
                /**
                 * The values of this enumeration determine how color values
                 * outside the minimum and maximum defined offset on a #hb_color_line_t
                 * are determined.
                 *
                 * See the OpenType spec [COLR](https://learn.microsoft.com/en-us/typography/opentype/spec/colr)
                 * section for details.
                 * @since 7.0.0
                 */
                paint_extend_t: paint_extend_tEnum
            }
            
            interface script_tEnum {
                readonly $gtype: GObject.GType<script_t>
                /**
                 * `Zyyy`
                 */
                readonly "COMMON": 1517910393
                /**
                 * `Zinh`
                 */
                readonly "INHERITED": 1516858984
                /**
                 * `Zzzz`
                 */
                readonly "UNKNOWN": 1517976186
                /**
                 * `Arab`
                 */
                readonly "ARABIC": 1098015074
                /**
                 * `Armn`
                 */
                readonly "ARMENIAN": 1098018158
                /**
                 * `Beng`
                 */
                readonly "BENGALI": 1113943655
                /**
                 * `Cyrl`
                 */
                readonly "CYRILLIC": 1132032620
                /**
                 * `Deva`
                 */
                readonly "DEVANAGARI": 1147500129
                /**
                 * `Geor`
                 */
                readonly "GEORGIAN": 1197830002
                /**
                 * `Grek`
                 */
                readonly "GREEK": 1198679403
                /**
                 * `Gujr`
                 */
                readonly "GUJARATI": 1198877298
                /**
                 * `Guru`
                 */
                readonly "GURMUKHI": 1198879349
                /**
                 * `Hang`
                 */
                readonly "HANGUL": 1214344807
                /**
                 * `Hani`
                 */
                readonly "HAN": 1214344809
                /**
                 * `Hebr`
                 */
                readonly "HEBREW": 1214603890
                /**
                 * `Hira`
                 */
                readonly "HIRAGANA": 1214870113
                /**
                 * `Knda`
                 */
                readonly "KANNADA": 1265525857
                /**
                 * `Kana`
                 */
                readonly "KATAKANA": 1264676449
                /**
                 * `Laoo`
                 */
                readonly "LAO": 1281453935
                /**
                 * `Latn`
                 */
                readonly "LATIN": 1281455214
                /**
                 * `Mlym`
                 */
                readonly "MALAYALAM": 1298954605
                /**
                 * `Orya`
                 */
                readonly "ORIYA": 1332902241
                /**
                 * `Taml`
                 */
                readonly "TAMIL": 1415671148
                /**
                 * `Telu`
                 */
                readonly "TELUGU": 1415933045
                /**
                 * `Thai`
                 */
                readonly "THAI": 1416126825
                /**
                 * `Tibt`
                 */
                readonly "TIBETAN": 1416192628
                /**
                 * `Bopo`
                 */
                readonly "BOPOMOFO": 1114599535
                /**
                 * `Brai`
                 */
                readonly "BRAILLE": 1114792297
                /**
                 * `Cans`
                 */
                readonly "CANADIAN_SYLLABICS": 1130458739
                /**
                 * `Cher`
                 */
                readonly "CHEROKEE": 1130915186
                /**
                 * `Ethi`
                 */
                readonly "ETHIOPIC": 1165256809
                /**
                 * `Khmr`
                 */
                readonly "KHMER": 1265134962
                /**
                 * `Mong`
                 */
                readonly "MONGOLIAN": 1299148391
                /**
                 * `Mymr`
                 */
                readonly "MYANMAR": 1299803506
                /**
                 * `Ogam`
                 */
                readonly "OGHAM": 1332175213
                /**
                 * `Runr`
                 */
                readonly "RUNIC": 1383427698
                /**
                 * `Sinh`
                 */
                readonly "SINHALA": 1399418472
                /**
                 * `Syrc`
                 */
                readonly "SYRIAC": 1400468067
                /**
                 * `Thaa`
                 */
                readonly "THAANA": 1416126817
                /**
                 * `Yiii`
                 */
                readonly "YI": 1500080489
                /**
                 * `Dsrt`
                 */
                readonly "DESERET": 1148416628
                /**
                 * `Goth`
                 */
                readonly "GOTHIC": 1198486632
                /**
                 * `Ital`
                 */
                readonly "OLD_ITALIC": 1232363884
                /**
                 * `Buhd`
                 */
                readonly "BUHID": 1114990692
                /**
                 * `Hano`
                 */
                readonly "HANUNOO": 1214344815
                /**
                 * `Tglg`
                 */
                readonly "TAGALOG": 1416064103
                /**
                 * `Tagb`
                 */
                readonly "TAGBANWA": 1415669602
                /**
                 * `Cprt`
                 */
                readonly "CYPRIOT": 1131442804
                /**
                 * `Limb`
                 */
                readonly "LIMBU": 1281977698
                /**
                 * `Linb`
                 */
                readonly "LINEAR_B": 1281977954
                /**
                 * `Osma`
                 */
                readonly "OSMANYA": 1332964705
                /**
                 * `Shaw`
                 */
                readonly "SHAVIAN": 1399349623
                /**
                 * `Tale`
                 */
                readonly "TAI_LE": 1415670885
                /**
                 * `Ugar`
                 */
                readonly "UGARITIC": 1432838514
                /**
                 * `Bugi`
                 */
                readonly "BUGINESE": 1114990441
                /**
                 * `Copt`
                 */
                readonly "COPTIC": 1131376756
                /**
                 * `Glag`
                 */
                readonly "GLAGOLITIC": 1198285159
                /**
                 * `Khar`
                 */
                readonly "KHAROSHTHI": 1265131890
                /**
                 * `Talu`
                 */
                readonly "NEW_TAI_LUE": 1415670901
                /**
                 * `Xpeo`
                 */
                readonly "OLD_PERSIAN": 1483761007
                /**
                 * `Sylo`
                 */
                readonly "SYLOTI_NAGRI": 1400466543
                /**
                 * `Tfng`
                 */
                readonly "TIFINAGH": 1415999079
                /**
                 * `Bali`
                 */
                readonly "BALINESE": 1113681001
                /**
                 * `Xsux`
                 */
                readonly "CUNEIFORM": 1483961720
                /**
                 * `Nkoo`
                 */
                readonly "NKO": 1315663727
                /**
                 * `Phag`
                 */
                readonly "PHAGS_PA": 1349017959
                /**
                 * `Phnx`
                 */
                readonly "PHOENICIAN": 1349021304
                /**
                 * `Cari`
                 */
                readonly "CARIAN": 1130459753
                /**
                 * `Cham`
                 */
                readonly "CHAM": 1130914157
                /**
                 * `Kali`
                 */
                readonly "KAYAH_LI": 1264675945
                /**
                 * `Lepc`
                 */
                readonly "LEPCHA": 1281716323
                /**
                 * `Lyci`
                 */
                readonly "LYCIAN": 1283023721
                /**
                 * `Lydi`
                 */
                readonly "LYDIAN": 1283023977
                /**
                 * `Olck`
                 */
                readonly "OL_CHIKI": 1332503403
                /**
                 * `Rjng`
                 */
                readonly "REJANG": 1382706791
                /**
                 * `Saur`
                 */
                readonly "SAURASHTRA": 1398895986
                /**
                 * `Sund`
                 */
                readonly "SUNDANESE": 1400204900
                /**
                 * `Vaii`
                 */
                readonly "VAI": 1449224553
                /**
                 * `Avst`
                 */
                readonly "AVESTAN": 1098281844
                /**
                 * `Bamu`
                 */
                readonly "BAMUM": 1113681269
                /**
                 * `Egyp`
                 */
                readonly "EGYPTIAN_HIEROGLYPHS": 1164409200
                /**
                 * `Armi`
                 */
                readonly "IMPERIAL_ARAMAIC": 1098018153
                /**
                 * `Phli`
                 */
                readonly "INSCRIPTIONAL_PAHLAVI": 1349020777
                /**
                 * `Prti`
                 */
                readonly "INSCRIPTIONAL_PARTHIAN": 1349678185
                /**
                 * `Java`
                 */
                readonly "JAVANESE": 1247901281
                /**
                 * `Kthi`
                 */
                readonly "KAITHI": 1265920105
                /**
                 * `Lisu`
                 */
                readonly "LISU": 1281979253
                /**
                 * `Mtei`
                 */
                readonly "MEETEI_MAYEK": 1299473769
                /**
                 * `Sarb`
                 */
                readonly "OLD_SOUTH_ARABIAN": 1398895202
                /**
                 * `Orkh`
                 */
                readonly "OLD_TURKIC": 1332898664
                /**
                 * `Samr`
                 */
                readonly "SAMARITAN": 1398893938
                /**
                 * `Lana`
                 */
                readonly "TAI_THAM": 1281453665
                /**
                 * `Tavt`
                 */
                readonly "TAI_VIET": 1415673460
                /**
                 * `Batk`
                 */
                readonly "BATAK": 1113683051
                /**
                 * `Brah`
                 */
                readonly "BRAHMI": 1114792296
                /**
                 * `Mand`
                 */
                readonly "MANDAIC": 1298230884
                /**
                 * `Cakm`
                 */
                readonly "CHAKMA": 1130457965
                /**
                 * `Merc`
                 */
                readonly "MEROITIC_CURSIVE": 1298494051
                /**
                 * `Mero`
                 */
                readonly "MEROITIC_HIEROGLYPHS": 1298494063
                /**
                 * `Plrd`
                 */
                readonly "MIAO": 1349284452
                /**
                 * `Shrd`
                 */
                readonly "SHARADA": 1399353956
                /**
                 * `Sora`
                 */
                readonly "SORA_SOMPENG": 1399812705
                /**
                 * `Takr`
                 */
                readonly "TAKRI": 1415670642
                /**
                 * `Bass`, Since: 0.9.30
                 */
                readonly "BASSA_VAH": 1113682803
                /**
                 * `Aghb`, Since: 0.9.30
                 */
                readonly "CAUCASIAN_ALBANIAN": 1097295970
                /**
                 * `Dupl`, Since: 0.9.30
                 */
                readonly "DUPLOYAN": 1148547180
                /**
                 * `Elba`, Since: 0.9.30
                 */
                readonly "ELBASAN": 1164730977
                /**
                 * `Gran`, Since: 0.9.30
                 */
                readonly "GRANTHA": 1198678382
                /**
                 * `Khoj`, Since: 0.9.30
                 */
                readonly "KHOJKI": 1265135466
                /**
                 * `Sind`, Since: 0.9.30
                 */
                readonly "KHUDAWADI": 1399418468
                /**
                 * `Lina`, Since: 0.9.30
                 */
                readonly "LINEAR_A": 1281977953
                /**
                 * `Mahj`, Since: 0.9.30
                 */
                readonly "MAHAJANI": 1298229354
                /**
                 * `Mani`, Since: 0.9.30
                 */
                readonly "MANICHAEAN": 1298230889
                /**
                 * `Mend`, Since: 0.9.30
                 */
                readonly "MENDE_KIKAKUI": 1298493028
                /**
                 * `Modi`, Since: 0.9.30
                 */
                readonly "MODI": 1299145833
                /**
                 * `Mroo`, Since: 0.9.30
                 */
                readonly "MRO": 1299345263
                /**
                 * `Nbat`, Since: 0.9.30
                 */
                readonly "NABATAEAN": 1315070324
                /**
                 * `Narb`, Since: 0.9.30
                 */
                readonly "OLD_NORTH_ARABIAN": 1315009122
                /**
                 * `Perm`, Since: 0.9.30
                 */
                readonly "OLD_PERMIC": 1348825709
                /**
                 * `Hmng`, Since: 0.9.30
                 */
                readonly "PAHAWH_HMONG": 1215131239
                /**
                 * `Palm`, Since: 0.9.30
                 */
                readonly "PALMYRENE": 1348562029
                /**
                 * `Pauc`, Since: 0.9.30
                 */
                readonly "PAU_CIN_HAU": 1348564323
                /**
                 * `Phlp`, Since: 0.9.30
                 */
                readonly "PSALTER_PAHLAVI": 1349020784
                /**
                 * `Sidd`, Since: 0.9.30
                 */
                readonly "SIDDHAM": 1399415908
                /**
                 * `Tirh`, Since: 0.9.30
                 */
                readonly "TIRHUTA": 1416196712
                /**
                 * `Wara`, Since: 0.9.30
                 */
                readonly "WARANG_CITI": 1466004065
                /**
                 * `Ahom`, Since: 0.9.30
                 */
                readonly "AHOM": 1097363309
                /**
                 * `Hluw`, Since: 0.9.30
                 */
                readonly "ANATOLIAN_HIEROGLYPHS": 1215067511
                /**
                 * `Hatr`, Since: 0.9.30
                 */
                readonly "HATRAN": 1214346354
                /**
                 * `Mult`, Since: 0.9.30
                 */
                readonly "MULTANI": 1299541108
                /**
                 * `Hung`, Since: 0.9.30
                 */
                readonly "OLD_HUNGARIAN": 1215655527
                /**
                 * `Sgnw`, Since: 0.9.30
                 */
                readonly "SIGNWRITING": 1399287415
                /**
                 * `Adlm`, Since: 1.3.0
                 */
                readonly "ADLAM": 1097100397
                /**
                 * `Bhks`, Since: 1.3.0
                 */
                readonly "BHAIKSUKI": 1114139507
                /**
                 * `Marc`, Since: 1.3.0
                 */
                readonly "MARCHEN": 1298231907
                /**
                 * `Osge`, Since: 1.3.0
                 */
                readonly "OSAGE": 1332963173
                /**
                 * `Tang`, Since: 1.3.0
                 */
                readonly "TANGUT": 1415671399
                /**
                 * `Newa`, Since: 1.3.0
                 */
                readonly "NEWA": 1315272545
                /**
                 * `Gonm`, Since: 1.6.0
                 */
                readonly "MASARAM_GONDI": 1198485101
                /**
                 * `Nshu`, Since: 1.6.0
                 */
                readonly "NUSHU": 1316186229
                /**
                 * `Soyo`, Since: 1.6.0
                 */
                readonly "SOYOMBO": 1399814511
                /**
                 * `Zanb`, Since: 1.6.0
                 */
                readonly "ZANABAZAR_SQUARE": 1516334690
                /**
                 * `Dogr`, Since: 1.8.0
                 */
                readonly "DOGRA": 1148151666
                /**
                 * `Gong`, Since: 1.8.0
                 */
                readonly "GUNJALA_GONDI": 1198485095
                /**
                 * `Rohg`, Since: 1.8.0
                 */
                readonly "HANIFI_ROHINGYA": 1383032935
                /**
                 * `Maka`, Since: 1.8.0
                 */
                readonly "MAKASAR": 1298230113
                /**
                 * `Medf`, Since: 1.8.0
                 */
                readonly "MEDEFAIDRIN": 1298490470
                /**
                 * `Sogo`, Since: 1.8.0
                 */
                readonly "OLD_SOGDIAN": 1399809903
                /**
                 * `Sogd`, Since: 1.8.0
                 */
                readonly "SOGDIAN": 1399809892
                /**
                 * `Elym`, Since: 2.4.0
                 */
                readonly "ELYMAIC": 1164736877
                /**
                 * `Nand`, Since: 2.4.0
                 */
                readonly "NANDINAGARI": 1315008100
                /**
                 * `Hmnp`, Since: 2.4.0
                 */
                readonly "NYIAKENG_PUACHUE_HMONG": 1215131248
                /**
                 * `Wcho`, Since: 2.4.0
                 */
                readonly "WANCHO": 1466132591
                /**
                 * `Chrs`, Since: 2.6.7
                 */
                readonly "CHORASMIAN": 1130918515
                /**
                 * `Diak`, Since: 2.6.7
                 */
                readonly "DIVES_AKURU": 1147756907
                /**
                 * `Kits`, Since: 2.6.7
                 */
                readonly "KHITAN_SMALL_SCRIPT": 1265202291
                /**
                 * `Yezi`, Since: 2.6.7
                 */
                readonly "YEZIDI": 1499822697
                /**
                 * `Cpmn`, Since: 3.0.0
                 */
                readonly "CYPRO_MINOAN": 1131441518
                /**
                 * `Ougr`, Since: 3.0.0
                 */
                readonly "OLD_UYGHUR": 1333094258
                /**
                 * `Tnsa`, Since: 3.0.0
                 */
                readonly "TANGSA": 1416524641
                /**
                 * `Toto`, Since: 3.0.0
                 */
                readonly "TOTO": 1416590447
                /**
                 * `Vith`, Since: 3.0.0
                 */
                readonly "VITHKUQI": 1449751656
                /**
                 * `Zmth`, Since: 3.4.0
                 */
                readonly "MATH": 1517122664
                /**
                 * `Kawi`, Since: 5.2.0
                 */
                readonly "KAWI": 1264678761
                /**
                 * `Nagm`, Since: 5.2.0
                 */
                readonly "NAG_MUNDARI": 1315006317
                /**
                 * `Gara`, Since: 10.0.0
                 */
                readonly "GARAY": 1197568609
                /**
                 * `Gukh`, Since: 10.0.0
                 */
                readonly "GURUNG_KHEMA": 1198877544
                /**
                 * `Krai`, Since: 10.0.0
                 */
                readonly "KIRAT_RAI": 1265787241
                /**
                 * `Onao`, Since: 10.0.0
                 */
                readonly "OL_ONAL": 1332633967
                /**
                 * `Sunu`, Since: 10.0.0
                 */
                readonly "SUNUWAR": 1400204917
                /**
                 * `Todr`, Since: 10.0.0
                 */
                readonly "TODHRI": 1416586354
                /**
                 * `Tutg`, Since: 10.0.0
                 */
                readonly "TULU_TIGALARI": 1416983655
                /**
                 * `Berf`, Since: 11.5.0
                 */
                readonly "BERIA_ERFE": 1113944678
                /**
                 * `Sidt`, Since: 11.5.0
                 */
                readonly "SIDETIC": 1399415924
                /**
                 * `Tayo`, Since: 11.5.0
                 */
                readonly "TAI_YO": 1415674223
                /**
                 * `Tols`, Since: 11.5.0
                 */
                readonly "TOLONG_SIKI": 1416588403
                /**
                 * No script set
                 */
                readonly "INVALID": 0
            }
            type script_t = script_tEnum[Exclude<keyof script_tEnum, "$gtype">]
            interface $Exports {
                /**
                 * Data type for scripts. Each #hb_script_t's value is an #hb_tag_t corresponding
                 * to the four-letter values defined by [ISO 15924](https://unicode.org/iso15924/).
                 *
                 * See also the Script (sc) property of the Unicode Character Database.
                 */
                script_t: script_tEnum
            }
            
            interface style_tag_tEnum {
                readonly $gtype: GObject.GType<style_tag_t>
                /**
                 * Used to vary between non-italic and italic.
                 * A value of 0 can be interpreted as "Roman" (non-italic); a value of 1 can
                 * be interpreted as (fully) italic.
                 */
                readonly "ITALIC": 1769234796
                /**
                 * Used to vary design to suit different text sizes.
                 * Non-zero. Values can be interpreted as text size, in points.
                 */
                readonly "OPTICAL_SIZE": 1869640570
                /**
                 * Used to vary between upright and slanted text. Values
                 * must be greater than -90 and less than +90. Values can be interpreted as
                 * the angle, in counter-clockwise degrees, of oblique slant from whatever the
                 * designer considers to be upright for that font design. Typical right-leaning
                 * Italic fonts have a negative slant angle (typically around -12)
                 */
                readonly "SLANT_ANGLE": 1936486004
                /**
                 * same as `HB_STYLE_TAG_SLANT_ANGLE` expression as ratio.
                 * Typical right-leaning Italic fonts have a positive slant ratio (typically around 0.2)
                 */
                readonly "SLANT_RATIO": 1399615092
                /**
                 * Used to vary width of text from narrower to wider.
                 * Non-zero. Values can be interpreted as a percentage of whatever the font
                 * designer considers “normal width” for that font design.
                 */
                readonly "WIDTH": 2003072104
                /**
                 * Used to vary stroke thicknesses or other design details
                 * to give variation from lighter to blacker. Values can be interpreted in direct
                 * comparison to values for usWeightClass in the OS/2 table,
                 * or the CSS font-weight property.
                 */
                readonly "WEIGHT": 2003265652
            }
            type style_tag_t = style_tag_tEnum[Exclude<keyof style_tag_tEnum, "$gtype">]
            interface $Exports {
                /**
                 * Defined by [OpenType Design-Variation Axis Tag Registry](https://docs.microsoft.com/en-us/typography/opentype/spec/dvaraxisreg).
                 * @since 3.0.0
                 */
                style_tag_t: style_tag_tEnum
            }
            
            interface unicode_combining_class_tEnum {
                readonly $gtype: GObject.GType<unicode_combining_class_t>
                /**
                 * Spacing and enclosing marks; also many vowel and consonant signs, even if nonspacing
                 */
                readonly "NOT_REORDERED": 0
                /**
                 * Marks which overlay a base letter or symbol
                 */
                readonly "OVERLAY": 1
                /**
                 * Diacritic nukta marks in Brahmi-derived scripts
                 */
                readonly "NUKTA": 7
                /**
                 * Hiragana/Katakana voicing marks
                 */
                readonly "KANA_VOICING": 8
                /**
                 * Viramas
                 */
                readonly "VIRAMA": 9
                /**
                 * [Hebrew]
                 */
                readonly "CCC10": 10
                /**
                 * [Hebrew]
                 */
                readonly "CCC11": 11
                /**
                 * [Hebrew]
                 */
                readonly "CCC12": 12
                /**
                 * [Hebrew]
                 */
                readonly "CCC13": 13
                /**
                 * [Hebrew]
                 */
                readonly "CCC14": 14
                /**
                 * [Hebrew]
                 */
                readonly "CCC15": 15
                /**
                 * [Hebrew]
                 */
                readonly "CCC16": 16
                /**
                 * [Hebrew]
                 */
                readonly "CCC17": 17
                /**
                 * [Hebrew]
                 */
                readonly "CCC18": 18
                /**
                 * [Hebrew]
                 */
                readonly "CCC19": 19
                /**
                 * [Hebrew]
                 */
                readonly "CCC20": 20
                /**
                 * [Hebrew]
                 */
                readonly "CCC21": 21
                /**
                 * [Hebrew]
                 */
                readonly "CCC22": 22
                /**
                 * [Hebrew]
                 */
                readonly "CCC23": 23
                /**
                 * [Hebrew]
                 */
                readonly "CCC24": 24
                /**
                 * [Hebrew]
                 */
                readonly "CCC25": 25
                /**
                 * [Hebrew]
                 */
                readonly "CCC26": 26
                /**
                 * [Arabic]
                 */
                readonly "CCC27": 27
                /**
                 * [Arabic]
                 */
                readonly "CCC28": 28
                /**
                 * [Arabic]
                 */
                readonly "CCC29": 29
                /**
                 * [Arabic]
                 */
                readonly "CCC30": 30
                /**
                 * [Arabic]
                 */
                readonly "CCC31": 31
                /**
                 * [Arabic]
                 */
                readonly "CCC32": 32
                /**
                 * [Arabic]
                 */
                readonly "CCC33": 33
                /**
                 * [Arabic]
                 */
                readonly "CCC34": 34
                /**
                 * [Arabic]
                 */
                readonly "CCC35": 35
                /**
                 * [Syriac]
                 */
                readonly "CCC36": 36
                /**
                 * [Telugu]
                 */
                readonly "CCC84": 84
                /**
                 * [Telugu]
                 */
                readonly "CCC91": 91
                /**
                 * [Thai]
                 */
                readonly "CCC103": 103
                /**
                 * [Thai]
                 */
                readonly "CCC107": 107
                /**
                 * [Lao]
                 */
                readonly "CCC118": 118
                /**
                 * [Lao]
                 */
                readonly "CCC122": 122
                /**
                 * [Tibetan]
                 */
                readonly "CCC129": 129
                /**
                 * [Tibetan]
                 */
                readonly "CCC130": 130
                /**
                 * [Tibetan] Since: 7.2.0
                 */
                readonly "CCC132": 132
                /**
                 * Marks attached at the bottom left
                 */
                readonly "ATTACHED_BELOW_LEFT": 200
                /**
                 * Marks attached directly below
                 */
                readonly "ATTACHED_BELOW": 202
                /**
                 * Marks attached directly above
                 */
                readonly "ATTACHED_ABOVE": 214
                /**
                 * Marks attached at the top right
                 */
                readonly "ATTACHED_ABOVE_RIGHT": 216
                /**
                 * Distinct marks at the bottom left
                 */
                readonly "BELOW_LEFT": 218
                /**
                 * Distinct marks directly below
                 */
                readonly "BELOW": 220
                /**
                 * Distinct marks at the bottom right
                 */
                readonly "BELOW_RIGHT": 222
                /**
                 * Distinct marks to the left
                 */
                readonly "LEFT": 224
                /**
                 * Distinct marks to the right
                 */
                readonly "RIGHT": 226
                /**
                 * Distinct marks at the top left
                 */
                readonly "ABOVE_LEFT": 228
                /**
                 * Distinct marks directly above
                 */
                readonly "ABOVE": 230
                /**
                 * Distinct marks at the top right
                 */
                readonly "ABOVE_RIGHT": 232
                /**
                 * Distinct marks subtending two bases
                 */
                readonly "DOUBLE_BELOW": 233
                /**
                 * Distinct marks extending above two bases
                 */
                readonly "DOUBLE_ABOVE": 234
                /**
                 * Greek iota subscript only
                 */
                readonly "IOTA_SUBSCRIPT": 240
                /**
                 * Invalid combining class
                 */
                readonly "INVALID": 255
            }
            type unicode_combining_class_t = unicode_combining_class_tEnum[Exclude<keyof unicode_combining_class_tEnum, "$gtype">]
            interface $Exports {
                /**
                 * Data type for the Canonical_Combining_Class (ccc) property
                 * from the Unicode Character Database.
                 *
                 * <note>Note: newer versions of Unicode may add new values.
                 * Client programs should be ready to handle any value in the 0..254 range
                 * being returned from hb_unicode_combining_class().</note>
                 */
                unicode_combining_class_t: unicode_combining_class_tEnum
            }
            
            interface unicode_general_category_tEnum {
                readonly $gtype: GObject.GType<unicode_general_category_t>
                /**
                 * [Cc]
                 */
                readonly "CONTROL": 0
                /**
                 * [Cf]
                 */
                readonly "FORMAT": 1
                /**
                 * [Cn]
                 */
                readonly "UNASSIGNED": 2
                /**
                 * [Co]
                 */
                readonly "PRIVATE_USE": 3
                /**
                 * [Cs]
                 */
                readonly "SURROGATE": 4
                /**
                 * [Ll]
                 */
                readonly "LOWERCASE_LETTER": 5
                /**
                 * [Lm]
                 */
                readonly "MODIFIER_LETTER": 6
                /**
                 * [Lo]
                 */
                readonly "OTHER_LETTER": 7
                /**
                 * [Lt]
                 */
                readonly "TITLECASE_LETTER": 8
                /**
                 * [Lu]
                 */
                readonly "UPPERCASE_LETTER": 9
                /**
                 * [Mc]
                 */
                readonly "SPACING_MARK": 10
                /**
                 * [Me]
                 */
                readonly "ENCLOSING_MARK": 11
                /**
                 * [Mn]
                 */
                readonly "NON_SPACING_MARK": 12
                /**
                 * [Nd]
                 */
                readonly "DECIMAL_NUMBER": 13
                /**
                 * [Nl]
                 */
                readonly "LETTER_NUMBER": 14
                /**
                 * [No]
                 */
                readonly "OTHER_NUMBER": 15
                /**
                 * [Pc]
                 */
                readonly "CONNECT_PUNCTUATION": 16
                /**
                 * [Pd]
                 */
                readonly "DASH_PUNCTUATION": 17
                /**
                 * [Pe]
                 */
                readonly "CLOSE_PUNCTUATION": 18
                /**
                 * [Pf]
                 */
                readonly "FINAL_PUNCTUATION": 19
                /**
                 * [Pi]
                 */
                readonly "INITIAL_PUNCTUATION": 20
                /**
                 * [Po]
                 */
                readonly "OTHER_PUNCTUATION": 21
                /**
                 * [Ps]
                 */
                readonly "OPEN_PUNCTUATION": 22
                /**
                 * [Sc]
                 */
                readonly "CURRENCY_SYMBOL": 23
                /**
                 * [Sk]
                 */
                readonly "MODIFIER_SYMBOL": 24
                /**
                 * [Sm]
                 */
                readonly "MATH_SYMBOL": 25
                /**
                 * [So]
                 */
                readonly "OTHER_SYMBOL": 26
                /**
                 * [Zl]
                 */
                readonly "LINE_SEPARATOR": 27
                /**
                 * [Zp]
                 */
                readonly "PARAGRAPH_SEPARATOR": 28
                /**
                 * [Zs]
                 */
                readonly "SPACE_SEPARATOR": 29
            }
            type unicode_general_category_t = unicode_general_category_tEnum[Exclude<keyof unicode_general_category_tEnum, "$gtype">]
            interface $Exports {
                /**
                 * Data type for the "General_Category" (gc) property from
                 * the Unicode Character Database.
                 */
                unicode_general_category_t: unicode_general_category_tEnum
            }
            
            interface buffer_diff_flags_tBitfield {
                readonly $gtype: GObject.GType<buffer_diff_flags_t>
                /**
                 * equal buffers.
                 */
                readonly "EQUAL": 0
                /**
                 * buffers with different
                 *     #hb_buffer_content_type_t.
                 */
                readonly "CONTENT_TYPE_MISMATCH": 1
                /**
                 * buffers with differing length.
                 */
                readonly "LENGTH_MISMATCH": 2
                /**
                 * `.notdef` glyph is present in the
                 *     reference buffer.
                 */
                readonly "NOTDEF_PRESENT": 4
                /**
                 * dotted circle glyph is present
                 *     in the reference buffer.
                 */
                readonly "DOTTED_CIRCLE_PRESENT": 8
                /**
                 * difference in #hb_glyph_info_t.codepoint
                 */
                readonly "CODEPOINT_MISMATCH": 16
                /**
                 * difference in #hb_glyph_info_t.cluster
                 */
                readonly "CLUSTER_MISMATCH": 32
                /**
                 * difference in #hb_glyph_flags_t.
                 */
                readonly "GLYPH_FLAGS_MISMATCH": 64
                /**
                 * difference in #hb_glyph_position_t.
                 */
                readonly "POSITION_MISMATCH": 128
            }
            type buffer_diff_flags_t = number
            interface $Exports {
                /**
                 * Flags from comparing two #hb_buffer_t's.
                 *
                 * Buffer with different #hb_buffer_content_type_t cannot be meaningfully
                 * compared in any further detail.
                 *
                 * For buffers with differing length, the per-glyph comparison is not
                 * attempted, though we do still scan reference buffer for dotted circle and
                 * `.notdef` glyphs.
                 *
                 * If the buffers have the same length, we compare them glyph-by-glyph and
                 * report which aspect(s) of the glyph info/position are different.
                 * @since 1.5.0
                 */
                buffer_diff_flags_t: buffer_diff_flags_tBitfield
            }
            
            interface buffer_flags_tBitfield {
                readonly $gtype: GObject.GType<buffer_flags_t>
                /**
                 * the default buffer flag.
                 */
                readonly "DEFAULT": 0
                /**
                 * flag indicating that special handling of the beginning
                 *                      of text paragraph can be applied to this buffer. Should usually
                 *                      be set, unless you are passing to the buffer only part
                 *                      of the text without the full context.
                 */
                readonly "BOT": 1
                /**
                 * flag indicating that special handling of the end of text
                 *                      paragraph can be applied to this buffer, similar to
                 *                      `HB_BUFFER_FLAG_BOT`.
                 */
                readonly "EOT": 2
                /**
                 * flag indication that character with Default_Ignorable
                 *                      Unicode property should use the corresponding glyph
                 *                      from the font, instead of hiding them (done by
                 *                      replacing them with the space glyph and zeroing the
                 *                      advance width.)  This flag takes precedence over
                 *                      `HB_BUFFER_FLAG_REMOVE_DEFAULT_IGNORABLES`.
                 */
                readonly "PRESERVE_DEFAULT_IGNORABLES": 4
                /**
                 * flag indication that character with Default_Ignorable
                 *                      Unicode property should be removed from glyph string
                 *                      instead of hiding them (done by replacing them with the
                 *                      space glyph and zeroing the advance width.)
                 *                      `HB_BUFFER_FLAG_PRESERVE_DEFAULT_IGNORABLES` takes
                 *                      precedence over this flag. Since: 1.8.0
                 */
                readonly "REMOVE_DEFAULT_IGNORABLES": 8
                /**
                 * flag indicating that a dotted circle should
                 *                      not be inserted in the rendering of incorrect
                 *                      character sequences (such at <0905 093E>). Since: 2.4.0
                 */
                readonly "DO_NOT_INSERT_DOTTED_CIRCLE": 16
                /**
                 * flag indicating that the hb_shape() call and its variants
                 *                      should perform various verification processes on the results
                 *                      of the shaping operation on the buffer.  If the verification
                 *                      fails, then either a buffer message is sent, if a message
                 *                      handler is installed on the buffer, or a message is written
                 *                      to standard error.  In either case, the shaping result might
                 *                      be modified to show the failed output. Since: 3.4.0
                 */
                readonly "VERIFY": 32
                /**
                 * flag indicating that the `HB_GLYPH_FLAG_UNSAFE_TO_CONCAT`
                 *                      glyph-flag should be produced by the shaper. By default
                 *                      it will not be produced since it incurs a cost. Since: 4.0.0
                 */
                readonly "PRODUCE_UNSAFE_TO_CONCAT": 64
                /**
                 * flag indicating that the `HB_GLYPH_FLAG_SAFE_TO_INSERT_TATWEEL`
                 *                      glyph-flag should be produced by the shaper. By default
                 *                      it will not be produced. Since: 5.1.0
                 */
                readonly "PRODUCE_SAFE_TO_INSERT_TATWEEL": 128
                /**
                 * All currently defined flags: Since: 4.4.0
                 */
                readonly "DEFINED": 255
            }
            type buffer_flags_t = number
            interface $Exports {
                /**
                 * Flags for #hb_buffer_t.
                 * @since 0.9.20
                 */
                buffer_flags_t: buffer_flags_tBitfield
            }
            
            interface buffer_serialize_flags_tBitfield {
                readonly $gtype: GObject.GType<buffer_serialize_flags_t>
                /**
                 * serialize glyph names, clusters and positions.
                 */
                readonly "DEFAULT": 0
                /**
                 * do not serialize glyph cluster.
                 */
                readonly "NO_CLUSTERS": 1
                /**
                 * do not serialize glyph position information.
                 */
                readonly "NO_POSITIONS": 2
                /**
                 * do no serialize glyph name.
                 */
                readonly "NO_GLYPH_NAMES": 4
                /**
                 * serialize glyph extents.
                 */
                readonly "GLYPH_EXTENTS": 8
                /**
                 * serialize glyph flags. Since: 1.5.0
                 */
                readonly "GLYPH_FLAGS": 16
                /**
                 * do not serialize glyph advances,
                 *  glyph offsets will reflect absolute glyph positions. Since: 1.8.0.
                 *  Note: when this flag is used with a partial range of the buffer (i.e.
                 *  `start` is not 0), calculating the absolute positions has a cost
                 *  proportional to `start`. If the buffer is serialized in many small
                 *  chunks, this can lead to quadratic behavior. It is recommended to
                 *  use a larger `buf_size` to minimize this cost.
                 */
                readonly "NO_ADVANCES": 32
                /**
                 * All currently defined flags. Since: 4.4.0
                 */
                readonly "DEFINED": 63
            }
            type buffer_serialize_flags_t = number
            interface $Exports {
                /**
                 * Flags that control what glyph information are serialized in hb_buffer_serialize_glyphs().
                 * @since 0.9.20
                 */
                buffer_serialize_flags_t: buffer_serialize_flags_tBitfield
            }
            
            interface glyph_flags_tBitfield {
                readonly $gtype: GObject.GType<glyph_flags_t>
                /**
                 * Indicates that if input text is broken at the
                 * 				   beginning of the cluster this glyph is part of,
                 * 				   then both sides need to be re-shaped, as the
                 * 				   result might be different.
                 * 				   On the flip side, it means that when this
                 * 				   flag is not present, then it is safe to break
                 * 				   the glyph-run at the beginning of this
                 * 				   cluster, and the two sides will represent the
                 * 				   exact same result one would get if breaking
                 * 				   input text at the beginning of this cluster
                 * 				   and shaping the two sides separately.
                 * 				   This can be used to optimize paragraph
                 * 				   layout, by avoiding re-shaping of each line
                 * 				   after line-breaking.
                 */
                readonly "UNSAFE_TO_BREAK": 1
                /**
                 * Indicates that if input text is changed on one
                 * 				   side of the beginning of the cluster this glyph
                 * 				   is part of, then the shaping results for the
                 * 				   other side might change.
                 * 				   Note that the absence of this flag will NOT by
                 * 				   itself mean that it IS safe to concat text.
                 * 				   Only two pieces of text both of which clear of
                 * 				   this flag can be concatenated safely.
                 * 				   This can be used to optimize paragraph
                 * 				   layout, by avoiding re-shaping of each line
                 * 				   after line-breaking, by limiting the
                 * 				   reshaping to a small piece around the
                 * 				   breaking position only, even if the breaking
                 * 				   position carries the
                 * 				   #HB_GLYPH_FLAG_UNSAFE_TO_BREAK or when
                 * 				   hyphenation or other text transformation
                 * 				   happens at line-break position, in the following
                 * 				   way:
                 * 				   1. Iterate back from the line-break position
                 * 				   until the first cluster start position that is
                 * 				   NOT unsafe-to-concat, 2. shape the segment from
                 * 				   there till the end of line, 3. check whether the
                 * 				   resulting glyph-run also is clear of the
                 * 				   unsafe-to-concat at its start-of-text position;
                 * 				   if it is, just splice it into place and the line
                 * 				   is shaped; If not, move on to a position further
                 * 				   back that is clear of unsafe-to-concat and retry
                 * 				   from there, and repeat.
                 * 				   At the start of next line a similar algorithm can
                 * 				   be implemented. That is: 1. Iterate forward from
                 * 				   the line-break position until the first cluster
                 * 				   start position that is NOT unsafe-to-concat, 2.
                 * 				   shape the segment from beginning of the line to
                 * 				   that position, 3. check whether the resulting
                 * 				   glyph-run also is clear of the unsafe-to-concat
                 * 				   at its end-of-text position; if it is, just splice
                 * 				   it into place and the beginning is shaped; If not,
                 * 				   move on to a position further forward that is clear
                 * 				   of unsafe-to-concat and retry up to there, and repeat.
                 * 				   A slight complication will arise in the
                 * 				   implementation of the algorithm above,
                 * 				   because while our buffer API has a way to
                 * 				   return flags for position corresponding to
                 * 				   start-of-text, there is currently no position
                 * 				   corresponding to end-of-text.  This limitation
                 * 				   can be alleviated by shaping more text than needed
                 * 				   and looking for unsafe-to-concat flag within text
                 * 				   clusters.
                 * 				   The #HB_GLYPH_FLAG_UNSAFE_TO_BREAK flag will
                 * 				   always imply this flag.
                 * 			   To use this flag, you must enable the buffer flag
                 * 			   `HB_BUFFER_FLAG_PRODUCE_UNSAFE_TO_CONCAT` during
                 * 			   shaping, otherwise the buffer flag will not be
                 * 			   reliably produced.
                 * 				   Since: 4.0.0
                 */
                readonly "UNSAFE_TO_CONCAT": 2
                /**
                 * In scripts that use elongation (Arabic,
                 * 				   Mongolian, Syriac, etc.), this flag signifies
                 * 				   that it is safe to insert a U+0640 TATWEEL
                 * 				   character before this cluster for elongation.
                 * 				   This flag does not determine the
                 * 				   script-specific elongation places, but only
                 * 				   when it is safe to do the elongation without
                 * 				   interrupting text shaping.
                 * 				   Since: 5.1.0
                 */
                readonly "SAFE_TO_INSERT_TATWEEL": 4
                /**
                 * All the currently defined flags.
                 */
                readonly "DEFINED": 7
            }
            type glyph_flags_t = number
            interface $Exports {
                /**
                 * Flags for #hb_glyph_info_t.
                 * @since 1.5.0
                 */
                glyph_flags_t: glyph_flags_tBitfield
            }
            
            interface ot_color_palette_flags_tBitfield {
                readonly $gtype: GObject.GType<ot_color_palette_flags_t>
                /**
                 * Default indicating that there is nothing special
                 *   to note about a color palette.
                 */
                readonly "DEFAULT": 0
                /**
                 * Flag indicating that the color
                 *   palette is appropriate to use when displaying the font on a light background such as white.
                 */
                readonly "USABLE_WITH_LIGHT_BACKGROUND": 1
                /**
                 * Flag indicating that the color
                 *   palette is appropriate to use when displaying the font on a dark background such as black.
                 */
                readonly "USABLE_WITH_DARK_BACKGROUND": 2
            }
            type ot_color_palette_flags_t = number
            interface $Exports {
                /**
                 * Flags that describe the properties of color palette.
                 * @since 2.1.0
                 */
                ot_color_palette_flags_t: ot_color_palette_flags_tBitfield
            }
            
            interface ot_math_glyph_part_flags_tBitfield {
                readonly $gtype: GObject.GType<ot_math_glyph_part_flags_t>
                /**
                 * This is an extender glyph part that
                 * can be repeated to reach the desired length.
                 */
                readonly "EXTENDER": 1
            }
            type ot_math_glyph_part_flags_t = number
            interface $Exports {
                /**
                 * Flags for math glyph parts.
                 * @since 1.3.3
                 */
                ot_math_glyph_part_flags_t: ot_math_glyph_part_flags_tBitfield
            }
            
            interface ot_var_axis_flags_tBitfield {
                readonly $gtype: GObject.GType<ot_var_axis_flags_t>
                /**
                 * The axis should not be exposed directly in user interfaces.
                 */
                readonly "HIDDEN": 1
            }
            type ot_var_axis_flags_t = number
            interface $Exports {
                /**
                 * Flags for #hb_ot_var_axis_info_t.
                 * @since 2.2.0
                 */
                ot_var_axis_flags_t: ot_var_axis_flags_tBitfield
            }
            /**
             * A callback method for #hb_buffer_t. The method gets called with the
             * #hb_buffer_t it was set on, the #hb_font_t the buffer is shaped with and a
             * message describing what step of the shaping process will be performed.
             * Returning `false` from this method will skip this shaping step and move to
             * the next one.
             * @since 1.1.3
             * @param buffer An #hb_buffer_t to work upon
             * @param font The #hb_font_t the `buffer` is shaped with
             * @param message `NULL`-terminated message passed to the function
             * @returns `true` to perform the shaping step, `false` to skip it.
             */
            type buffer_message_func_t = (buffer: never, font: never, message: string) => never
            /**
             * A virtual method for the #hb_color_line_t to fetch color stops.
             * @since 7.0.0
             * @param color_line a #hb_color_line_t object
             * @param color_line_data the data accompanying `color_line`
             * @param start the index of the first color stop to return
             * @returns the total number of color stops in `color_line`, Array of #hb_color_stop_t to populate
             */
            type color_line_get_color_stops_func_t = (color_line: never, color_line_data: never | null, start: number) => [number, never[]]
            /**
             * A virtual method for the `hb_color_line_t` to fetches the extend mode.
             * @since 7.0.0
             * @param color_line a #hb_color_line_t object
             * @param color_line_data the data accompanying `color_line`
             * @returns the extend mode of `color_line`
             */
            type color_line_get_extend_func_t = (color_line: never, color_line_data: never | null) => never
            /**
             * A virtual method for destroy user-data callbacks.
             */
            type destroy_func_t = () => void
            /**
             * A virtual method for the #hb_draw_funcs_t to perform a "close-path" draw
             * operation.
             * @since 4.0.0
             * @param dfuncs draw functions object
             * @param draw_data The data accompanying the draw functions in hb_font_draw_glyph()
             * @param st current draw state
             */
            type draw_close_path_func_t = (dfuncs: never, draw_data: never | null, st: never) => void
            /**
             * A virtual method for the #hb_draw_funcs_t to perform a "cubic-to" draw
             * operation.
             * @since 4.0.0
             * @param dfuncs draw functions object
             * @param draw_data The data accompanying the draw functions in hb_font_draw_glyph()
             * @param st current draw state
             * @param control1_x X component of first control point
             * @param control1_y Y component of first control point
             * @param control2_x X component of second control point
             * @param control2_y Y component of second control point
             * @param to_x X component of target point
             * @param to_y Y component of target point
             */
            type draw_cubic_to_func_t = (dfuncs: never, draw_data: never | null, st: never, control1_x: number, control1_y: number, control2_x: number, control2_y: number, to_x: number, to_y: number) => void
            /**
             * A virtual method for the #hb_draw_funcs_t to perform a "line-to" draw
             * operation.
             * @since 4.0.0
             * @param dfuncs draw functions object
             * @param draw_data The data accompanying the draw functions in hb_font_draw_glyph()
             * @param st current draw state
             * @param to_x X component of target point
             * @param to_y Y component of target point
             */
            type draw_line_to_func_t = (dfuncs: never, draw_data: never | null, st: never, to_x: number, to_y: number) => void
            /**
             * A virtual method for the #hb_draw_funcs_t to perform a "move-to" draw
             * operation.
             * @since 4.0.0
             * @param dfuncs draw functions object
             * @param draw_data The data accompanying the draw functions in hb_font_draw_glyph()
             * @param st current draw state
             * @param to_x X component of target point
             * @param to_y Y component of target point
             */
            type draw_move_to_func_t = (dfuncs: never, draw_data: never | null, st: never, to_x: number, to_y: number) => void
            /**
             * A virtual method for the #hb_draw_funcs_t to perform a "quadratic-to" draw
             * operation.
             * @since 4.0.0
             * @param dfuncs draw functions object
             * @param draw_data The data accompanying the draw functions in hb_font_draw_glyph()
             * @param st current draw state
             * @param control_x X component of control point
             * @param control_y Y component of control point
             * @param to_x X component of target point
             * @param to_y Y component of target point
             */
            type draw_quadratic_to_func_t = (dfuncs: never, draw_data: never | null, st: never, control_x: number, control_y: number, to_x: number, to_y: number) => void
            /**
             * A virtual method for the #hb_font_funcs_t of an #hb_font_t object.
             * @since 7.0.0
             * @deprecated since 11.2.0 Use hb_font_draw_glyph_func_or_fail_t instead.
             * @param font #hb_font_t to work upon
             * @param font_data  `font` user data pointer
             * @param glyph The glyph ID to query
             * @param draw_funcs The draw functions to send the shape data to
             * @param draw_data The data accompanying the draw functions
             */
            type font_draw_glyph_func_t = (font: never, font_data: never | null, glyph: never, draw_funcs: never, draw_data: never | null) => void
            /**
             * A virtual method for the #hb_font_funcs_t of an #hb_font_t object.
             * @since 11.2.0
             * @param font #hb_font_t to work upon
             * @param font_data  `font` user data pointer
             * @param glyph The glyph ID to query
             * @param draw_funcs The draw functions to send the shape data to
             * @param draw_data The data accompanying the draw functions
             * @returns `true` if glyph was drawn, `false` otherwise
             */
            type font_draw_glyph_or_fail_func_t = (font: never, font_data: never | null, glyph: never, draw_funcs: never, draw_data: never | null) => never
            /**
             * This method should retrieve the extents for a font.
             * @param font #hb_font_t to work upon
             * @param font_data  `font` user data pointer
             * @returns , The font extents retrieved
             */
            type font_get_font_extents_func_t = (font: never, font_data: never | null) => [never, never]
            /**
             * A virtual method for the #hb_font_funcs_t of an #hb_font_t object.
             *
             * This method should retrieve the advance for a specified glyph. The
             * method must return an #hb_position_t.
             * @param font #hb_font_t to work upon
             * @param font_data  `font` user data pointer
             * @param glyph The glyph ID to query
             * @returns The advance of `glyph` within `font`
             */
            type font_get_glyph_advance_func_t = (font: never, font_data: never | null, glyph: never) => never
            /**
             * A virtual method for the #hb_font_funcs_t of an #hb_font_t object.
             *
             * This method should retrieve the advances for a sequence of glyphs.
             * @param font #hb_font_t to work upon
             * @param font_data  `font` user data pointer
             * @param count The number of glyph IDs in the sequence queried
             * @param first_glyph The first glyph ID to query
             * @param glyph_stride The stride between successive glyph IDs
             * @param advance_stride The stride between successive advances
             * @returns , The first advance retrieved
             */
            type font_get_glyph_advances_func_t = (font: never, font_data: never | null, count: number, first_glyph: never, glyph_stride: number, advance_stride: number) => never
            /**
             * A virtual method for the #hb_font_funcs_t of an #hb_font_t object.
             *
             * This method should retrieve the (X,Y) coordinates (in scaled units) for a
             * specified contour point in a glyph. Each coordinate must be returned as
             * an #hb_position_t output parameter.
             * @param font #hb_font_t to work upon
             * @param font_data  `font` user data pointer
             * @param glyph The glyph ID to query
             * @param point_index The contour-point index to query
             * @returns `true` if data found, `false` otherwise, The X value retrieved for the contour point, The Y value retrieved for the contour point
             */
            type font_get_glyph_contour_point_func_t = (font: never, font_data: never | null, glyph: never, point_index: number) => [never, never, never]
            /**
             * A virtual method for the #hb_font_funcs_t of an #hb_font_t object.
             *
             * This method should retrieve the extents for a specified glyph. Extents must be
             * returned in an #hb_glyph_extents output parameter.
             * @param font #hb_font_t to work upon
             * @param font_data  `font` user data pointer
             * @param glyph The glyph ID to query
             * @returns `true` if data found, `false` otherwise, The #hb_glyph_extents_t retrieved
             */
            type font_get_glyph_extents_func_t = (font: never, font_data: never | null, glyph: never) => [never, never]
            /**
             * A virtual method for the #hb_font_funcs_t of an #hb_font_t object.
             *
             * This method should retrieve the glyph ID that corresponds to a glyph-name
             * string.
             * @param font #hb_font_t to work upon
             * @param font_data  `font` user data pointer
             * @param name The name string to query
             * @returns `true` if data found, `false` otherwise, The glyph ID retrieved
             */
            type font_get_glyph_from_name_func_t = (font: never, font_data: never | null, name: string[]) => [never, never]
            /**
             * A virtual method for the #hb_font_funcs_t of an #hb_font_t object.
             *
             * This method should retrieve the glyph ID for a specified Unicode code point
             * font, with an optional variation selector.
             * @deprecated since 1.2.3
             * @param font #hb_font_t to work upon
             * @param font_data  `font` user data pointer
             * @param unicode The Unicode code point to query
             * @param variation_selector The  variation-selector code point to query
             * @returns `true` if data found, `false` otherwise, The glyph ID retrieved
             */
            type font_get_glyph_func_t = (font: never, font_data: never | null, unicode: never, variation_selector: never) => [never, never]
            /**
             * This method should retrieve the kerning-adjustment value for a glyph-pair in
             * the specified font, for horizontal text segments.
             * @param font #hb_font_t to work upon
             * @param font_data  `font` user data pointer
             * @param first_glyph The glyph ID of the first glyph in the glyph pair
             * @param second_glyph The glyph ID of the second glyph in the glyph pair
             */
            type font_get_glyph_kerning_func_t = (font: never, font_data: never | null, first_glyph: never, second_glyph: never) => never
            /**
             * A virtual method for the #hb_font_funcs_t of an #hb_font_t object.
             *
             * This method should retrieve the glyph name that corresponds to a
             * glyph ID. The name should be returned in a string output parameter.
             * @param font #hb_font_t to work upon
             * @param font_data  `font` user data pointer
             * @param glyph The glyph ID to query
             * @returns `true` if data found, `false` otherwise, Name string retrieved for the glyph ID
             */
            type font_get_glyph_name_func_t = (font: never, font_data: never | null, glyph: never) => [never, string[]]
            /**
             * A virtual method for the #hb_font_funcs_t of an #hb_font_t object.
             *
             * This method should retrieve the (X,Y) coordinates (in scaled units) of the
             * origin for a glyph. Each coordinate must be returned in an #hb_position_t
             * output parameter.
             * @param font #hb_font_t to work upon
             * @param font_data  `font` user data pointer
             * @param glyph The glyph ID to query
             * @returns `true` if data found, `false` otherwise, The X coordinate of the origin, The Y coordinate of the origin
             */
            type font_get_glyph_origin_func_t = (font: never, font_data: never | null, glyph: never) => [never, never, never]
            /**
             * A virtual method for the #hb_font_funcs_t of an #hb_font_t object.
             *
             * This method should retrieve the (X,Y) coordinates (in scaled units) of the
             * origin for each requested glyph. Each coordinate value must be returned in
             * an #hb_position_t in the two output parameters.
             * @since 11.3.0
             * @param font #hb_font_t to work upon
             * @param font_data  `font` user data pointer
             * @param count number of glyphs to query
             * @param first_glyph The first glyph ID to query
             * @param glyph_stride The stride between successive glyph IDs
             * @param x_stride The stride between successive origin X coordinates
             * @param y_stride The stride between successive origin Y coordinates
             * @returns `true` if data found, `false` otherwise, The first origin X coordinate retrieved, The first origin Y coordinate retrieved
             */
            type font_get_glyph_origins_func_t = (font: never, font_data: never | null, count: number, first_glyph: never, glyph_stride: number, x_stride: number, y_stride: number) => [never, never, never]
            /**
             * A virtual method for the #hb_font_funcs_t of an #hb_font_t object.
             * @since 4.0.0
             * @deprecated since 7.0.0 Use #hb_font_draw_glyph_func_t instead
             * @param font #hb_font_t to work upon
             * @param font_data  `font` user data pointer
             * @param glyph The glyph ID to query
             * @param draw_funcs The draw functions to send the shape data to
             * @param draw_data The data accompanying the draw functions
             */
            type font_get_glyph_shape_func_t = (font: never, font_data: never | null, glyph: never, draw_funcs: never, draw_data: never | null) => void
            /**
             * A virtual method for the #hb_font_funcs_t of an #hb_font_t object.
             *
             * This method should retrieve the nominal glyph ID for a specified Unicode code
             * point. Glyph IDs must be returned in a #hb_codepoint_t output parameter.
             * @param font #hb_font_t to work upon
             * @param font_data  `font` user data pointer
             * @param unicode The Unicode code point to query
             * @returns `true` if data found, `false` otherwise, The glyph ID retrieved
             */
            type font_get_nominal_glyph_func_t = (font: never, font_data: never | null, unicode: never) => [never, never]
            /**
             * A virtual method for the #hb_font_funcs_t of an #hb_font_t object.
             *
             * This method should retrieve the nominal glyph IDs for a sequence of
             * Unicode code points. Glyph IDs must be returned in a #hb_codepoint_t
             * output parameter.
             * @param font #hb_font_t to work upon
             * @param font_data  `font` user data pointer
             * @param count number of code points to query
             * @param first_unicode The first Unicode code point to query
             * @param unicode_stride The stride between successive code points
             * @param glyph_stride The stride between successive glyph IDs
             * @returns the number of code points processed, The first glyph ID retrieved
             */
            type font_get_nominal_glyphs_func_t = (font: never, font_data: never | null, count: number, first_unicode: never, unicode_stride: number, glyph_stride: number) => [number, never]
            /**
             * A virtual method for the #hb_font_funcs_t of an #hb_font_t object.
             *
             * This method should retrieve the glyph ID for a specified Unicode code point
             * followed by a specified Variation Selector code point. Glyph IDs must be
             * returned in a #hb_codepoint_t output parameter.
             * @param font #hb_font_t to work upon
             * @param font_data  `font` user data pointer
             * @param unicode The Unicode code point to query
             * @param variation_selector The  variation-selector code point to query
             * @returns `true` if data found, `false` otherwise, The glyph ID retrieved
             */
            type font_get_variation_glyph_func_t = (font: never, font_data: never | null, unicode: never, variation_selector: never) => [never, never]
            /**
             * A virtual method for the #hb_font_funcs_t of an #hb_font_t object.
             * @since 7.0.0
             * @deprecated since 11.2.0 Use hb_font_paint_glyph_or_fail_func_t instead.
             * @param font #hb_font_t to work upon
             * @param font_data  `font` user data pointer
             * @param glyph The glyph ID to query
             * @param paint_funcs The paint functions to use
             * @param paint_data The data accompanying the paint functions
             * @param palette_index The color palette to use
             * @param foreground The foreground color
             */
            type font_paint_glyph_func_t = (font: never, font_data: never | null, glyph: never, paint_funcs: never, paint_data: never | null, palette_index: number, foreground: never) => never
            /**
             * A virtual method for the #hb_font_funcs_t of an #hb_font_t object.
             * @since 11.2.0
             * @param font #hb_font_t to work upon
             * @param font_data  `font` user data pointer
             * @param glyph The glyph ID to query
             * @param paint_funcs The paint functions to use
             * @param paint_data The data accompanying the paint functions
             * @param palette_index The color palette to use
             * @param foreground The foreground color
             * @returns `true` if glyph was painted, `false` otherwise
             */
            type font_paint_glyph_or_fail_func_t = (font: never, font_data: never | null, glyph: never, paint_funcs: never, paint_data: never | null, palette_index: number, foreground: never) => never
            /**
             * Callback function for hb_face_get_table_tags().
             * @since 10.0.0
             * @param face A face object
             * @param start_offset The index of first table tag to retrieve
             * @returns Total number of tables, or zero if it is not possible to list, The array of table tags found
             */
            type get_table_tags_func_t = (face: never, start_offset: number) => [number, never[] | null]
            /**
             * A virtual method for the #hb_paint_funcs_t to paint a
             * color everywhere within the current clip.
             *
             * When `is_foreground` is true, this color originates from the
             * foreground-color sentinel in the font's color data.  The
             *  `color` parameter still carries a fully resolved RGBA value
             * (with any paint-tree alpha already applied), so backends
             * that do not need to distinguish the foreground can simply
             * use `color` directly.
             *
             * Backends that defer foreground resolution (e.g. to honor a
             * CSS `currentColor` or a runtime uniform) should substitute
             * their own foreground RGB when `is_foreground` is true, but
             * must combine the alpha from `color` with their foreground
             * alpha, since it encodes additional modulation from the
             * paint tree.  For this mode to work correctly, the caller
             * should pass a fully-opaque foreground color to
             * hb_font_paint_glyph(), so that the alpha in `color`
             * reflects only the paint-tree contribution.
             * @since 7.0.0
             * @param funcs paint functions object
             * @param paint_data The data accompanying the paint functions in hb_font_paint_glyph()
             * @param is_foreground whether the color is the foreground
             * @param color The color to use, unpremultiplied
             */
            type paint_color_func_t = (funcs: never, paint_data: never | null, is_foreground: never, color: never) => void
            /**
             * A virtual method for the #hb_paint_funcs_t to render a color glyph by glyph index.
             * @since 8.2.0
             * @param funcs paint functions object
             * @param paint_data The data accompanying the paint functions in hb_font_paint_glyph()
             * @param glyph the glyph ID
             * @param font the font
             * @returns `true` if the glyph was painted, `false` otherwise.
             */
            type paint_color_glyph_func_t = (funcs: never, paint_data: never | null, glyph: never, font: never) => never
            /**
             * A virtual method for #hb_paint_funcs_t to fetch a custom palette override
             * color for `color_index`.
             *
             * Custom palette colors override colors from the font's selected color palette.
             * It is not necessary to override all palette entries; return `false` for
             * entries that should be taken from the font palette.
             *
             * This function might be called multiple times, but the custom palette is
             * expected to remain unchanged for the duration of one
             * hb_font_paint_glyph() call.
             * @since 7.0.0
             * @param funcs paint functions object.
             * @param paint_data data accompanying the paint functions in hb_font_paint_glyph().
             * @param color_index color index to fetch.
             * @returns `true` if a custom color is provided, `false` otherwise., fetched color.
             */
            type paint_custom_palette_color_func_t = (funcs: never, paint_data: never | null, color_index: number) => [never, never]
            /**
             * A virtual method for the #hb_paint_funcs_t to fill a glyph's shape with
             * a solid color. If not implemented, a sequence of "push-clip-glyph",
             * "color", "pop-clip" paint operations, in that order, will be emitted
             * instead.
             * @since 14.3.0
             * @param funcs paint functions object
             * @param paint_data The data accompanying the paint functions in hb_font_paint_glyph()
             * @param glyph the glyph ID
             * @param font the font
             * @param is_foreground whether the color is the foreground
             * @param color The color to use, unpremultiplied
             */
            type paint_fill_glyph_func_t = (funcs: never, paint_data: never | null, glyph: never, font: never, is_foreground: never, color: never) => void
            /**
             * A virtual method for the #hb_paint_funcs_t to paint a glyph image.
             *
             * This method is called for glyphs with image blobs in the CBDT,
             * sbix or SVG tables. The `format` identifies the kind of data that
             * is contained in `image`. Possible values include #HB_PAINT_IMAGE_FORMAT_PNG,
             * #HB_PAINT_IMAGE_FORMAT_SVG and #HB_PAINT_IMAGE_FORMAT_BGRA.
             *
             * The image dimensions and glyph extents are provided if available,
             * and should be used to size and position the image.
             * @since 7.0.0
             * @param funcs paint functions object
             * @param paint_data The data accompanying the paint functions in hb_font_paint_glyph()
             * @param image the image data
             * @param width width of the raster image in pixels, or 0
             * @param height height of the raster image in pixels, or 0
             * @param format the image format as a tag
             * @param slant Deprecated. Always set to 0.0.
             * @param extents glyph extents for desired rendering
             * @returns Whether the operation was successful.
             */
            type paint_image_func_t = (funcs: never, paint_data: never | null, image: never, width: number, height: number, format: never, slant: number, extents: never | null) => never
            /**
             * A virtual method for the #hb_paint_funcs_t to paint a linear
             * gradient everywhere within the current clip.
             *
             * The `color_line` object contains information about the colors of the gradients.
             * It is only valid for the duration of the callback, you cannot keep it around.
             *
             * The coordinates of the points are interpreted according
             * to the current transform.
             *
             * See the OpenType spec [COLR](https://learn.microsoft.com/en-us/typography/opentype/spec/colr)
             * section for details on how the points define the direction
             * of the gradient, and how to interpret the `color_line`.
             * @since 7.0.0
             * @param funcs paint functions object
             * @param paint_data The data accompanying the paint functions in hb_font_paint_glyph()
             * @param color_line Color information for the gradient
             * @param x0 X coordinate of the first point
             * @param y0 Y coordinate of the first point
             * @param x1 X coordinate of the second point
             * @param y1 Y coordinate of the second point
             * @param x2 X coordinate of the third point
             * @param y2 Y coordinate of the third point
             */
            type paint_linear_gradient_func_t = (funcs: never, paint_data: never | null, color_line: never, x0: number, y0: number, x1: number, y1: number, x2: number, y2: number) => void
            /**
             * A virtual method for the #hb_paint_funcs_t to undo
             * the effect of a prior call to the #hb_paint_funcs_push_clip_glyph_func_t,
             * #hb_paint_funcs_push_clip_rectangle_func_t, or
             * #hb_paint_funcs_push_clip_path_end_func_t vfuncs.
             * @since 7.0.0
             * @param funcs paint functions object
             * @param paint_data The data accompanying the paint functions in hb_font_paint_glyph()
             */
            type paint_pop_clip_func_t = (funcs: never, paint_data: never | null) => void
            /**
             * A virtual method for the #hb_paint_funcs_t to undo
             * the effect of a prior call to the #hb_paint_funcs_push_group_func_t
             * vfunc.
             *
             * This call stops the redirection to the intermediate surface,
             * and then composites it on the previous surface, using the
             * compositing mode passed to this call.
             * @since 7.0.0
             * @param funcs paint functions object
             * @param paint_data The data accompanying the paint functions in hb_font_paint_glyph()
             * @param mode the compositing mode to use
             */
            type paint_pop_group_func_t = (funcs: never, paint_data: never | null, mode: never) => void
            /**
             * A virtual method for the #hb_paint_funcs_t to undo
             * the effect of a prior call to the #hb_paint_funcs_push_transform_func_t
             * vfunc.
             * @since 7.0.0
             * @param funcs paint functions object
             * @param paint_data The data accompanying the paint functions in hb_font_paint_glyph()
             */
            type paint_pop_transform_func_t = (funcs: never, paint_data: never | null) => void
            /**
             * A virtual method for the #hb_paint_funcs_t to clip
             * subsequent paint calls to the outline of a glyph.
             *
             * The coordinates of the glyph outline are expected in the
             * current `font` scale (ie. the results of calling
             * hb_font_draw_glyph() with `font`). The outline is
             * transformed by the current transform.
             *
             * This clip is applied in addition to the current clip,
             * and remains in effect until a matching call to
             * the #hb_paint_funcs_pop_clip_func_t vfunc.
             * @since 7.0.0
             * @param funcs paint functions object
             * @param paint_data The data accompanying the paint functions in hb_font_paint_glyph()
             * @param glyph the glyph ID
             * @param font the font
             */
            type paint_push_clip_glyph_func_t = (funcs: never, paint_data: never | null, glyph: never, font: never) => void
            /**
             * A virtual method for the #hb_paint_funcs_t to close the
             * clip path started by the #hb_paint_push_clip_path_start_func_t
             * vfunc.  The emitted path is now active as a clip; subsequent
             * paint ops are masked by it until a matching
             * #hb_paint_pop_clip_func_t call.
             * @since 14.2.0
             * @param funcs paint functions object
             * @param paint_data The data accompanying the paint functions in hb_font_paint_glyph()
             */
            type paint_push_clip_path_end_func_t = (funcs: never, paint_data: never | null) => void
            /**
             * A virtual method for the #hb_paint_funcs_t to begin clipping
             * to an arbitrary path.  The backend returns an
             * #hb_draw_funcs_t it owns (the caller must not free it)
             * that the caller feeds the clip outline to via hb_draw_*()
             * calls, plus a `draw_data` value to pass alongside those
             * calls.  Both are only valid until the matching
             * #hb_paint_push_clip_path_end_func_t call; no other paint
             * calls should be made in between.  The clip remains
             * in effect until a later #hb_paint_pop_clip_func_t call.
             * @since 14.2.0
             * @param funcs paint functions object
             * @param paint_data The data accompanying the paint functions in hb_font_paint_glyph()
             * @returns draw funcs that accumulate   the clip path, or `NULL` if arbitrary-path clipping is not   supported., location to store the draw data the caller should   pass alongside the returned draw funcs.
             */
            type paint_push_clip_path_start_func_t = (funcs: never, paint_data: never | null) => [never, never | null]
            /**
             * A virtual method for the #hb_paint_funcs_t to clip
             * subsequent paint calls to a rectangle.
             *
             * The coordinates of the rectangle are interpreted according
             * to the current transform.
             *
             * This clip is applied in addition to the current clip,
             * and remains in effect until a matching call to
             * the #hb_paint_funcs_pop_clip_func_t vfunc.
             * @since 7.0.0
             * @param funcs paint functions object
             * @param paint_data The data accompanying the paint functions in hb_font_paint_glyph()
             * @param xmin min X for the rectangle
             * @param ymin min Y for the rectangle
             * @param xmax max X for the rectangle
             * @param ymax max Y for the rectangle
             */
            type paint_push_clip_rectangle_func_t = (funcs: never, paint_data: never | null, xmin: number, ymin: number, xmax: number, ymax: number) => void
            /**
             * A virtual method for the #hb_paint_funcs_t to use
             * an intermediate surface for subsequent paint calls,
             * with the compositing mode known in advance.
             *
             * This is like #hb_paint_push_group_func_t, but the
             * compositing mode is provided at push time. By default
             * this calls #hb_paint_push_group_func_t.
             * @since 14.2.0
             * @param funcs paint functions object
             * @param paint_data The data accompanying the paint functions in hb_font_paint_glyph()
             * @param mode the compositing mode that will be used when the group is popped
             */
            type paint_push_group_for_func_t = (funcs: never, paint_data: never | null, mode: never) => void
            /**
             * A virtual method for the #hb_paint_funcs_t to use
             * an intermediate surface for subsequent paint calls.
             *
             * The drawing will be redirected to an intermediate surface
             * until a matching call to the #hb_paint_funcs_pop_group_func_t
             * vfunc.
             * @since 7.0.0
             * @param funcs paint functions object
             * @param paint_data The data accompanying the paint functions in hb_font_paint_glyph()
             */
            type paint_push_group_func_t = (funcs: never, paint_data: never | null) => void
            /**
             * A virtual method for the #hb_paint_funcs_t to apply
             * a transform to subsequent paint calls.
             *
             * This transform is applied after the current transform,
             * and remains in effect until a matching call to
             * the #hb_paint_funcs_pop_transform_func_t vfunc.
             * @since 7.0.0
             * @param funcs paint functions object
             * @param paint_data The data accompanying the paint functions in hb_font_paint_glyph()
             * @param xx xx component of the transform matrix
             * @param yx yx component of the transform matrix
             * @param xy xy component of the transform matrix
             * @param yy yy component of the transform matrix
             * @param dx dx component of the transform matrix
             * @param dy dy component of the transform matrix
             */
            type paint_push_transform_func_t = (funcs: never, paint_data: never | null, xx: number, yx: number, xy: number, yy: number, dx: number, dy: number) => void
            /**
             * A virtual method for the #hb_paint_funcs_t to paint a radial
             * gradient everywhere within the current clip.
             *
             * The `color_line` object contains information about the colors of the gradients.
             * It is only valid for the duration of the callback, you cannot keep it around.
             *
             * The coordinates of the points are interpreted according
             * to the current transform.
             *
             * See the OpenType spec [COLR](https://learn.microsoft.com/en-us/typography/opentype/spec/colr)
             * section for details on how the points define the direction
             * of the gradient, and how to interpret the `color_line`.
             * @since 7.0.0
             * @param funcs paint functions object
             * @param paint_data The data accompanying the paint functions in hb_font_paint_glyph()
             * @param color_line Color information for the gradient
             * @param x0 X coordinate of the first circle's center
             * @param y0 Y coordinate of the first circle's center
             * @param r0 radius of the first circle
             * @param x1 X coordinate of the second circle's center
             * @param y1 Y coordinate of the second circle's center
             * @param r1 radius of the second circle
             */
            type paint_radial_gradient_func_t = (funcs: never, paint_data: never | null, color_line: never, x0: number, y0: number, r0: number, x1: number, y1: number, r1: number) => void
            /**
             * A virtual method for the #hb_paint_funcs_t to paint a sweep
             * gradient everywhere within the current clip.
             *
             * The `color_line` object contains information about the colors of the gradients.
             * It is only valid for the duration of the callback, you cannot keep it around.
             *
             * The coordinates of the points are interpreted according
             * to the current transform.
             *
             * See the OpenType spec [COLR](https://learn.microsoft.com/en-us/typography/opentype/spec/colr)
             * section for details on how the points define the direction
             * of the gradient, and how to interpret the `color_line`.
             * @since 7.0.0
             * @param funcs paint functions object
             * @param paint_data The data accompanying the paint functions in hb_font_paint_glyph()
             * @param color_line Color information for the gradient
             * @param x0 X coordinate of the circle's center
             * @param y0 Y coordinate of the circle's center
             * @param start_angle the start angle, in radians
             * @param end_angle the end angle, in radians
             */
            type paint_sweep_gradient_func_t = (funcs: never, paint_data: never | null, color_line: never, x0: number, y0: number, start_angle: number, end_angle: number) => void
            /**
             * Callback invoked once per (a0, a1) sector of a sweep
             * gradient tiling.  See hb_paint_sweep_gradient_tiles().
             * @since 14.2.0
             * @param a0 segment start angle, in radians.
             * @param c0 segment start color.
             * @param a1 segment end angle, in radians.
             * @param c1 segment end color.
             */
            type paint_sweep_gradient_tile_func_t = (a0: number, c0: never, a1: number, c1: never) => void
            /**
             * Callback function for hb_face_create_for_tables(). The `tag` is the tag of the
             * table to reference, and the special tag #HB_TAG_NONE is used to reference the
             * blob of the face itself. If referencing the face blob is not possible, it is
             * recommended to set hb_get_table_tags_func_t on the `face` to allow
             * hb_face_reference_blob() to create a face blob out of individual table blobs.
             * @since 0.9.2
             * @param face an #hb_face_t to reference table for
             * @param tag the tag of the table to reference
             * @returns A pointer to the `tag` table within `face` or `NULL` if the table is not found or cannot be referenced.
             */
            type reference_table_func_t = (face: never, tag: never) => never
            /**
             * A virtual method for the #hb_unicode_funcs_t structure.
             *
             * This method should retrieve the Canonical Combining Class (ccc)
             * property for a specified Unicode code point.
             * @param ufuncs A Unicode-functions structure
             * @param unicode The code point to query
             * @returns The #hb_unicode_combining_class_t of `unicode`
             */
            type unicode_combining_class_func_t = (ufuncs: never, unicode: never) => never
            /**
             * A virtual method for the #hb_unicode_funcs_t structure.
             *
             * This method should compose a sequence of two input Unicode code
             * points by canonical equivalence, returning the composed code
             * point in a #hb_codepoint_t output parameter (if successful).
             * The method must return an #hb_bool_t indicating the success
             * of the composition.
             * @param ufuncs A Unicode-functions structure
             * @param a The first code point to compose
             * @param b The second code point to compose
             * @returns `true` is `a`,@b composed, `false` otherwise, The composed code point
             */
            type unicode_compose_func_t = (ufuncs: never, a: never, b: never) => [never, never]
            /**
             * Fully decompose `u` to its Unicode compatibility decomposition. The codepoints of the decomposition will be written to `decomposed`.
             * The complete length of the decomposition will be returned.
             *
             * If `u` has no compatibility decomposition, zero should be returned.
             *
             * The Unicode standard guarantees that a buffer of length #HB_UNICODE_MAX_DECOMPOSITION_LEN codepoints will always be sufficient for any
             * compatibility decomposition plus an terminating value of 0.  Consequently, `decompose` must be allocated by the caller to be at least this length.  Implementations
             * of this function type must ensure that they do not write past the provided array.
             * @deprecated since 2.0.0
             * @param ufuncs a Unicode function structure
             * @param u codepoint to decompose
             * @param decomposed address of codepoint array (of length #HB_UNICODE_MAX_DECOMPOSITION_LEN) to write decomposition into
             * @returns number of codepoints in the full compatibility decomposition of `u`, or 0 if no decomposition available.
             */
            type unicode_decompose_compatibility_func_t = (ufuncs: never, u: never, decomposed: never) => number
            /**
             * A virtual method for the #hb_unicode_funcs_t structure.
             *
             * This method should decompose an input Unicode code point,
             * returning the two decomposed code points in #hb_codepoint_t
             * output parameters (if successful). The method must return an
             * #hb_bool_t indicating the success of the composition.
             * @param ufuncs A Unicode-functions structure
             * @param ab The code point to decompose
             * @returns `true` if `ab` decomposed, `false` otherwise, The first decomposed code point, The second decomposed code point
             */
            type unicode_decompose_func_t = (ufuncs: never, ab: never) => [never, never, never]
            /**
             * A virtual method for the #hb_unicode_funcs_t structure.
             * @deprecated since 2.0.0
             * @param ufuncs A Unicode-functions structure
             * @param unicode The code point to query
             */
            type unicode_eastasian_width_func_t = (ufuncs: never, unicode: never) => number
            /**
             * A virtual method for the #hb_unicode_funcs_t structure.
             *
             * This method should retrieve the General Category property for
             * a specified Unicode code point.
             * @param ufuncs A Unicode-functions structure
             * @param unicode The code point to query
             * @returns The #hb_unicode_general_category_t of `unicode`
             */
            type unicode_general_category_func_t = (ufuncs: never, unicode: never) => never
            /**
             * A virtual method for the #hb_unicode_funcs_t structure.
             *
             * This method should retrieve the Bi-Directional Mirroring Glyph
             * code point for a specified Unicode code point.
             *
             * <note>Note: If a code point does not have a specified
             * Bi-Directional Mirroring Glyph defined, the method should
             * return the original code point.</note>
             * @param ufuncs A Unicode-functions structure
             * @param unicode The code point to query
             * @returns The #hb_codepoint_t of the Mirroring Glyph for `unicode`
             */
            type unicode_mirroring_func_t = (ufuncs: never, unicode: never) => never
            /**
             * A virtual method for the #hb_unicode_funcs_t structure.
             *
             * This method should retrieve the Script property for a
             * specified Unicode code point.
             * @param ufuncs A Unicode-functions structure
             * @param unicode The code point to query
             * @returns The #hb_script_t of `unicode`
             */
            type unicode_script_func_t = (ufuncs: never, unicode: never) => never
            /**
             * Data type for booleans.
             */
            type bool_t = number
            /**
             * Data type for holding Unicode codepoints. Also
             * used to hold glyph IDs.
             */
            type codepoint_t = number
            /**
             * Data type for holding color values. Colors are eight bits per
             * channel RGB plus alpha transparency.
             */
            type color_t = number
            /**
             * A virtual method for the #hb_font_funcs_t of an #hb_font_t object.
             *
             * This method should retrieve the extents for a font, for horizontal-direction
             * text segments. Extents must be returned in an #hb_glyph_extents output
             * parameter.
             */
            type font_get_font_h_extents_func_t = never
            /**
             * A virtual method for the #hb_font_funcs_t of an #hb_font_t object.
             *
             * This method should retrieve the extents for a font, for vertical-direction
             * text segments. Extents must be returned in an #hb_glyph_extents output
             * parameter.
             */
            type font_get_font_v_extents_func_t = never
            /**
             * A virtual method for the #hb_font_funcs_t of an #hb_font_t object.
             *
             * This method should retrieve the advance for a specified glyph, in
             * horizontal-direction text segments. Advances must be returned in
             * an #hb_position_t output parameter.
             */
            type font_get_glyph_h_advance_func_t = never
            /**
             * A virtual method for the #hb_font_funcs_t of an #hb_font_t object.
             *
             * This method should retrieve the advances for a sequence of glyphs, in
             * horizontal-direction text segments.
             */
            type font_get_glyph_h_advances_func_t = never
            /**
             * A virtual method for the #hb_font_funcs_t of an #hb_font_t object.
             *
             * This method should retrieve the kerning-adjustment value for a glyph-pair in
             * the specified font, for horizontal text segments.
             */
            type font_get_glyph_h_kerning_func_t = never
            /**
             * A virtual method for the #hb_font_funcs_t of an #hb_font_t object.
             *
             * This method should retrieve the (X,Y) coordinates (in scaled units) of the
             * origin for a glyph, for horizontal-direction text segments. Each
             * coordinate must be returned in an #hb_position_t output parameter.
             */
            type font_get_glyph_h_origin_func_t = never
            /**
             * A virtual method for the #hb_font_funcs_t of an #hb_font_t object.
             *
             * This method should retrieve the (X,Y) coordinates (in scaled units) of the
             * origin for requested glyph, for horizontal-direction text segments. Each
             * coordinate must be returned in a the x/y #hb_position_t output parameters.
             */
            type font_get_glyph_h_origins_func_t = never
            /**
             * A virtual method for the #hb_font_funcs_t of an #hb_font_t object.
             *
             * This method should retrieve the advance for a specified glyph, in
             * vertical-direction text segments. Advances must be returned in
             * an #hb_position_t output parameter.
             */
            type font_get_glyph_v_advance_func_t = never
            /**
             * A virtual method for the #hb_font_funcs_t of an #hb_font_t object.
             *
             * This method should retrieve the advances for a sequence of glyphs, in
             * vertical-direction text segments.
             */
            type font_get_glyph_v_advances_func_t = never
            /**
             * A virtual method for the #hb_font_funcs_t of an #hb_font_t object.
             *
             * This method should retrieve the kerning-adjustment value for a glyph-pair in
             * the specified font, for vertical text segments.
             */
            type font_get_glyph_v_kerning_func_t = never
            /**
             * A virtual method for the #hb_font_funcs_t of an #hb_font_t object.
             *
             * This method should retrieve the (X,Y) coordinates (in scaled units) of the
             * origin for a glyph, for vertical-direction text segments. Each coordinate
             * must be returned in an #hb_position_t output parameter.
             */
            type font_get_glyph_v_origin_func_t = never
            /**
             * A virtual method for the #hb_font_funcs_t of an #hb_font_t object.
             *
             * This method should retrieve the (X,Y) coordinates (in scaled units) of the
             * origin for requested glyph, for vertical-direction text segments. Each
             * coordinate must be returned in a the x/y #hb_position_t output parameters.
             */
            type font_get_glyph_v_origins_func_t = never
            /**
             * Data type for bitmasks.
             */
            type mask_t = number
            /**
             * An integral type representing an OpenType 'name' table name identifier.
             * There are predefined name IDs, as well as name IDs return from other
             * API.  These can be used to fetch name strings from a font face.
             */
            type ot_name_id_t = number
            /**
             * Data type for holding a single coordinate value.
             * Contour points and other multi-dimensional data are
             * stored as tuples of #hb_position_t's.
             */
            type position_t = number
            /**
             * Data type for tag identifiers. Tags are four
             * byte integers, each byte representing a character.
             *
             * Tags are used to identify tables, design-variation axes,
             * scripts, languages, font features, and baselines with
             * human-readable names.
             */
            type tag_t = number

            interface $Exports {
                __name__: "HarfBuzz"
                __version__: "0.0"
                AAT_LAYOUT_NO_SELECTOR_INDEX: 65535
                BUFFER_REPLACEMENT_CODEPOINT_DEFAULT: 65533
                CODEPOINT_INVALID: never
                FEATURE_GLOBAL_START: 0
                FONT_NO_VAR_NAMED_INSTANCE: 4294967295
                LANGUAGE_INVALID: never
                OT_LAYOUT_DEFAULT_LANGUAGE_INDEX: 65535
                OT_LAYOUT_NO_FEATURE_INDEX: 65535
                OT_LAYOUT_NO_SCRIPT_INDEX: 65535
                OT_LAYOUT_NO_VARIATIONS_INDEX: 4294967295
                OT_MAX_TAGS_PER_LANGUAGE: 3
                OT_MAX_TAGS_PER_SCRIPT: 3
                OT_SHAPE_BUFFER_FORMAT_SERIAL: 1
                OT_VAR_NO_AXIS_INDEX: 4294967295
                UNICODE_COMBINING_CLASS_CCC133: 133
                UNICODE_MAX: 1114111
                UNICODE_MAX_DECOMPOSITION_LEN: 19
                VERSION_MAJOR: 14
                VERSION_MICRO: 1
                VERSION_MINOR: 3
                VERSION_STRING: "14.3.1"
                /**
                 * Fetches the name identifier of the specified feature type in the face's `name` table.
                 * @since 2.2.0
                 * @param face #hb_face_t to work upon
                 * @param feature_type The #hb_aat_layout_feature_type_t of the requested feature type
                 * @returns Name identifier of the requested feature type
                 */
                aat_layout_feature_type_get_name_id(face: never, feature_type: never): never
                /**
                 * Fetches a list of the selectors available for the specified feature in the given face.
                 *
                 * If upon return, `default_index` is set to #HB_AAT_LAYOUT_NO_SELECTOR_INDEX, then
                 * the feature type is non-exclusive.  Otherwise, `default_index` is the index of
                 * the selector that is selected by default.
                 * @since 2.2.0
                 * @param face #hb_face_t to work upon
                 * @param feature_type The #hb_aat_layout_feature_type_t of the requested feature type
                 * @param start_offset offset of the first feature type to retrieve
                 * @returns Number of all available feature selectors,              A buffer pointer. The selectors available for the feature type queries., The index of the feature's default selector, if any
                 */
                aat_layout_feature_type_get_selector_infos(face: never, feature_type: never, start_offset: number): [number, never[], number]
                /**
                 * Fetches a list of the AAT feature types included in the specified face.
                 * @since 2.2.0
                 * @param face #hb_face_t to work upon
                 * @param start_offset offset of the first feature type to retrieve
                 * @returns Number of all available feature types., Array of feature types found
                 */
                aat_layout_get_feature_types(face: never, start_offset: number): [number, never[] | null]
                /**
                 * Tests whether the specified face includes any positioning information
                 * in the `kerx` table.
                 *
                 * <note>Note: does not examine the `GPOS` table.</note>
                 * @since 2.3.0
                 * @param face #hb_face_t to work upon
                 * @returns `true` if data found, `false` otherwise
                 */
                aat_layout_has_positioning(face: never): never
                /**
                 * Tests whether the specified face includes any substitutions in the
                 * `morx` or `mort` tables.
                 *
                 * <note>Note: does not examine the `GSUB` table.</note>
                 * @since 2.3.0
                 * @param face #hb_face_t to work upon
                 * @returns `true` if data found, `false` otherwise
                 */
                aat_layout_has_substitution(face: never): never
                /**
                 * Tests whether the specified face includes any tracking information
                 * in the `trak` table.
                 * @since 2.3.0
                 * @param face #hb_face_t to work upon
                 * @returns `true` if data found, `false` otherwise
                 */
                aat_layout_has_tracking(face: never): never
                /**
                 * Makes a writable copy of `blob`.
                 * @since 1.8.0
                 * @param blob A blob.
                 * @returns The new blob, or nullptr if allocation failed
                 */
                blob_copy_writable_or_fail(blob: never): never
                /**
                 * Creates a new blob containing the data from the
                 * specified binary font file.
                 *
                 * The filename is passed directly to the system on all platforms,
                 * except on Windows, where the filename is interpreted as UTF-8.
                 * Only if the filename is not valid UTF-8, it will be interpreted
                 * according to the system codepage.
                 * @since 1.7.7
                 * @param file_name A font filename
                 * @returns An #hb_blob_t pointer with the content of the file, or hb_blob_get_empty() if failed.
                 */
                blob_create_from_file(file_name: string): never
                /**
                 * Creates a new blob containing the data from the specified file.
                 *
                 * The filename is passed directly to the system on all platforms,
                 * except on Windows, where the filename is interpreted as UTF-8.
                 * Only if the filename is not valid UTF-8, it will be interpreted
                 * according to the system codepage.
                 * @since 2.8.2
                 * @param file_name A filename
                 * @returns An #hb_blob_t pointer with the content of the file, or `NULL` if failed.
                 */
                blob_create_from_file_or_fail(file_name: string): never
                /**
                 * Returns a blob that represents a range of bytes in `parent`.  The new
                 * blob is always created with #HB_MEMORY_MODE_READONLY, meaning that it
                 * will never modify data in the parent blob.  The parent data is not
                 * expected to be modified, and will result in undefined behavior if it
                 * is.
                 *
                 * Makes `parent` immutable.
                 * @since 0.9.2
                 * @param parent Parent blob.
                 * @param offset Start offset of sub-blob within `parent`, in bytes.
                 * @param length Length of sub-blob.
                 * @returns New blob, or the empty blob if something failed or if `length` is zero or `offset` is beyond the end of `parent`'s data.  Destroy with hb_blob_destroy().
                 */
                blob_create_sub_blob(parent: never, offset: number, length: number): never
                /**
                 * Fetches the data from a blob.
                 * @since 0.9.2
                 * @param blob a blob.
                 * @returns the byte data of `blob`.
                 */
                blob_get_data(blob: never): string[] | null
                /**
                 * Tries to make blob data writable (possibly copying it) and
                 * return pointer to data.
                 *
                 * Fails if blob has been made immutable, or if memory allocation
                 * fails.
                 * @since 0.9.2
                 * @param blob a blob.
                 * @returns Writable blob data, or `NULL` if failed.
                 */
                blob_get_data_writable(blob: never): string[]
                /**
                 * Returns the singleton empty blob.
                 *
                 * See TODO:link object types for more information.
                 * @since 0.9.2
                 * @returns The empty blob.
                 */
                blob_get_empty(): never
                /**
                 * Fetches the length of a blob's data.
                 * @since 0.9.2
                 * @param blob a blob.
                 * @returns the length of `blob` data in bytes.
                 */
                blob_get_length(blob: never): number
                /**
                 * Tests whether a blob is immutable.
                 * @since 0.9.2
                 * @param blob a blob.
                 * @returns `true` if `blob` is immutable, `false` otherwise
                 */
                blob_is_immutable(blob: never): never
                /**
                 * Makes a blob immutable.
                 * @since 0.9.2
                 * @param blob a blob
                 */
                blob_make_immutable(blob: never): void
                /**
                 * Appends a character with the Unicode value of `codepoint` to `buffer`, and
                 * gives it the initial cluster value of `cluster`. Clusters can be any thing
                 * the client wants, they are usually used to refer to the index of the
                 * character in the input text stream and are output in
                 * #hb_glyph_info_t.cluster field.
                 *
                 * This function does not check the validity of `codepoint`, it is up to the
                 * caller to ensure it is a valid Unicode code point.
                 * @since 0.9.7
                 * @param buffer An #hb_buffer_t
                 * @param codepoint A Unicode code point.
                 * @param cluster The cluster value of `codepoint`.
                 */
                buffer_add(buffer: never, codepoint: never, cluster: number): void
                /**
                 * Appends characters from `text` array to `buffer`. The `item_offset` is the
                 * position of the first character from `text` that will be appended, and
                 *  `item_length` is the number of character. When shaping part of a larger text
                 * (e.g. a run of text from a paragraph), instead of passing just the substring
                 * corresponding to the run, it is preferable to pass the whole
                 * paragraph and specify the run start and length as `item_offset` and
                 *  `item_length`, respectively, to give HarfBuzz the full context to be able,
                 * for example, to do cross-run Arabic shaping or properly handle combining
                 * marks at stat of run.
                 *
                 * This function does not check the validity of `text`, it is up to the caller
                 * to ensure it contains a valid Unicode scalar values.  In contrast,
                 * hb_buffer_add_utf32() can be used that takes similar input but performs
                 * sanity-check on the input.
                 * @since 0.9.31
                 * @param buffer a #hb_buffer_t to append characters to.
                 * @param text an array of Unicode code points to append.
                 * @param item_offset the offset of the first code point to add to the `buffer`.
                 * @param item_length the number of code points to add to the `buffer`, or -1 for the               end of `text` (assuming it is `NULL` terminated).
                 */
                buffer_add_codepoints(buffer: never, text: never[], item_offset: number, item_length: number): void
                /**
                 * Similar to hb_buffer_add_codepoints(), but allows only access to first 256
                 * Unicode code points that can fit in 8-bit strings.
                 *
                 * <note>Has nothing to do with non-Unicode Latin-1 encoding.</note>
                 * @since 0.9.39
                 * @param buffer An #hb_buffer_t
                 * @param text an array of UTF-8               characters to append
                 * @param item_offset the offset of the first character to add to the `buffer`
                 * @param item_length the number of characters to add to the `buffer`, or -1 for the               end of `text` (assuming it is `NULL` terminated)
                 */
                buffer_add_latin1(buffer: never, text: Uint8Array, item_offset: number, item_length: number): void
                /**
                 * See hb_buffer_add_codepoints().
                 *
                 * Replaces invalid UTF-16 characters with the `buffer` replacement code point,
                 * see hb_buffer_set_replacement_codepoint().
                 * @since 0.9.2
                 * @param buffer An #hb_buffer_t
                 * @param text An array of UTF-16 characters to append
                 * @param item_offset The offset of the first character to add to the `buffer`
                 * @param item_length The number of characters to add to the `buffer`, or -1 for the               end of `text` (assuming it is `NULL` terminated)
                 */
                buffer_add_utf16(buffer: never, text: number[], item_offset: number, item_length: number): void
                /**
                 * See hb_buffer_add_codepoints().
                 *
                 * Replaces invalid UTF-32 characters with the `buffer` replacement code point,
                 * see hb_buffer_set_replacement_codepoint().
                 * @since 0.9.2
                 * @param buffer An #hb_buffer_t
                 * @param text An array of UTF-32 characters to append
                 * @param item_offset The offset of the first character to add to the `buffer`
                 * @param item_length The number of characters to add to the `buffer`, or -1 for the               end of `text` (assuming it is `NULL` terminated)
                 */
                buffer_add_utf32(buffer: never, text: number[], item_offset: number, item_length: number): void
                /**
                 * See hb_buffer_add_codepoints().
                 *
                 * Replaces invalid UTF-8 characters with the `buffer` replacement code point,
                 * see hb_buffer_set_replacement_codepoint().
                 * @since 0.9.2
                 * @param buffer An #hb_buffer_t
                 * @param text An array of UTF-8               characters to append.
                 * @param item_offset The offset of the first character to add to the `buffer`.
                 * @param item_length The number of characters to add to the `buffer`, or -1 for the               end of `text` (assuming it is `NULL` terminated).
                 */
                buffer_add_utf8(buffer: never, text: Uint8Array, item_offset: number, item_length: number): void
                /**
                 * Check if allocating memory for the buffer succeeded.
                 * @since 0.9.2
                 * @param buffer An #hb_buffer_t
                 * @returns `true` if `buffer` memory allocation succeeded, `false` otherwise.
                 */
                buffer_allocation_successful(buffer: never): never
                /**
                 * Append (part of) contents of another buffer to this buffer.
                 * @since 1.5.0
                 * @param buffer An #hb_buffer_t
                 * @param source source #hb_buffer_t
                 * @param start start index into source buffer to copy.  Use 0 to copy from start of buffer.
                 * @param end end index into source buffer to copy.  Use `UINT_MAX` (or ((unsigned int) -1)) to copy to end of buffer.
                 */
                buffer_append(buffer: never, source: never, start: number, end: number): void
                /**
                 * Called by a message callback after modifying buffer glyph indices,
                 * to update internal caches.
                 *
                 * If not called from inside a message callback, does nothing.
                 * @since 13.0.0
                 * @param buffer An #hb_buffer_t
                 */
                buffer_changed(buffer: never): void
                /**
                 * Similar to hb_buffer_reset(), but does not clear the Unicode functions and
                 * the replacement code point.
                 * @since 0.9.11
                 * @param buffer An #hb_buffer_t
                 */
                buffer_clear_contents(buffer: never): void
                /**
                 * Creates a new #hb_buffer_t with all properties to defaults.
                 * @since 0.9.2
                 * @returns  A newly allocated #hb_buffer_t with a reference count of 1. The initial reference count should be released with hb_buffer_destroy() when you are done using the #hb_buffer_t. This function never returns `NULL`. If memory cannot be allocated, a special #hb_buffer_t object will be returned on which hb_buffer_allocation_successful() returns `false`.
                 */
                buffer_create(): never
                /**
                 * Creates a new #hb_buffer_t, similar to hb_buffer_create(). The only
                 * difference is that the buffer is configured similarly to `src`.
                 * @since 3.3.0
                 * @param src An #hb_buffer_t
                 * @returns  A newly allocated #hb_buffer_t, similar to hb_buffer_create().
                 */
                buffer_create_similar(src: never): never
                /**
                 * Deserializes glyphs `buffer` from textual representation in the format
                 * produced by hb_buffer_serialize_glyphs().
                 * @since 0.9.7
                 * @param buffer an #hb_buffer_t buffer.
                 * @param buf string to deserialize
                 * @param font font for getting glyph IDs
                 * @param format the #hb_buffer_serialize_format_t of the input `buf`
                 * @returns `true` if the full string was parsed, `false` otherwise., output pointer to the character after last                               consumed one.
                 */
                buffer_deserialize_glyphs(buffer: never, buf: string[], font: never | null, format: never): [never, string]
                /**
                 * Deserializes Unicode `buffer` from textual representation in the format
                 * produced by hb_buffer_serialize_unicode().
                 * @since 2.7.3
                 * @param buffer an #hb_buffer_t buffer.
                 * @param buf string to deserialize
                 * @param format the #hb_buffer_serialize_format_t of the input `buf`
                 * @returns `true` if the full string was parsed, `false` otherwise., output pointer to the character after last                               consumed one.
                 */
                buffer_deserialize_unicode(buffer: never, buf: string[], format: never): [never, string]
                /**
                 * If dottedcircle_glyph is (hb_codepoint_t) -1 then #HB_BUFFER_DIFF_FLAG_DOTTED_CIRCLE_PRESENT
                 * and #HB_BUFFER_DIFF_FLAG_NOTDEF_PRESENT are never returned.  This should be used by most
                 * callers if just comparing two buffers is needed.
                 * @since 1.5.0
                 * @param buffer a buffer.
                 * @param reference other buffer to compare to.
                 * @param dottedcircle_glyph glyph id of U+25CC DOTTED CIRCLE, or (hb_codepoint_t) -1.
                 * @param position_fuzz allowed absolute difference in position values.
                 */
                buffer_diff(buffer: never, reference: never, dottedcircle_glyph: never, position_fuzz: number): never
                /**
                 * Fetches the cluster level of a buffer. The #hb_buffer_cluster_level_t
                 * dictates one aspect of how HarfBuzz will treat non-base characters
                 * during shaping.
                 * @since 0.9.42
                 * @param buffer An #hb_buffer_t
                 * @returns The cluster level of `buffer`
                 */
                buffer_get_cluster_level(buffer: never): never
                /**
                 * Fetches the type of `buffer` contents. Buffers are either empty, contain
                 * characters (before shaping), or contain glyphs (the result of shaping).
                 * @since 0.9.5
                 * @param buffer An #hb_buffer_t
                 * @returns The type of `buffer` contents
                 */
                buffer_get_content_type(buffer: never): never
                /**
                 * See hb_buffer_set_direction()
                 * @since 0.9.2
                 * @param buffer An #hb_buffer_t
                 * @returns The direction of the `buffer`.
                 */
                buffer_get_direction(buffer: never): never
                /**
                 * Fetches an empty #hb_buffer_t.
                 * @since 0.9.2
                 * @returns The empty buffer
                 */
                buffer_get_empty(): never
                /**
                 * Fetches the #hb_buffer_flags_t of `buffer`.
                 * @since 0.9.7
                 * @param buffer An #hb_buffer_t
                 * @returns The `buffer` flags
                 */
                buffer_get_flags(buffer: never): never
                /**
                 * Returns `buffer` glyph information array.  Returned pointer
                 * is valid as long as `buffer` contents are not modified.
                 * @since 0.9.2
                 * @param buffer An #hb_buffer_t
                 * @returns  The `buffer` glyph information array. The value valid as long as buffer has not been modified.
                 */
                buffer_get_glyph_infos(buffer: never): never[]
                /**
                 * Returns `buffer` glyph position array.  Returned pointer
                 * is valid as long as `buffer` contents are not modified.
                 *
                 * If buffer did not have positions before, the positions will be
                 * initialized to zeros, unless this function is called from
                 * within a buffer message callback (see hb_buffer_set_message_func()),
                 * in which case `NULL` is returned.
                 * @since 0.9.2
                 * @param buffer An #hb_buffer_t
                 * @returns  The `buffer` glyph position array. The value valid as long as buffer has not been modified.
                 */
                buffer_get_glyph_positions(buffer: never): never[]
                /**
                 * See hb_buffer_set_invisible_glyph().
                 * @since 2.0.0
                 * @param buffer An #hb_buffer_t
                 * @returns The `buffer` invisible #hb_codepoint_t
                 */
                buffer_get_invisible_glyph(buffer: never): never
                /**
                 * See hb_buffer_set_language().
                 * @since 0.9.2
                 * @param buffer An #hb_buffer_t
                 * @returns  The #hb_language_t of the buffer. Must not be freed by the caller.
                 */
                buffer_get_language(buffer: never): never
                /**
                 * Returns the number of items in the buffer.
                 * @since 0.9.2
                 * @param buffer An #hb_buffer_t
                 * @returns The `buffer` length. The value valid as long as buffer has not been modified.
                 */
                buffer_get_length(buffer: never): number
                /**
                 * See hb_buffer_set_not_found_glyph().
                 * @since 3.1.0
                 * @param buffer An #hb_buffer_t
                 * @returns The `buffer` not-found #hb_codepoint_t
                 */
                buffer_get_not_found_glyph(buffer: never): never
                /**
                 * See hb_buffer_set_not_found_variation_selector_glyph().
                 * @since 10.0.0
                 * @param buffer An #hb_buffer_t
                 * @returns The `buffer` not-found-variation-selector #hb_codepoint_t
                 */
                buffer_get_not_found_variation_selector_glyph(buffer: never): never
                /**
                 * See hb_buffer_set_random_state().
                 * @since 8.4.0
                 * @param buffer An #hb_buffer_t
                 * @returns The `buffer` random state
                 */
                buffer_get_random_state(buffer: never): number
                /**
                 * Fetches the #hb_codepoint_t that replaces invalid entries for a given encoding
                 * when adding text to `buffer`.
                 * @since 0.9.31
                 * @param buffer An #hb_buffer_t
                 * @returns The `buffer` replacement #hb_codepoint_t
                 */
                buffer_get_replacement_codepoint(buffer: never): never
                /**
                 * Fetches the script of `buffer`.
                 * @since 0.9.2
                 * @param buffer An #hb_buffer_t
                 * @returns The #hb_script_t of the `buffer`
                 */
                buffer_get_script(buffer: never): never
                /**
                 * Sets `props` to the #hb_segment_properties_t of `buffer`.
                 * @since 0.9.7
                 * @param buffer An #hb_buffer_t
                 * @returns , The output #hb_segment_properties_t
                 */
                buffer_get_segment_properties(buffer: never): never
                /**
                 * Fetches the Unicode-functions structure of a buffer.
                 * @since 0.9.2
                 * @param buffer An #hb_buffer_t
                 * @returns The Unicode-functions structure
                 */
                buffer_get_unicode_funcs(buffer: never): never
                /**
                 * Sets unset buffer segment properties based on buffer Unicode
                 * contents.  If buffer is not empty, it must have content type
                 * #HB_BUFFER_CONTENT_TYPE_UNICODE.
                 *
                 * If buffer script is not set (ie. is #HB_SCRIPT_INVALID), it
                 * will be set to the Unicode script of the first character in
                 * the buffer that has a script other than #HB_SCRIPT_COMMON,
                 * #HB_SCRIPT_INHERITED, and #HB_SCRIPT_UNKNOWN.
                 *
                 * Next, if buffer direction is not set (ie. is #HB_DIRECTION_INVALID),
                 * it will be set to the natural horizontal direction of the
                 * buffer script as returned by hb_script_get_horizontal_direction().
                 * If hb_script_get_horizontal_direction() returns #HB_DIRECTION_INVALID,
                 * then #HB_DIRECTION_LTR is used.
                 *
                 * Finally, if buffer language is not set (ie. is #HB_LANGUAGE_INVALID),
                 * it will be set to the process's default language as returned by
                 * hb_language_get_default().  This may change in the future by
                 * taking buffer script into consideration when choosing a language.
                 * Note that hb_language_get_default() is NOT threadsafe the first time
                 * it is called.  See documentation for that function for details.
                 * @since 0.9.7
                 * @param buffer An #hb_buffer_t
                 */
                buffer_guess_segment_properties(buffer: never): void
                /**
                 * Returns whether `buffer` has glyph position data.
                 * A buffer gains position data when hb_buffer_get_glyph_positions() is called on it,
                 * and cleared of position data when hb_buffer_clear_contents() is called.
                 * @since 2.7.3
                 * @param buffer an #hb_buffer_t.
                 * @returns `true` if the `buffer` has position array, `false` otherwise.
                 */
                buffer_has_positions(buffer: never): never
                /**
                 * Reorders a glyph buffer to have canonical in-cluster glyph order / position.
                 * The resulting clusters should behave identical to pre-reordering clusters.
                 *
                 * <note>This has nothing to do with Unicode normalization.</note>
                 * @since 0.9.2
                 * @param buffer An #hb_buffer_t
                 */
                buffer_normalize_glyphs(buffer: never): void
                /**
                 * Pre allocates memory for `buffer` to fit at least `size` number of items.
                 * @since 0.9.2
                 * @param buffer An #hb_buffer_t
                 * @param size Number of items to pre allocate.
                 * @returns `true` if `buffer` memory allocation succeeded, `false` otherwise
                 */
                buffer_pre_allocate(buffer: never, size: number): never
                /**
                 * Resets the buffer to its initial status, as if it was just newly created
                 * with hb_buffer_create().
                 * @since 0.9.2
                 * @param buffer An #hb_buffer_t
                 */
                buffer_reset(buffer: never): void
                /**
                 * Reverses buffer contents.
                 * @since 0.9.2
                 * @param buffer An #hb_buffer_t
                 */
                buffer_reverse(buffer: never): void
                /**
                 * Reverses buffer clusters.  That is, the buffer contents are
                 * reversed, then each cluster (consecutive items having the
                 * same cluster number) are reversed again.
                 * @since 0.9.2
                 * @param buffer An #hb_buffer_t
                 */
                buffer_reverse_clusters(buffer: never): void
                /**
                 * Reverses buffer contents between `start` and `end`.
                 * @since 0.9.41
                 * @param buffer An #hb_buffer_t
                 * @param start start index
                 * @param end end index
                 */
                buffer_reverse_range(buffer: never, start: number, end: number): void
                /**
                 * Serializes `buffer` into a textual representation of its content, whether
                 * Unicode codepoints or glyph identifiers and positioning information. This is
                 * useful for showing the contents of the buffer, for example during debugging.
                 * See the documentation of hb_buffer_serialize_unicode() and
                 * hb_buffer_serialize_glyphs() for a description of the output format.
                 * @since 2.7.3
                 * @param buffer an #hb_buffer_t buffer.
                 * @param start the first item in `buffer` to serialize.
                 * @param end the last item in `buffer` to serialize.
                 * @param font the #hb_font_t used to shape this buffer, needed to        read glyph names and extents. If `NULL`, an empty font will be used.
                 * @param format the #hb_buffer_serialize_format_t to use for formatting the output.
                 * @param flags the #hb_buffer_serialize_flags_t that control what glyph properties         to serialize.
                 * @returns The number of serialized items., output string to       write serialized buffer into., if not `NULL`, will be set to the number of bytes written into `buf`.
                 */
                buffer_serialize(buffer: never, start: number, end: number, font: never | null, format: never, flags: never): [number, Uint8Array, number]
                /**
                 * Parses a string into an #hb_buffer_serialize_format_t. Does not check if
                 *  `str` is a valid buffer serialization format, use
                 * hb_buffer_serialize_list_formats() to get the list of supported formats.
                 * @since 0.9.7
                 * @param str a string to parse
                 * @returns The parsed #hb_buffer_serialize_format_t.
                 */
                buffer_serialize_format_from_string(str: Uint8Array): never
                /**
                 * Converts `format` to the string corresponding it, or `NULL` if it is not a valid
                 * #hb_buffer_serialize_format_t.
                 * @since 0.9.7
                 * @param format an #hb_buffer_serialize_format_t to convert.
                 * @returns  A `NULL` terminated string corresponding to `format`. Should not be freed.
                 */
                buffer_serialize_format_to_string(format: never): string
                /**
                 * Serializes `buffer` into a textual representation of its glyph content,
                 * useful for showing the contents of the buffer, for example during debugging.
                 * There are currently two supported serialization formats:
                 *
                 * ## text
                 * A human-readable, plain text format.
                 * The serialized glyphs will look something like:
                 *
                 * |[<!-- language="plain" -->
                 * [uni0651=0@518,0+0|uni0628=0+1897]
                 * ]|
                 *
                 * - The serialized glyphs are delimited with `[` and `]`.
                 * - Glyphs are separated with `|`
                 * - Each glyph starts with glyph name, or glyph index if
                 *   #HB_BUFFER_SERIALIZE_FLAG_NO_GLYPH_NAMES flag is set. Then,
                 *   - If #HB_BUFFER_SERIALIZE_FLAG_NO_CLUSTERS is not set, `=` then #hb_glyph_info_t.cluster.
                 *   - If #HB_BUFFER_SERIALIZE_FLAG_NO_POSITIONS is not set, the #hb_glyph_position_t in the format:
                 *     - If #hb_glyph_position_t.x_offset and #hb_glyph_position_t.y_offset are not both 0, `@x_offset,y_offset`. Then,
                 *     - `+x_advance`, then `,y_advance` if #hb_glyph_position_t.y_advance is not 0. Then,
                 *   - If #HB_BUFFER_SERIALIZE_FLAG_GLYPH_EXTENTS is set, the #hb_glyph_extents_t in the format `<x_bearing,y_bearing,width,height>`
                 *
                 * ## json
                 * A machine-readable, structured format.
                 * The serialized glyphs will look something like:
                 *
                 * |[<!-- language="plain" -->
                 * [{"g":"uni0651","cl":0,"dx":518,"dy":0,"ax":0,"ay":0},
                 *  {"g":"uni0628","cl":0,"dx":0,"dy":0,"ax":1897,"ay":0}]
                 * ]|
                 *
                 * Each glyph is a JSON object, with the following properties:
                 * - `g`: the glyph name or glyph index if
                 *   #HB_BUFFER_SERIALIZE_FLAG_NO_GLYPH_NAMES flag is set.
                 * - `cl`: #hb_glyph_info_t.cluster if
                 *   #HB_BUFFER_SERIALIZE_FLAG_NO_CLUSTERS is not set.
                 * - `dx`,`dy`,`ax`,`ay`: #hb_glyph_position_t.x_offset, #hb_glyph_position_t.y_offset,
                 *    #hb_glyph_position_t.x_advance and #hb_glyph_position_t.y_advance
                 *    respectively, if #HB_BUFFER_SERIALIZE_FLAG_NO_POSITIONS is not set.
                 * - `xb`,`yb`,`w`,`h`: #hb_glyph_extents_t.x_bearing, #hb_glyph_extents_t.y_bearing,
                 *    #hb_glyph_extents_t.width and #hb_glyph_extents_t.height respectively if
                 *    #HB_BUFFER_SERIALIZE_FLAG_GLYPH_EXTENTS is set.
                 * @since 0.9.7
                 * @param buffer an #hb_buffer_t buffer.
                 * @param start the first item in `buffer` to serialize.
                 * @param end the last item in `buffer` to serialize.
                 * @param font the #hb_font_t used to shape this buffer, needed to        read glyph names and extents. If `NULL`, an empty font will be used.
                 * @param format the #hb_buffer_serialize_format_t to use for formatting the output.
                 * @param flags the #hb_buffer_serialize_flags_t that control what glyph properties         to serialize.
                 * @returns The number of serialized items., output string to       write serialized buffer into., if not `NULL`, will be set to the number of bytes written into `buf`.
                 */
                buffer_serialize_glyphs(buffer: never, start: number, end: number, font: never | null, format: never, flags: never): [number, Uint8Array, number]
                /**
                 * Returns a list of supported buffer serialization formats.
                 * @since 0.9.7
                 * @returns  A string array of buffer serialization formats. Should not be freed.
                 */
                buffer_serialize_list_formats(): string[]
                /**
                 * Serializes `buffer` into a textual representation of its content,
                 * when the buffer contains Unicode codepoints (i.e., before shaping). This is
                 * useful for showing the contents of the buffer, for example during debugging.
                 * There are currently two supported serialization formats:
                 *
                 * ## text
                 * A human-readable, plain text format.
                 * The serialized codepoints will look something like:
                 *
                 * |[<!-- language="plain" -->
                 *  <U+0651=0|U+0628=1>
                 * ]|
                 *
                 * - Glyphs are separated with `|`
                 * - Unicode codepoints are expressed as zero-padded four (or more)
                 *   digit hexadecimal numbers preceded by `U+`
                 * - If #HB_BUFFER_SERIALIZE_FLAG_NO_CLUSTERS is not set, the cluster
                 *   will be indicated with a `=` then #hb_glyph_info_t.cluster.
                 *
                 * ## json
                 * A machine-readable, structured format.
                 * The serialized codepoints will be a list of objects with the following
                 * properties:
                 * - `u`: the Unicode codepoint as a decimal integer
                 * - `cl`: #hb_glyph_info_t.cluster if
                 *   #HB_BUFFER_SERIALIZE_FLAG_NO_CLUSTERS is not set.
                 *
                 * For example:
                 *
                 * |[<!-- language="plain" -->
                 * [{u:1617,cl:0},{u:1576,cl:1}]
                 * ]|
                 * @since 2.7.3
                 * @param buffer an #hb_buffer_t buffer.
                 * @param start the first item in `buffer` to serialize.
                 * @param end the last item in `buffer` to serialize.
                 * @param format the #hb_buffer_serialize_format_t to use for formatting the output.
                 * @param flags the #hb_buffer_serialize_flags_t that control what glyph properties         to serialize.
                 * @returns The number of serialized items., output string to       write serialized buffer into., if not `NULL`, will be set to the number of bytes written into `buf`.
                 */
                buffer_serialize_unicode(buffer: never, start: number, end: number, format: never, flags: never): [number, Uint8Array, number]
                /**
                 * Sets the cluster level of a buffer. The #hb_buffer_cluster_level_t
                 * dictates one aspect of how HarfBuzz will treat non-base characters
                 * during shaping.
                 * @since 0.9.42
                 * @param buffer An #hb_buffer_t
                 * @param cluster_level The cluster level to set on the buffer
                 */
                buffer_set_cluster_level(buffer: never, cluster_level: never): void
                /**
                 * Sets the type of `buffer` contents. Buffers are either empty, contain
                 * characters (before shaping), or contain glyphs (the result of shaping).
                 *
                 * You rarely need to call this function, since a number of other
                 * functions transition the content type for you. Namely:
                 *
                 * - A newly created buffer starts with content type
                 *   %HB_BUFFER_CONTENT_TYPE_INVALID. Calling hb_buffer_reset(),
                 *   hb_buffer_clear_contents(), as well as calling hb_buffer_set_length()
                 *   with an argument of zero all set the buffer content type to invalid
                 *   as well.
                 *
                 * - Calling hb_buffer_add_utf8(), hb_buffer_add_utf16(),
                 *   hb_buffer_add_utf32(), hb_buffer_add_codepoints() and
                 *   hb_buffer_add_latin1() expect that buffer is either empty and
                 *   have a content type of invalid, or that buffer content type is
                 *   %HB_BUFFER_CONTENT_TYPE_UNICODE, and they also set the content
                 *   type to Unicode if they added anything to an empty buffer.
                 *
                 * - Finally hb_shape() and hb_shape_full() expect that the buffer
                 *   is either empty and have content type of invalid, or that buffer
                 *   content type is %HB_BUFFER_CONTENT_TYPE_UNICODE, and upon
                 *   success they set the buffer content type to
                 *   %HB_BUFFER_CONTENT_TYPE_GLYPHS.
                 *
                 * The above transitions are designed such that one can use a buffer
                 * in a loop of "reset : add-text : shape" without needing to ever
                 * modify the content type manually.
                 * @since 0.9.5
                 * @param buffer An #hb_buffer_t
                 * @param content_type The type of buffer contents to set
                 */
                buffer_set_content_type(buffer: never, content_type: never): void
                /**
                 * Set the text flow direction of the buffer. No shaping can happen without
                 * setting `buffer` direction, and it controls the visual direction for the
                 * output glyphs; for RTL direction the glyphs will be reversed. Many layout
                 * features depend on the proper setting of the direction, for example,
                 * reversing RTL text before shaping, then shaping with LTR direction is not
                 * the same as keeping the text in logical order and shaping with RTL
                 * direction.
                 * @since 0.9.2
                 * @param buffer An #hb_buffer_t
                 * @param direction the #hb_direction_t of the `buffer`
                 */
                buffer_set_direction(buffer: never, direction: never): void
                /**
                 * Sets `buffer` flags to `flags`. See #hb_buffer_flags_t.
                 * @since 0.9.7
                 * @param buffer An #hb_buffer_t
                 * @param flags The buffer flags to set
                 */
                buffer_set_flags(buffer: never, flags: never): void
                /**
                 * Sets the #hb_codepoint_t that replaces invisible characters in
                 * the shaping result.  If set to zero (default), the glyph for the
                 * U+0020 SPACE character is used.  Otherwise, this value is used
                 * verbatim.
                 * @since 2.0.0
                 * @param buffer An #hb_buffer_t
                 * @param invisible the invisible #hb_codepoint_t
                 */
                buffer_set_invisible_glyph(buffer: never, invisible: never): void
                /**
                 * Sets the language of `buffer` to `language`.
                 *
                 * Languages are crucial for selecting which OpenType feature to apply to the
                 * buffer which can result in applying language-specific behaviour. Languages
                 * are orthogonal to the scripts, and though they are related, they are
                 * different concepts and should not be confused with each other.
                 *
                 * Use hb_language_from_string() to convert from BCP 47 language tags to
                 * #hb_language_t.
                 * @since 0.9.2
                 * @param buffer An #hb_buffer_t
                 * @param language An hb_language_t to set
                 */
                buffer_set_language(buffer: never, language: never): void
                /**
                 * Similar to hb_buffer_pre_allocate(), but clears any new items added at the
                 * end.
                 * @since 0.9.2
                 * @param buffer An #hb_buffer_t
                 * @param length The new length of `buffer`
                 * @returns `true` if `buffer` memory allocation succeeded, `false` otherwise.
                 */
                buffer_set_length(buffer: never, length: number): never
                /**
                 * Sets the implementation function for #hb_buffer_message_func_t.
                 * @since 1.1.3
                 * @param buffer An #hb_buffer_t
                 * @param func Callback function
                 */
                buffer_set_message_func(buffer: never, func: never): void
                /**
                 * Sets the #hb_codepoint_t that replaces characters not found in
                 * the font during shaping.
                 *
                 * The not-found glyph defaults to zero, sometimes known as the
                 * ".notdef" glyph.  This API allows for differentiating the two.
                 * @since 3.1.0
                 * @param buffer An #hb_buffer_t
                 * @param not_found the not-found #hb_codepoint_t
                 */
                buffer_set_not_found_glyph(buffer: never, not_found: never): void
                /**
                 * Sets the #hb_codepoint_t that replaces variation-selector characters not resolved
                 * in the font during shaping.
                 *
                 * The not-found-variation-selector glyph defaults to #HB_CODEPOINT_INVALID,
                 * in which case an unresolved variation-selector will be removed from the glyph
                 * string during shaping. This API allows for changing that and retaining a glyph,
                 * such that the situation can be detected by the client and handled accordingly
                 * (e.g. by using a different font).
                 * @since 10.0.0
                 * @param buffer An #hb_buffer_t
                 * @param not_found_variation_selector the not-found-variation-selector #hb_codepoint_t
                 */
                buffer_set_not_found_variation_selector_glyph(buffer: never, not_found_variation_selector: never): void
                /**
                 * Sets the random state of the buffer. The state changes
                 * every time a glyph uses randomness (eg. the `rand`
                 * OpenType feature). This function together with
                 * hb_buffer_get_random_state() allow for transferring
                 * the current random state to a subsequent buffer, to
                 * get better randomness distribution.
                 *
                 * Defaults to 1 and when buffer contents are cleared.
                 * A value of 0 disables randomness during shaping.
                 * @since 8.4.0
                 * @param buffer An #hb_buffer_t
                 * @param state the new random state
                 */
                buffer_set_random_state(buffer: never, state: number): void
                /**
                 * Sets the #hb_codepoint_t that replaces invalid entries for a given encoding
                 * when adding text to `buffer`.
                 *
                 * Default is #HB_BUFFER_REPLACEMENT_CODEPOINT_DEFAULT.
                 * @since 0.9.31
                 * @param buffer An #hb_buffer_t
                 * @param replacement the replacement #hb_codepoint_t
                 */
                buffer_set_replacement_codepoint(buffer: never, replacement: never): void
                /**
                 * Sets the script of `buffer` to `script`.
                 *
                 * Script is crucial for choosing the proper shaping behaviour for scripts that
                 * require it (e.g. Arabic) and the which OpenType features defined in the font
                 * to be applied.
                 *
                 * You can pass one of the predefined #hb_script_t values, or use
                 * hb_script_from_string() or hb_script_from_iso15924_tag() to get the
                 * corresponding script from an ISO 15924 script tag.
                 * @since 0.9.2
                 * @param buffer An #hb_buffer_t
                 * @param script An #hb_script_t to set.
                 */
                buffer_set_script(buffer: never, script: never): void
                /**
                 * Sets the segment properties of the buffer, a shortcut for calling
                 * hb_buffer_set_direction(), hb_buffer_set_script() and
                 * hb_buffer_set_language() individually.
                 * @since 0.9.7
                 * @param buffer An #hb_buffer_t
                 * @param props An #hb_segment_properties_t to use
                 */
                buffer_set_segment_properties(buffer: never, props: never): void
                /**
                 * Sets the Unicode-functions structure of a buffer to
                 *  `unicode_funcs`.
                 * @since 0.9.2
                 * @param buffer An #hb_buffer_t
                 * @param unicode_funcs The Unicode-functions structure
                 */
                buffer_set_unicode_funcs(buffer: never, unicode_funcs: never): void
                /**
                 * Allocates `nmemb` elements of `size` bytes each, initialized to zero,
                 * using the allocator set at compile-time. Typically just calloc().
                 * @since 11.0.0
                 * @param nmemb The number of elements to allocate.
                 * @param size The size of each element.
                 * @returns A pointer to the allocated memory.
                 */
                calloc(nmemb: number, size: number): never | null
                /**
                 * Fetches the alpha channel of the given `color`.
                 * @since 2.1.0
                 * @param color an #hb_color_t we are interested in its channels.
                 * @returns Alpha channel value
                 */
                color_get_alpha(color: never): number
                /**
                 * Fetches the blue channel of the given `color`.
                 * @since 2.1.0
                 * @param color an #hb_color_t we are interested in its channels.
                 * @returns Blue channel value
                 */
                color_get_blue(color: never): number
                /**
                 * Fetches the green channel of the given `color`.
                 * @since 2.1.0
                 * @param color an #hb_color_t we are interested in its channels.
                 * @returns Green channel value
                 */
                color_get_green(color: never): number
                /**
                 * Fetches the red channel of the given `color`.
                 * @since 2.1.0
                 * @param color an #hb_color_t we are interested in its channels.
                 * @returns Red channel value
                 */
                color_get_red(color: never): number
                /**
                 * Fetches a list of color stops from the given color line object.
                 *
                 * Note that due to variations being applied, the returned color stops
                 * may be out of order. It is the callers responsibility to ensure that
                 * color stops are sorted by their offset before they are used.
                 * @since 7.0.0
                 * @param color_line a #hb_color_line_t object
                 * @param start the index of the first color stop to return
                 * @returns the total number of color stops in `color_line`, Array of #hb_color_stop_t to populate
                 */
                color_line_get_color_stops(color_line: never, start: number): [number, never[]]
                /**
                 * Fetches the extend mode of the color line object.
                 * @since 7.0.0
                 * @param color_line a #hb_color_line_t object
                 * @returns the extend mode of `color_line`
                 */
                color_line_get_extend(color_line: never): never
                /**
                 * Converts a string to an #hb_direction_t.
                 *
                 * Matching is loose and applies only to the first letter. For
                 * examples, "LTR" and "left-to-right" will both return #HB_DIRECTION_LTR.
                 *
                 * Unmatched strings will return #HB_DIRECTION_INVALID.
                 * @since 0.9.2
                 * @param str String to convert
                 * @returns The #hb_direction_t matching `str`
                 */
                direction_from_string(str: Uint8Array): never
                /**
                 * Converts an #hb_direction_t to a string.
                 * @since 0.9.2
                 * @param direction The #hb_direction_t to convert
                 * @returns The string corresponding to `direction`
                 */
                direction_to_string(direction: never): string
                /**
                 * Emits a circle approximated by four cubic Bezier curves.  If
                 *  `stroke_width` is a finite positive value, the circle is
                 * rendered as an outlined ring of that thickness centered on
                 * the nominal radius; if `stroke_width` is `NaN`, the circle is
                 * rendered as a filled disc.
                 * @since 14.2.0
                 * @param dfuncs draw functions
                 * @param draw_data associated draw data passed by the caller
                 * @param st current draw state
                 * @param cx center X coordinate
                 * @param cy center Y coordinate
                 * @param r radius
                 * @param stroke_width stroke width, or `NaN` for a filled disc
                 */
                draw_circle(dfuncs: never, draw_data: never | null, st: never, cx: number, cy: number, r: number, stroke_width: number): void
                /**
                 * Perform a "close-path" draw operation.
                 * @since 4.0.0
                 * @param dfuncs draw functions
                 * @param draw_data associated draw data passed by the caller
                 * @param st current draw state
                 */
                draw_close_path(dfuncs: never, draw_data: never | null, st: never): void
                /**
                 * Perform a "cubic-to" draw operation.
                 * @since 4.0.0
                 * @param dfuncs draw functions
                 * @param draw_data associated draw data passed by the caller
                 * @param st current draw state
                 * @param control1_x X component of first control point
                 * @param control1_y Y component of first control point
                 * @param control2_x X component of second control point
                 * @param control2_y Y component of second control point
                 * @param to_x X component of target point
                 * @param to_y Y component of target point
                 */
                draw_cubic_to(dfuncs: never, draw_data: never | null, st: never, control1_x: number, control1_y: number, control2_x: number, control2_y: number, to_x: number, to_y: number): void
                /**
                 * Creates a new draw callbacks object.
                 * @since 4.0.0
                 * @returns  A newly allocated #hb_draw_funcs_t with a reference count of 1. The initial reference count should be released with hb_draw_funcs_destroy when you are done using the #hb_draw_funcs_t. This function never returns `NULL`. If memory cannot be allocated, a special singleton #hb_draw_funcs_t object will be returned.
                 */
                draw_funcs_create(): never
                /**
                 * Fetches the singleton empty draw-functions structure.
                 * @since 7.0.0
                 * @returns The empty draw-functions structure
                 */
                draw_funcs_get_empty(): never
                /**
                 * Checks whether `dfuncs` is immutable.
                 * @since 4.0.0
                 * @param dfuncs draw functions
                 * @returns `true` if `dfuncs` is immutable, `false` otherwise
                 */
                draw_funcs_is_immutable(dfuncs: never): never
                /**
                 * Makes `dfuncs` object immutable.
                 * @since 4.0.0
                 * @param dfuncs draw functions
                 */
                draw_funcs_make_immutable(dfuncs: never): void
                /**
                 * Sets close-path callback to the draw functions object.
                 * @since 4.0.0
                 * @param dfuncs draw functions object
                 * @param func close-path callback
                 */
                draw_funcs_set_close_path_func(dfuncs: never, func: never): void
                /**
                 * Sets cubic-to callback to the draw functions object.
                 * @since 4.0.0
                 * @param dfuncs draw functions
                 * @param func cubic-to callback
                 */
                draw_funcs_set_cubic_to_func(dfuncs: never, func: never): void
                /**
                 * Sets line-to callback to the draw functions object.
                 * @since 4.0.0
                 * @param dfuncs draw functions object
                 * @param func line-to callback
                 */
                draw_funcs_set_line_to_func(dfuncs: never, func: never): void
                /**
                 * Sets move-to callback to the draw functions object.
                 * @since 4.0.0
                 * @param dfuncs draw functions object
                 * @param func move-to callback
                 */
                draw_funcs_set_move_to_func(dfuncs: never, func: never): void
                /**
                 * Sets quadratic-to callback to the draw functions object.
                 * @since 4.0.0
                 * @param dfuncs draw functions object
                 * @param func quadratic-to callback
                 */
                draw_funcs_set_quadratic_to_func(dfuncs: never, func: never): void
                /**
                 * Emits a tapered line segment as a filled trapezoid.  `w0` and
                 *  `w1` are the full stroke widths at the start and end points
                 * respectively; they may differ for a tapered stroke or match
                 * for a uniform one.  Pass `NaN` for `w1` to use `w0` (uniform
                 * stroke) without repeating the value.
                 *
                 * With #HB_DRAW_LINE_CAP_SQUARE each endpoint is extended along
                 * the line direction by half its local stroke width, so four
                 * `hb_draw_line()` calls form a closed rectangle without gaps
                 * at the corners.
                 * @since 14.2.0
                 * @param dfuncs draw functions
                 * @param draw_data associated draw data passed by the caller
                 * @param st current draw state
                 * @param x0 start X coordinate
                 * @param y0 start Y coordinate
                 * @param w0 stroke width at the start
                 * @param x1 end X coordinate
                 * @param y1 end Y coordinate
                 * @param w1 stroke width at the end
                 * @param cap end-cap shape (butt or square)
                 */
                draw_line(dfuncs: never, draw_data: never | null, st: never, x0: number, y0: number, w0: number, x1: number, y1: number, w1: number, cap: never): void
                /**
                 * Perform a "line-to" draw operation.
                 * @since 4.0.0
                 * @param dfuncs draw functions
                 * @param draw_data associated draw data passed by the caller
                 * @param st current draw state
                 * @param to_x X component of target point
                 * @param to_y Y component of target point
                 */
                draw_line_to(dfuncs: never, draw_data: never | null, st: never, to_x: number, to_y: number): void
                /**
                 * Perform a "move-to" draw operation.
                 * @since 4.0.0
                 * @param dfuncs draw functions
                 * @param draw_data associated draw data passed by the caller
                 * @param st current draw state
                 * @param to_x X component of target point
                 * @param to_y Y component of target point
                 */
                draw_move_to(dfuncs: never, draw_data: never | null, st: never, to_x: number, to_y: number): void
                /**
                 * Perform a "quadratic-to" draw operation.
                 * @since 4.0.0
                 * @param dfuncs draw functions
                 * @param draw_data associated draw data passed by the caller
                 * @param st current draw state
                 * @param control_x X component of control point
                 * @param control_y Y component of control point
                 * @param to_x X component of target point
                 * @param to_y Y component of target point
                 */
                draw_quadratic_to(dfuncs: never, draw_data: never | null, st: never, control_x: number, control_y: number, to_x: number, to_y: number): void
                /**
                 * Emits an axis-aligned rectangle.  If `stroke_width` is a finite
                 * positive value, the rectangle is rendered as an outlined ring
                 * of that thickness centered on the edges; if `stroke_width` is
                 * `NaN`, the rectangle is rendered filled.
                 *
                 * Note: stroked rectangles produce a bounding box covering the
                 * full outer rectangle, so if the pen is a GPU fragment-shader
                 * backend, the shader runs for every interior pixel even though
                 * only the outline contributes coverage.  For very thin
                 * outlines where the interior is much larger than the stroke,
                 * emitting four hb_draw_line() segments (one per edge) is
                 * considerably cheaper per frame.
                 * @since 14.2.0
                 * @param dfuncs draw functions
                 * @param draw_data associated draw data passed by the caller
                 * @param st current draw state
                 * @param x top-left X coordinate
                 * @param y top-left Y coordinate
                 * @param w width (may be negative)
                 * @param h height (may be negative)
                 * @param stroke_width stroke width, or `NaN` for a filled rectangle
                 */
                draw_rectangle(dfuncs: never, draw_data: never | null, st: never, x: number, y: number, w: number, h: number, stroke_width: number): void
                /**
                 * Add table for `tag` with data provided by `blob` to the face.  `face` must
                 * be created using hb_face_builder_create().
                 * @since 1.9.0
                 * @param face A face object created with hb_face_builder_create()
                 * @param tag The #hb_tag_t of the table to add
                 * @param blob The blob containing the table data to add
                 */
                face_builder_add_table(face: never, tag: never, blob: never): never
                /**
                 * Creates a #hb_face_t that can be used with hb_face_builder_add_table().
                 * After tables are added to the face, it can be compiled to a binary
                 * font file by calling hb_face_reference_blob().
                 * @since 1.9.0
                 * @returns New face.
                 */
                face_builder_create(): never
                /**
                 * Set the ordering of tables for serialization. Any tables not
                 * specified in the tags list will be ordered after the tables in
                 * tags, ordered by the default sort ordering.
                 * @since 5.3.0
                 * @param face A face object created with hb_face_builder_create()
                 * @param tags ordered list of table tags terminated by   %HB_TAG_NONE
                 */
                face_builder_sort_tables(face: never, tags: never[]): void
                /**
                 * Collects the mapping from Unicode characters to nominal glyphs of the `face`,
                 * and optionally all of the Unicode characters covered by `face`.
                 * @since 7.0.0
                 * @param face A face object
                 * @returns , The map to add Unicode-to-glyph mapping to, The set to add Unicode characters to, or `NULL`
                 */
                face_collect_nominal_glyph_mapping(face: never): [never, never | null]
                /**
                 * Collects all of the Unicode characters covered by `face` and adds
                 * them to the #hb_set_t set `out`.
                 * @since 1.9.0
                 * @param face A face object
                 * @returns , The set to add Unicode characters to
                 */
                face_collect_unicodes(face: never): never
                /**
                 * Collects all Unicode "Variation Selector" characters covered by `face` and adds
                 * them to the #hb_set_t set `out`.
                 * @since 1.9.0
                 * @param face A face object
                 * @returns , The set to add Variation Selector characters to
                 */
                face_collect_variation_selectors(face: never): never
                /**
                 * Collects all Unicode characters for `variation_selector` covered by `face` and adds
                 * them to the #hb_set_t set `out`.
                 * @since 1.9.0
                 * @param face A face object
                 * @param variation_selector The Variation Selector to query
                 * @returns , The set to add Unicode characters to
                 */
                face_collect_variation_unicodes(face: never, variation_selector: never): never
                /**
                 * Fetches the number of faces in a blob.
                 * @since 1.7.7
                 * @param blob a blob.
                 * @returns Number of faces in `blob`
                 */
                face_count(blob: never): number
                /**
                 * Constructs a new face object from the specified blob and
                 * a face index into that blob.
                 *
                 * The face index is used for blobs of file formats such as TTC and
                 * DFont that can contain more than one face.  Face indices within
                 * such collections are zero-based.
                 *
                 * <note>Note: If the blob font format is not a collection, `index`
                 * is ignored.  Otherwise, only the lower 16-bits of `index` are used.
                 * The unmodified `index` can be accessed via hb_face_get_index().</note>
                 *
                 * <note>Note: The high 16-bits of `index`, if non-zero, are used by
                 * hb_font_create() to load named-instances in variable fonts.  See
                 * hb_font_create() for details.</note>
                 * @since 0.9.2
                 * @param blob #hb_blob_t to work upon
                 * @param index The index of the face within `blob`
                 * @returns The new face object
                 */
                face_create(blob: never, index: number): never
                /**
                 * Variant of hb_face_create(), built for those cases where it is more
                 * convenient to provide data for individual tables instead of the whole font
                 * data. With the caveat that hb_face_get_table_tags() would not work
                 * with faces created this way. You can address that by calling the
                 * hb_face_set_get_table_tags_func() function and setting the appropriate callback.
                 *
                 * Creates a new face object from the specified `user_data` and `reference_table_func`,
                 * with the `destroy` callback.
                 * @since 0.9.2
                 * @param reference_table_func Table-referencing function
                 * @returns The new face object
                 */
                face_create_for_tables(reference_table_func: never): never
                /**
                 * A thin wrapper around hb_blob_create_from_file_or_fail()
                 * followed by hb_face_create_or_fail().
                 * @since 10.1.0
                 * @param file_name A font filename
                 * @param index The index of the face within the file
                 * @returns The new face object, or `NULL` if no face is found at the specified index or the file cannot be read.
                 */
                face_create_from_file_or_fail(file_name: string, index: number): never
                /**
                 * A thin wrapper around the face loader functions registered with HarfBuzz.
                 * If `loader_name` is `NULL` or the empty string, the first available loader
                 * is used.
                 *
                 * For example, the FreeType ("ft") loader might be able to load
                 * WOFF and WOFF2 files if FreeType is built with those features,
                 * whereas the OpenType ("ot") loader will not.
                 * @since 11.0.0
                 * @param file_name A font filename
                 * @param index The index of the face within the file
                 * @param loader_name The name of the loader to use, or `NULL`
                 * @returns The new face object, or `NULL` if the file cannot be read or the loader fails to load the face.
                 */
                face_create_from_file_or_fail_using(file_name: string, index: number, loader_name: string | null): never
                /**
                 * Like hb_face_create(), but returns `NULL` if the blob data
                 * contains no usable font face at the specified index.
                 * @since 10.1.0
                 * @param blob #hb_blob_t to work upon
                 * @param index The index of the face within `blob`
                 * @returns The new face object, or `NULL` if no face is found at the specified index.
                 */
                face_create_or_fail(blob: never, index: number): never
                /**
                 * A thin wrapper around the face loader functions registered with HarfBuzz.
                 * If `loader_name` is `NULL` or the empty string, the first available loader
                 * is used.
                 *
                 * For example, the FreeType ("ft") loader might be able to load
                 * WOFF and WOFF2 files if FreeType is built with those features,
                 * whereas the OpenType ("ot") loader will not.
                 * @since 11.0.0
                 * @param blob #hb_blob_t to work upon
                 * @param index The index of the face within `blob`
                 * @param loader_name The name of the loader to use, or `NULL`
                 * @returns The new face object, or `NULL` if the loader fails to load the face.
                 */
                face_create_or_fail_using(blob: never, index: number, loader_name: string | null): never
                /**
                 * Fetches the singleton empty face object.
                 * @since 0.9.2
                 * @returns The empty face object
                 */
                face_get_empty(): never
                /**
                 * Fetches the glyph-count value of the specified face object.
                 * @since 0.9.7
                 * @param face A face object
                 * @returns The glyph-count value of `face`
                 */
                face_get_glyph_count(face: never): number
                /**
                 * Fetches the face-index corresponding to the given face.
                 *
                 * <note>Note: face indices within a collection are zero-based.</note>
                 * @since 0.9.2
                 * @param face A face object
                 * @returns The index of `face`.
                 */
                face_get_index(face: never): number
                /**
                 * Fetches a list of all table tags for a face, if possible. The list returned will
                 * begin at the offset provided
                 * @since 1.6.0
                 * @param face A face object
                 * @param start_offset The index of first table tag to retrieve
                 * @returns Total number of tables, or zero if it is not possible to list, The array of table tags found
                 */
                face_get_table_tags(face: never, start_offset: number): [number, never[] | null]
                /**
                 * Fetches the units-per-em (UPEM) value of the specified face object.
                 *
                 * Typical UPEM values for fonts are 1000, or 2048, but any value
                 * in between 16 and 16,384 is allowed for OpenType fonts.
                 * @since 0.9.2
                 * @param face A face object
                 * @returns The upem value of `face`
                 */
                face_get_upem(face: never): number
                /**
                 * Tests whether the given face object is immutable.
                 * @since 0.9.2
                 * @param face A face object
                 * @returns `true` is `face` is immutable, `false` otherwise
                 */
                face_is_immutable(face: never): never
                /**
                 * Retrieves the list of face loaders supported by HarfBuzz.
                 * @since 11.0.0
                 * @returns a    `NULL`-terminated array of supported face loaders    constant strings. The returned array is owned by HarfBuzz    and should not be modified or freed.
                 */
                face_list_loaders(): string[]
                /**
                 * Makes the given face object immutable.
                 * @since 0.9.2
                 * @param face A face object
                 */
                face_make_immutable(face: never): void
                /**
                 * Fetches a pointer to the binary blob that contains the specified face.
                 * If referencing the face data is not possible, this function creates a blob
                 * out of individual table blobs if hb_face_get_table_tags() works with this
                 * face, otherwise it returns an empty blob.
                 * @since 0.9.2
                 * @param face A face object
                 * @returns A pointer to the blob for `face`
                 */
                face_reference_blob(face: never): never
                /**
                 * Fetches a reference to the specified table within
                 * the specified face. Returns an empty blob if referencing table data is not
                 * possible.
                 * @since 0.9.2
                 * @param face A face object
                 * @param tag The #hb_tag_t of the table to query
                 * @returns A pointer to the `tag` table within `face`
                 */
                face_reference_table(face: never, tag: never): never
                /**
                 * Sets the table-tag-fetching function for the specified face object.
                 * @since 10.0.0
                 * @param face A face object
                 * @param func The table-tag-fetching function
                 */
                face_set_get_table_tags_func(face: never, func: never): void
                /**
                 * Sets the glyph count for a face object to the specified value.
                 *
                 * This API is used in rare circumstances.
                 * @since 0.9.7
                 * @param face A face object
                 * @param glyph_count The glyph-count value to assign
                 */
                face_set_glyph_count(face: never, glyph_count: number): void
                /**
                 * Assigns the specified face-index to `face`. Fails if the
                 * face is immutable.
                 *
                 * <note>Note: changing the index has no effect on the face itself
                 * This only changes the value returned by hb_face_get_index().</note>
                 * @since 0.9.2
                 * @param face A face object
                 * @param index The index to assign
                 */
                face_set_index(face: never, index: number): void
                /**
                 * Sets the units-per-em (upem) for a face object to the specified value.
                 *
                 * This API is used in rare circumstances.
                 * @since 0.9.2
                 * @param face A face object
                 * @param upem The units-per-em value to assign
                 */
                face_set_upem(face: never, upem: number): void
                /**
                 * Parses a string into a #hb_feature_t.
                 *
                 * The format for specifying feature strings follows. All valid CSS
                 * font-feature-settings values other than 'normal' and the global values are
                 * also accepted, though not documented below. CSS string escapes are not
                 * supported.
                 *
                 * The range indices refer to the positions between Unicode characters. The
                 * position before the first character is always 0.
                 *
                 * The format is Python-esque.  Here is how it all works:
                 *
                 * <informaltable pgwide='1' align='left' frame='none'>
                 * <tgroup cols='5'>
                 * <thead>
                 * <row><entry>Syntax</entry>    <entry>Value</entry> <entry>Start</entry> <entry>End</entry></row>
                 * </thead>
                 * <tbody>
                 * <row><entry>Setting value:</entry></row>
                 * <row><entry>kern</entry>      <entry>1</entry>     <entry>0</entry>      <entry>∞</entry>   <entry>Turn feature on</entry></row>
                 * <row><entry>+kern</entry>     <entry>1</entry>     <entry>0</entry>      <entry>∞</entry>   <entry>Turn feature on</entry></row>
                 * <row><entry>-kern</entry>     <entry>0</entry>     <entry>0</entry>      <entry>∞</entry>   <entry>Turn feature off</entry></row>
                 * <row><entry>kern=0</entry>    <entry>0</entry>     <entry>0</entry>      <entry>∞</entry>   <entry>Turn feature off</entry></row>
                 * <row><entry>kern=1</entry>    <entry>1</entry>     <entry>0</entry>      <entry>∞</entry>   <entry>Turn feature on</entry></row>
                 * <row><entry>aalt=2</entry>    <entry>2</entry>     <entry>0</entry>      <entry>∞</entry>   <entry>Choose 2nd alternate</entry></row>
                 * <row><entry>Setting index:</entry></row>
                 * <row><entry>kern[]</entry>    <entry>1</entry>     <entry>0</entry>      <entry>∞</entry>   <entry>Turn feature on</entry></row>
                 * <row><entry>kern[:]</entry>   <entry>1</entry>     <entry>0</entry>      <entry>∞</entry>   <entry>Turn feature on</entry></row>
                 * <row><entry>kern[5:]</entry>  <entry>1</entry>     <entry>5</entry>      <entry>∞</entry>   <entry>Turn feature on, partial</entry></row>
                 * <row><entry>kern[:5]</entry>  <entry>1</entry>     <entry>0</entry>      <entry>5</entry>   <entry>Turn feature on, partial</entry></row>
                 * <row><entry>kern[3:5]</entry> <entry>1</entry>     <entry>3</entry>      <entry>5</entry>   <entry>Turn feature on, range</entry></row>
                 * <row><entry>kern[3]</entry>   <entry>1</entry>     <entry>3</entry>      <entry>3+1</entry> <entry>Turn feature on, single char</entry></row>
                 * <row><entry>Mixing it all:</entry></row>
                 * <row><entry>aalt[3:5]=2</entry> <entry>2</entry>   <entry>3</entry>      <entry>5</entry>   <entry>Turn 2nd alternate on for range</entry></row>
                 * </tbody>
                 * </tgroup>
                 * </informaltable>
                 * @since 0.9.5
                 * @param str a string to parse
                 * @returns `true` if `str` is successfully parsed, `false` otherwise, the #hb_feature_t to initialize with the parsed values
                 */
                feature_from_string(str: Uint8Array): [never, never]
                /**
                 * Converts a #hb_feature_t into a `NULL`-terminated string in the format
                 * understood by hb_feature_from_string(). The client in responsible for
                 * allocating big enough size for `buf`, 128 bytes is more than enough.
                 *
                 * Note that the feature value will be omitted if it is '1', but the
                 * string won't include any whitespace.
                 * @since 0.9.5
                 * @param feature an #hb_feature_t to convert
                 * @returns , output string
                 */
                feature_to_string(feature: never): string[]
                /**
                 * Adds the origin coordinates to an (X,Y) point coordinate, in
                 * the specified glyph ID in the specified font.
                 *
                 * Calls the appropriate direction-specific variant (horizontal
                 * or vertical) depending on the value of `direction`.
                 * @since 0.9.2
                 * @param font #hb_font_t to work upon
                 * @param glyph The glyph ID to query
                 * @param direction The direction of the text segment
                 * @returns , Input = The original X coordinate     Output = The X coordinate plus the X-coordinate of the origin, Input = The original Y coordinate     Output = The Y coordinate plus the Y-coordinate of the origin
                 */
                font_add_glyph_origin_for_direction(font: never, glyph: never, direction: never, x: never, y: never): [never, never]
                /**
                 * Notifies the `font` that underlying font data has changed.
                 * This has the effect of increasing the serial as returned
                 * by hb_font_get_serial(), which invalidates internal caches.
                 * @since 4.4.0
                 * @param font #hb_font_t to work upon
                 */
                font_changed(font: never): void
                /**
                 * Constructs a new font object from the specified face.
                 *
                 * <note>Note: If `face`'s index value (as passed to hb_face_create()
                 * has non-zero top 16-bits, those bits minus one are passed to
                 * hb_font_set_var_named_instance(), effectively loading a named-instance
                 * of a variable font, instead of the default-instance.  This allows
                 * specifying which named-instance to load by default when creating the
                 * face.</note>
                 * @since 0.9.2
                 * @param face a face.
                 * @returns The new font object
                 */
                font_create(face: never): never
                /**
                 * Constructs a sub-font font object from the specified `parent` font,
                 * replicating the parent's properties.
                 * @since 0.9.2
                 * @param parent The parent font object
                 * @returns The new sub-font font object
                 */
                font_create_sub_font(parent: never): never
                /**
                 * Draws the outline that corresponds to a glyph in the specified `font`.
                 *
                 * This is an older name for hb_font_draw_glyph_or_fail(), with no
                 * return value.
                 *
                 * The outline is returned by way of calls to the callbacks of the `dfuncs`
                 * objects, with `draw_data` passed to them.
                 * @since 7.0.0
                 * @param font #hb_font_t to work upon
                 * @param glyph The glyph ID
                 * @param dfuncs #hb_draw_funcs_t to draw to
                 * @param draw_data User data to pass to draw callbacks
                 */
                font_draw_glyph(font: never, glyph: never, dfuncs: never, draw_data: never | null): void
                /**
                 * Draws the outline that corresponds to a glyph in the specified `font`.
                 *
                 * This is a newer name for hb_font_draw_glyph(), that returns `false`
                 * if the font has no outlines for the glyph.
                 *
                 * The outline is returned by way of calls to the callbacks of the `dfuncs`
                 * objects, with `draw_data` passed to them.
                 * @since 11.2.0
                 * @param font #hb_font_t to work upon
                 * @param glyph The glyph ID
                 * @param dfuncs #hb_draw_funcs_t to draw to
                 * @param draw_data User data to pass to draw callbacks
                 * @returns `true` if glyph was drawn, `false` otherwise
                 */
                font_draw_glyph_or_fail(font: never, glyph: never, dfuncs: never, draw_data: never | null): never
                /**
                 * Creates a new #hb_font_funcs_t structure of font functions.
                 * @since 0.9.2
                 * @returns The font-functions structure
                 */
                font_funcs_create(): never
                /**
                 * Fetches an empty font-functions structure.
                 * @since 0.9.2
                 * @returns The font-functions structure
                 */
                font_funcs_get_empty(): never
                /**
                 * Tests whether a font-functions structure is immutable.
                 * @since 0.9.2
                 * @param ffuncs The font-functions structure
                 * @returns `true` if `ffuncs` is immutable, `false` otherwise
                 */
                font_funcs_is_immutable(ffuncs: never): never
                /**
                 * Makes a font-functions structure immutable.
                 * @since 0.9.2
                 * @param ffuncs The font-functions structure
                 */
                font_funcs_make_immutable(ffuncs: never): void
                /**
                 * Sets the implementation function for #hb_font_draw_glyph_func_t.
                 * @since 7.0.0
                 * @deprecated since 11.2.0 Use hb_font_funcs_set_draw_glyph_or_fail_func instead.
                 * @param ffuncs A font-function structure
                 * @param func The callback function to assign
                 */
                font_funcs_set_draw_glyph_func(ffuncs: never, func: never): void
                /**
                 * Sets the implementation function for #hb_font_draw_glyph_or_fail_func_t.
                 * @since 11.2.0
                 * @param ffuncs A font-function structure
                 * @param func The callback function to assign
                 */
                font_funcs_set_draw_glyph_or_fail_func(ffuncs: never, func: never): void
                /**
                 * Sets the implementation function for #hb_font_get_font_h_extents_func_t.
                 * @since 1.1.2
                 * @param ffuncs A font-function structure
                 * @param func The callback function to assign
                 */
                font_funcs_set_font_h_extents_func(ffuncs: never, func: never): void
                /**
                 * Sets the implementation function for #hb_font_get_font_v_extents_func_t.
                 * @since 1.1.2
                 * @param ffuncs A font-function structure
                 * @param func The callback function to assign
                 */
                font_funcs_set_font_v_extents_func(ffuncs: never, func: never): void
                /**
                 * Sets the implementation function for #hb_font_get_glyph_contour_point_func_t.
                 * @since 0.9.2
                 * @param ffuncs A font-function structure
                 * @param func The callback function to assign
                 */
                font_funcs_set_glyph_contour_point_func(ffuncs: never, func: never): void
                /**
                 * Sets the implementation function for #hb_font_get_glyph_extents_func_t.
                 * @since 0.9.2
                 * @param ffuncs A font-function structure
                 * @param func The callback function to assign
                 */
                font_funcs_set_glyph_extents_func(ffuncs: never, func: never): void
                /**
                 * Sets the implementation function for #hb_font_get_glyph_from_name_func_t.
                 * @since 0.9.2
                 * @param ffuncs A font-function structure
                 * @param func The callback function to assign
                 */
                font_funcs_set_glyph_from_name_func(ffuncs: never, func: never): void
                /**
                 * Deprecated.  Use hb_font_funcs_set_nominal_glyph_func() and
                 * hb_font_funcs_set_variation_glyph_func() instead.
                 * @since 0.9.2
                 * @deprecated since 1.2.3
                 * @param ffuncs The font-functions structure
                 * @param func callback function
                 */
                font_funcs_set_glyph_func(ffuncs: never, func: never): void
                /**
                 * Sets the implementation function for #hb_font_get_glyph_h_advance_func_t.
                 * @since 0.9.2
                 * @param ffuncs A font-function structure
                 * @param func The callback function to assign
                 */
                font_funcs_set_glyph_h_advance_func(ffuncs: never, func: never): void
                /**
                 * Sets the implementation function for #hb_font_get_glyph_h_advances_func_t.
                 * @since 1.8.6
                 * @param ffuncs A font-function structure
                 * @param func The callback function to assign
                 */
                font_funcs_set_glyph_h_advances_func(ffuncs: never, func: never): void
                /**
                 * Sets the implementation function for #hb_font_get_glyph_h_kerning_func_t.
                 * @since 0.9.2
                 * @param ffuncs A font-function structure
                 * @param func The callback function to assign
                 */
                font_funcs_set_glyph_h_kerning_func(ffuncs: never, func: never): void
                /**
                 * Sets the implementation function for #hb_font_get_glyph_h_origin_func_t.
                 * @since 0.9.2
                 * @param ffuncs A font-function structure
                 * @param func The callback function to assign
                 */
                font_funcs_set_glyph_h_origin_func(ffuncs: never, func: never): void
                /**
                 * Sets the implementation function for #hb_font_get_glyph_h_origins_func_t.
                 * @since 11.3.0
                 * @param ffuncs A font-function structure
                 * @param func The callback function to assign
                 */
                font_funcs_set_glyph_h_origins_func(ffuncs: never, func: never): void
                /**
                 * Sets the implementation function for #hb_font_get_glyph_name_func_t.
                 * @since 0.9.2
                 * @param ffuncs A font-function structure
                 * @param func The callback function to assign
                 */
                font_funcs_set_glyph_name_func(ffuncs: never, func: never): void
                /**
                 * Sets the implementation function for #hb_font_get_glyph_shape_func_t,
                 * which is the same as #hb_font_draw_glyph_func_t.
                 * @since 4.0.0
                 * @deprecated since 7.0.0 Use hb_font_funcs_set_draw_glyph_func() instead
                 * @param ffuncs A font-function structure
                 * @param func The callback function to assign
                 */
                font_funcs_set_glyph_shape_func(ffuncs: never, func: never): void
                /**
                 * Sets the implementation function for #hb_font_get_glyph_v_advance_func_t.
                 * @since 0.9.2
                 * @param ffuncs A font-function structure
                 * @param func The callback function to assign
                 */
                font_funcs_set_glyph_v_advance_func(ffuncs: never, func: never): void
                /**
                 * Sets the implementation function for #hb_font_get_glyph_v_advances_func_t.
                 * @since 1.8.6
                 * @param ffuncs A font-function structure
                 * @param func The callback function to assign
                 */
                font_funcs_set_glyph_v_advances_func(ffuncs: never, func: never): void
                /**
                 * Sets the implementation function for #hb_font_get_glyph_v_kerning_func_t.
                 * @since 0.9.2
                 * @deprecated since 2.0.0
                 * @param ffuncs A font-function structure
                 * @param func The callback function to assign
                 */
                font_funcs_set_glyph_v_kerning_func(ffuncs: never, func: never): void
                /**
                 * Sets the implementation function for #hb_font_get_glyph_v_origin_func_t.
                 * @since 0.9.2
                 * @param ffuncs A font-function structure
                 * @param func The callback function to assign
                 */
                font_funcs_set_glyph_v_origin_func(ffuncs: never, func: never): void
                /**
                 * Sets the implementation function for #hb_font_get_glyph_v_origins_func_t.
                 * @since 11.3.0
                 * @param ffuncs A font-function structure
                 * @param func The callback function to assign
                 */
                font_funcs_set_glyph_v_origins_func(ffuncs: never, func: never): void
                /**
                 * Sets the implementation function for #hb_font_get_nominal_glyph_func_t.
                 * @since 1.2.3
                 * @param ffuncs A font-function structure
                 * @param func The callback function to assign
                 */
                font_funcs_set_nominal_glyph_func(ffuncs: never, func: never): void
                /**
                 * Sets the implementation function for #hb_font_get_nominal_glyphs_func_t.
                 * @since 2.0.0
                 * @param ffuncs A font-function structure
                 * @param func The callback function to assign
                 */
                font_funcs_set_nominal_glyphs_func(ffuncs: never, func: never): void
                /**
                 * Sets the implementation function for #hb_font_paint_glyph_func_t.
                 * @since 7.0.0
                 * @deprecated since 11.2.0 Use hb_font_funcs_set_paint_glyph_or_fail_func() instead.
                 * @param ffuncs A font-function structure
                 * @param func The callback function to assign
                 */
                font_funcs_set_paint_glyph_func(ffuncs: never, func: never): void
                /**
                 * Sets the implementation function for #hb_font_paint_glyph_or_fail_func_t.
                 * @since 11.2.0
                 * @param ffuncs A font-function structure
                 * @param func The callback function to assign
                 */
                font_funcs_set_paint_glyph_or_fail_func(ffuncs: never, func: never): void
                /**
                 * Sets the implementation function for #hb_font_get_variation_glyph_func_t.
                 * @since 1.2.3
                 * @param ffuncs A font-function structure
                 * @param func The callback function to assign
                 */
                font_funcs_set_variation_glyph_func(ffuncs: never, func: never): void
                /**
                 * Fetches the empty font object.
                 * @since 0.9.2
                 * @returns The empty font object
                 */
                font_get_empty(): never
                /**
                 * Fetches the extents for a font in a text segment of the
                 * specified direction.
                 *
                 * Calls the appropriate direction-specific variant (horizontal
                 * or vertical) depending on the value of `direction`.
                 * @since 1.1.3
                 * @param font #hb_font_t to work upon
                 * @param direction The direction of the text segment
                 * @returns , The #hb_font_extents_t retrieved
                 */
                font_get_extents_for_direction(font: never, direction: never): never
                /**
                 * Fetches the face associated with the specified font object.
                 * @since 0.9.2
                 * @param font #hb_font_t to work upon
                 * @returns The #hb_face_t value
                 */
                font_get_face(font: never): never
                /**
                 * Fetches the glyph ID for a Unicode code point in the specified
                 * font, with an optional variation selector.
                 *
                 * If `variation_selector` is 0, calls hb_font_get_nominal_glyph();
                 * otherwise calls hb_font_get_variation_glyph().
                 * @since 0.9.2
                 * @param font #hb_font_t to work upon
                 * @param unicode The Unicode code point to query
                 * @param variation_selector A variation-selector code point
                 * @returns `true` if data found, `false` otherwise, The glyph ID retrieved
                 */
                font_get_glyph(font: never, unicode: never, variation_selector: never): [never, never]
                /**
                 * Fetches the advance for a glyph ID from the specified font,
                 * in a text segment of the specified direction.
                 *
                 * Calls the appropriate direction-specific variant (horizontal
                 * or vertical) depending on the value of `direction`.
                 * @since 0.9.2
                 * @param font #hb_font_t to work upon
                 * @param glyph The glyph ID to query
                 * @param direction The direction of the text segment
                 * @returns , The horizontal advance retrieved, The vertical advance retrieved
                 */
                font_get_glyph_advance_for_direction(font: never, glyph: never, direction: never): [never, never]
                /**
                 * Fetches the advances for a sequence of glyph IDs in the specified
                 * font, in a text segment of the specified direction.
                 *
                 * Calls the appropriate direction-specific variant (horizontal
                 * or vertical) depending on the value of `direction`.
                 * @since 1.8.6
                 * @param font #hb_font_t to work upon
                 * @param direction The direction of the text segment
                 * @param count The number of glyph IDs in the sequence queried
                 * @param first_glyph The first glyph ID to query
                 * @param glyph_stride The stride between successive glyph IDs
                 * @returns , The first advance retrieved, The stride between successive advances
                 */
                font_get_glyph_advances_for_direction(font: never, direction: never, count: number, first_glyph: never, glyph_stride: number): [never, number]
                /**
                 * Fetches the (x,y) coordinates of a specified contour-point index
                 * in the specified glyph, within the specified font.
                 * @since 0.9.2
                 * @param font #hb_font_t to work upon
                 * @param glyph The glyph ID to query
                 * @param point_index The contour-point index to query
                 * @returns `true` if data found, `false` otherwise, The X value retrieved for the contour point, The Y value retrieved for the contour point
                 */
                font_get_glyph_contour_point(font: never, glyph: never, point_index: number): [never, never, never]
                /**
                 * Fetches the (X,Y) coordinates of a specified contour-point index
                 * in the specified glyph ID in the specified font, with respect
                 * to the origin in a text segment in the specified direction.
                 *
                 * Calls the appropriate direction-specific variant (horizontal
                 * or vertical) depending on the value of `direction`.
                 * @since 0.9.2
                 * @param font #hb_font_t to work upon
                 * @param glyph The glyph ID to query
                 * @param point_index The contour-point index to query
                 * @param direction The direction of the text segment
                 * @returns `true` if data found, `false` otherwise, The X value retrieved for the contour point, The Y value retrieved for the contour point
                 */
                font_get_glyph_contour_point_for_origin(font: never, glyph: never, point_index: number, direction: never): [never, never, never]
                /**
                 * Fetches the #hb_glyph_extents_t data for a glyph ID
                 * in the specified font.
                 * @since 0.9.2
                 * @param font #hb_font_t to work upon
                 * @param glyph The glyph ID to query
                 * @returns `true` if data found, `false` otherwise, The #hb_glyph_extents_t retrieved
                 */
                font_get_glyph_extents(font: never, glyph: never): [never, never]
                /**
                 * Fetches the #hb_glyph_extents_t data for a glyph ID
                 * in the specified font, with respect to the origin in
                 * a text segment in the specified direction.
                 *
                 * Calls the appropriate direction-specific variant (horizontal
                 * or vertical) depending on the value of `direction`.
                 * @since 0.9.2
                 * @param font #hb_font_t to work upon
                 * @param glyph The glyph ID to query
                 * @param direction The direction of the text segment
                 * @returns `true` if data found, `false` otherwise, The #hb_glyph_extents_t retrieved
                 */
                font_get_glyph_extents_for_origin(font: never, glyph: never, direction: never): [never, never]
                /**
                 * Fetches the glyph ID that corresponds to a name string in the specified `font`.
                 *
                 * <note>Note: `len` == -1 means the name string is null-terminated.</note>
                 * @since 0.9.2
                 * @param font #hb_font_t to work upon
                 * @param name The name string to query
                 * @returns `true` if data found, `false` otherwise, The glyph ID retrieved
                 */
                font_get_glyph_from_name(font: never, name: string[]): [never, never]
                /**
                 * Fetches the advance for a glyph ID in the specified font,
                 * for horizontal text segments.
                 * @since 0.9.2
                 * @param font #hb_font_t to work upon
                 * @param glyph The glyph ID to query
                 * @returns The advance of `glyph` within `font`
                 */
                font_get_glyph_h_advance(font: never, glyph: never): never
                /**
                 * Fetches the advances for a sequence of glyph IDs in the specified
                 * font, for horizontal text segments.
                 * @since 1.8.6
                 * @param font #hb_font_t to work upon
                 * @param count The number of glyph IDs in the sequence queried
                 * @param first_glyph The first glyph ID to query
                 * @param glyph_stride The stride between successive glyph IDs
                 * @param advance_stride The stride between successive advances
                 * @returns , The first advance retrieved
                 */
                font_get_glyph_h_advances(font: never, count: number, first_glyph: never, glyph_stride: number, advance_stride: number): never
                /**
                 * Fetches the kerning-adjustment value for a glyph-pair in
                 * the specified font, for horizontal text segments.
                 *
                 * <note>It handles legacy kerning only (as returned by the corresponding
                 * #hb_font_funcs_t function).</note>
                 * @since 0.9.2
                 * @param font #hb_font_t to work upon
                 * @param left_glyph The glyph ID of the left glyph in the glyph pair
                 * @param right_glyph The glyph ID of the right glyph in the glyph pair
                 * @returns The kerning adjustment value
                 */
                font_get_glyph_h_kerning(font: never, left_glyph: never, right_glyph: never): never
                /**
                 * Fetches the (X,Y) coordinates of the origin for a glyph ID
                 * in the specified font, for horizontal text segments.
                 * @since 0.9.2
                 * @param font #hb_font_t to work upon
                 * @param glyph The glyph ID to query
                 * @returns `true` if data found, `false` otherwise, The X coordinate of the origin, The Y coordinate of the origin
                 */
                font_get_glyph_h_origin(font: never, glyph: never): [never, never, never]
                /**
                 * Fetches the (X,Y) coordinates of the origin for requested glyph IDs
                 * in the specified font, for horizontal text segments.
                 * @since 11.3.0
                 * @param font #hb_font_t to work upon
                 * @param count The number of glyph IDs in the sequence queried
                 * @param first_glyph The first glyph ID to query
                 * @param glyph_stride The stride between successive glyph IDs
                 * @param x_stride The stride between successive X coordinates
                 * @param y_stride The stride between successive Y coordinates
                 * @returns `true` if data found, `false` otherwise, The first X coordinate of the origin retrieved, The first Y coordinate of the origin retrieved
                 */
                font_get_glyph_h_origins(font: never, count: number, first_glyph: never, glyph_stride: number, x_stride: number, y_stride: number): [never, never, never]
                /**
                 * Fetches the kerning-adjustment value for a glyph-pair in the specified font.
                 *
                 * Calls the appropriate direction-specific variant (horizontal
                 * or vertical) depending on the value of `direction`.
                 * @since 0.9.2
                 * @param font #hb_font_t to work upon
                 * @param first_glyph The glyph ID of the first glyph in the glyph pair to query
                 * @param second_glyph The glyph ID of the second glyph in the glyph pair to query
                 * @param direction The direction of the text segment
                 * @returns , The horizontal kerning-adjustment value retrieved, The vertical kerning-adjustment value retrieved
                 */
                font_get_glyph_kerning_for_direction(font: never, first_glyph: never, second_glyph: never, direction: never): [never, never]
                /**
                 * Fetches the glyph-name string for a glyph ID in the specified `font`.
                 *
                 * According to the OpenType specification, glyph names are limited to 63
                 * characters and can only contain (a subset of) ASCII.
                 * @since 0.9.2
                 * @param font #hb_font_t to work upon
                 * @param glyph The glyph ID to query
                 * @returns `true` if data found, `false` otherwise, Name string retrieved for the glyph ID
                 */
                font_get_glyph_name(font: never, glyph: never): [never, string[]]
                /**
                 * Fetches the (X,Y) coordinates of the origin for a glyph in
                 * the specified font.
                 *
                 * Calls the appropriate direction-specific variant (horizontal
                 * or vertical) depending on the value of `direction`.
                 * @since 0.9.2
                 * @param font #hb_font_t to work upon
                 * @param glyph The glyph ID to query
                 * @param direction The direction of the text segment
                 * @returns , The X coordinate retrieved for the origin, The Y coordinate retrieved for the origin
                 */
                font_get_glyph_origin_for_direction(font: never, glyph: never, direction: never): [never, never]
                /**
                 * Fetches the glyph shape that corresponds to a glyph in the specified `font`.
                 * The shape is returned by way of calls to the callbacks of the `dfuncs`
                 * objects, with `draw_data` passed to them.
                 * @since 4.0.0
                 * @deprecated since 7.0.0 Use hb_font_draw_glyph() instead
                 * @param font #hb_font_t to work upon
                 * @param glyph The glyph ID
                 * @param dfuncs #hb_draw_funcs_t to draw to
                 * @param draw_data User data to pass to draw callbacks
                 */
                font_get_glyph_shape(font: never, glyph: never, dfuncs: never, draw_data: never | null): void
                /**
                 * Fetches the advance for a glyph ID in the specified font,
                 * for vertical text segments.
                 * @since 0.9.2
                 * @param font #hb_font_t to work upon
                 * @param glyph The glyph ID to query
                 * @returns The advance of `glyph` within `font`
                 */
                font_get_glyph_v_advance(font: never, glyph: never): never
                /**
                 * Fetches the advances for a sequence of glyph IDs in the specified
                 * font, for vertical text segments.
                 * @since 1.8.6
                 * @param font #hb_font_t to work upon
                 * @param count The number of glyph IDs in the sequence queried
                 * @param first_glyph The first glyph ID to query
                 * @param glyph_stride The stride between successive glyph IDs
                 * @returns , The first advance retrieved, The stride between successive advances
                 */
                font_get_glyph_v_advances(font: never, count: number, first_glyph: never, glyph_stride: number): [never, number]
                /**
                 * Fetches the kerning-adjustment value for a glyph-pair in
                 * the specified font, for vertical text segments.
                 *
                 * <note>It handles legacy kerning only (as returned by the corresponding
                 * #hb_font_funcs_t function).</note>
                 * @since 0.9.2
                 * @deprecated since 2.0.0
                 * @param font #hb_font_t to work upon
                 * @param top_glyph The glyph ID of the top glyph in the glyph pair
                 * @param bottom_glyph The glyph ID of the bottom glyph in the glyph pair
                 * @returns The kerning adjustment value
                 */
                font_get_glyph_v_kerning(font: never, top_glyph: never, bottom_glyph: never): never
                /**
                 * Fetches the (X,Y) coordinates of the origin for a glyph ID
                 * in the specified font, for vertical text segments.
                 * @since 0.9.2
                 * @param font #hb_font_t to work upon
                 * @param glyph The glyph ID to query
                 * @returns `true` if data found, `false` otherwise, The X coordinate of the origin, The Y coordinate of the origin
                 */
                font_get_glyph_v_origin(font: never, glyph: never): [never, never, never]
                /**
                 * Fetches the (X,Y) coordinates of the origin for requested glyph IDs
                 * in the specified font, for vertical text segments.
                 * @since 11.3.0
                 * @param font #hb_font_t to work upon
                 * @param count The number of glyph IDs in the sequence queried
                 * @param first_glyph The first glyph ID to query
                 * @param glyph_stride The stride between successive glyph IDs
                 * @param x_stride The stride between successive X coordinates
                 * @param y_stride The stride between successive Y coordinates
                 * @returns `true` if data found, `false` otherwise, The first X coordinate of the origin retrieved, The first Y coordinate of the origin retrieved
                 */
                font_get_glyph_v_origins(font: never, count: number, first_glyph: never, glyph_stride: number, x_stride: number, y_stride: number): [never, never, never]
                /**
                 * Fetches the extents for a specified font, for horizontal
                 * text segments.
                 * @since 1.1.3
                 * @param font #hb_font_t to work upon
                 * @returns `true` if data found, `false` otherwise, The font extents retrieved
                 */
                font_get_h_extents(font: never): [never, never]
                /**
                 * Fetches the nominal glyph ID for a Unicode code point in the
                 * specified font.
                 *
                 * This version of the function should not be used to fetch glyph IDs
                 * for code points modified by variation selectors. For variation-selector
                 * support, user hb_font_get_variation_glyph() or use hb_font_get_glyph().
                 * @since 1.2.3
                 * @param font #hb_font_t to work upon
                 * @param unicode The Unicode code point to query
                 * @returns `true` if data found, `false` otherwise, The glyph ID retrieved
                 */
                font_get_nominal_glyph(font: never, unicode: never): [never, never]
                /**
                 * Fetches the nominal glyph IDs for a sequence of Unicode code points. Glyph
                 * IDs must be returned in a #hb_codepoint_t output parameter. Stops at the
                 * first unsupported glyph ID.
                 * @since 2.6.3
                 * @param font #hb_font_t to work upon
                 * @param count number of code points to query
                 * @param first_unicode The first Unicode code point to query
                 * @param unicode_stride The stride between successive code points
                 * @param glyph_stride The stride between successive glyph IDs
                 * @returns the number of code points processed, The first glyph ID retrieved
                 */
                font_get_nominal_glyphs(font: never, count: number, first_unicode: never, unicode_stride: number, glyph_stride: number): [number, never]
                /**
                 * Fetches the parent font of `font`.
                 * @since 0.9.2
                 * @param font #hb_font_t to work upon
                 * @returns The parent font object
                 */
                font_get_parent(font: never): never
                /**
                 * Fetches the horizontal and vertical points-per-em (ppem) of a font.
                 * @since 0.9.2
                 * @param font #hb_font_t to work upon
                 * @returns , Horizontal ppem value, Vertical ppem value
                 */
                font_get_ppem(font: never): [number, number]
                /**
                 * Fetches the "point size" of a font. Used in CoreText to
                 * implement optical sizing.
                 * @since 1.6.0
                 * @param font #hb_font_t to work upon
                 * @returns Point size.  A value of zero means "not set."
                 */
                font_get_ptem(font: never): number
                /**
                 * Fetches the horizontal and vertical scale of a font.
                 * @since 0.9.2
                 * @param font #hb_font_t to work upon
                 * @returns , Horizontal scale value, Vertical scale value
                 */
                font_get_scale(font: never): [number, number]
                /**
                 * Returns the internal serial number of the font. The serial
                 * number is increased every time a setting on the font is
                 * changed, using a setter function.
                 * @since 4.4.0
                 * @param font #hb_font_t to work upon
                 * @returns serial number
                 */
                font_get_serial(font: never): number
                /**
                 * Fetches the "synthetic boldness" parameters of a font.
                 * @since 7.0.0
                 * @param font #hb_font_t to work upon
                 * @returns , return location for horizontal value, return location for vertical value, return location for in-place value
                 */
                font_get_synthetic_bold(font: never): [number, number, never]
                /**
                 * Fetches the "synthetic slant" of a font.
                 * @since 3.3.0
                 * @param font #hb_font_t to work upon
                 * @returns Synthetic slant.  By default is zero.
                 */
                font_get_synthetic_slant(font: never): number
                /**
                 * Fetches the extents for a specified font, for vertical
                 * text segments.
                 * @since 1.1.3
                 * @param font #hb_font_t to work upon
                 * @returns `true` if data found, `false` otherwise, The font extents retrieved
                 */
                font_get_v_extents(font: never): [never, never]
                /**
                 * Fetches the list of variation coordinates (in design-space units) currently
                 * set on a font.
                 *
                 * <note>Note that if no variation coordinates are set, this function may
                 * return %NULL.</note>
                 *
                 * <note>If variations have been set on the font using normalized coordinates
                 * (i.e. via hb_font_set_var_coords_normalized()), the design coordinates will
                 * have NaN (Not a Number) values.</note>
                 *
                 * Return value is valid as long as variation coordinates of the font
                 * are not modified.
                 * @since 3.3.0
                 * @param font #hb_font_t to work upon
                 * @returns coordinates array, Number of coordinates retrieved
                 */
                font_get_var_coords_design(font: never): [number, number]
                /**
                 * Fetches the list of normalized variation coordinates currently
                 * set on a font.
                 *
                 * <note>Note that if no variation coordinates are set, this function may
                 * return %NULL.</note>
                 *
                 * Return value is valid as long as variation coordinates of the font
                 * are not modified.
                 * @since 1.4.2
                 * @param font #hb_font_t to work upon
                 * @returns coordinates array, Number of coordinates retrieved
                 */
                font_get_var_coords_normalized(font: never): [number, number]
                /**
                 * Returns the currently-set named-instance index of the font.
                 * @since 7.0.0
                 * @param font a font.
                 * @returns Named-instance index or %HB_FONT_NO_VAR_NAMED_INSTANCE.
                 */
                font_get_var_named_instance(font: never): number
                /**
                 * Fetches the glyph ID for a Unicode code point when followed by
                 * by the specified variation-selector code point, in the specified
                 * font.
                 * @since 1.2.3
                 * @param font #hb_font_t to work upon
                 * @param unicode The Unicode code point to query
                 * @param variation_selector The  variation-selector code point to query
                 * @returns `true` if data found, `false` otherwise, The glyph ID retrieved
                 */
                font_get_variation_glyph(font: never, unicode: never, variation_selector: never): [never, never]
                /**
                 * Fetches the glyph ID from `font` that matches the specified string.
                 * Strings of the format `gidDDD` or `uniUUUU` are parsed automatically.
                 *
                 * <note>Note: `len` == -1 means the string is null-terminated.</note>
                 * @since 0.9.2
                 * @param font #hb_font_t to work upon
                 * @param s string to query
                 * @returns `true` if data found, `false` otherwise, The glyph ID corresponding to the string requested
                 */
                font_glyph_from_string(font: never, s: Uint8Array): [never, never]
                /**
                 * Fetches the name of the specified glyph ID in `font` and returns
                 * it in string `s`.
                 *
                 * If the glyph ID has no name in `font`, a string of the form `gidDDD` is
                 * generated, with `DDD` being the glyph ID.
                 *
                 * According to the OpenType specification, glyph names are limited to 63
                 * characters and can only contain (a subset of) ASCII.
                 * @since 0.9.2
                 * @param font #hb_font_t to work upon
                 * @param glyph The glyph ID to query
                 * @returns , The string containing the glyph name
                 */
                font_glyph_to_string(font: never, glyph: never): string[]
                /**
                 * Tests whether a font object is immutable.
                 * @since 0.9.2
                 * @param font #hb_font_t to work upon
                 * @returns `true` if `font` is immutable, `false` otherwise
                 */
                font_is_immutable(font: never): never
                /**
                 * Tests whether a font is synthetic. A synthetic font is one
                 * that has either synthetic slant or synthetic bold set on it.
                 * @since 11.2.0
                 * @param font #hb_font_t to work upon
                 * @returns `true` if the font is synthetic, `false` otherwise.
                 */
                font_is_synthetic(font: never): never
                /**
                 * Retrieves the list of font functions supported by HarfBuzz.
                 * @since 11.0.0
                 * @returns a    `NULL`-terminated array of supported font functions    constant strings. The returned array is owned by HarfBuzz    and should not be modified or freed.
                 */
                font_list_funcs(): string[]
                /**
                 * Makes `font` immutable.
                 * @since 0.9.2
                 * @param font #hb_font_t to work upon
                 */
                font_make_immutable(font: never): void
                /**
                 * Paints the glyph. This function is similar to
                 * hb_font_paint_glyph_or_fail(), but if painting a color glyph
                 * failed, it will fall back to painting an outline monochrome
                 * glyph.
                 *
                 * The painting instructions are returned by way of calls to
                 * the callbacks of the `funcs` object, with `paint_data` passed
                 * to them.
                 *
                 * If the font has color palettes (see hb_ot_color_has_palettes()),
                 * then `palette_index` selects the palette to use. If the font only
                 * has one palette, this will be 0.
                 * @since 7.0.0
                 * @param font #hb_font_t to work upon
                 * @param glyph The glyph ID
                 * @param pfuncs #hb_paint_funcs_t to paint with
                 * @param paint_data User data to pass to paint callbacks
                 * @param palette_index The index of the font's color palette to use
                 * @param foreground The foreground color, unpremultipled
                 */
                font_paint_glyph(font: never, glyph: never, pfuncs: never, paint_data: never | null, palette_index: number, foreground: never): void
                /**
                 * Paints a color glyph.
                 *
                 * Succeeds if `glyph` has color paint layers (COLRv0),
                 * a color paint graph (COLRv1), or a bitmap image that the
                 * font's callbacks render successfully.  Returns `false` if
                 * the font has no color data for `glyph`; the client can then
                 * fall back to hb_font_draw_glyph_or_fail() for the monochrome
                 * outline.
                 *
                 * The painting instructions are returned by way of calls to
                 * the callbacks of the `funcs` object, with `paint_data` passed
                 * to them.
                 *
                 * If the font has color palettes (see hb_ot_color_has_palettes()),
                 * then `palette_index` selects the palette to use. If the font only
                 * has one palette, this will be 0.
                 * @since 11.2.0
                 * @param font #hb_font_t to work upon
                 * @param glyph The glyph ID
                 * @param pfuncs #hb_paint_funcs_t to paint with
                 * @param paint_data User data to pass to paint callbacks
                 * @param palette_index The index of the font's color palette to use
                 * @param foreground The foreground color, unpremultipled
                 * @returns `true` if glyph was painted, `false` otherwise
                 */
                font_paint_glyph_or_fail(font: never, glyph: never, pfuncs: never, paint_data: never | null, palette_index: number, foreground: never): never
                /**
                 * Sets `face` as the font-face value of `font`.
                 * @since 1.4.3
                 * @param font #hb_font_t to work upon
                 * @param face The #hb_face_t to assign
                 */
                font_set_face(font: never, face: never): void
                /**
                 * Replaces the font-functions structure attached to a font, updating
                 * the font's user-data with `font`-data and the `destroy` callback.
                 * @since 0.9.2
                 * @param font #hb_font_t to work upon
                 * @param klass The font-functions structure.
                 * @param destroy The function to call when `font_data` is not needed anymore
                 */
                font_set_funcs(font: never, klass: never, destroy: never | null): void
                /**
                 * Replaces the user data attached to a font, updating the font's
                 *  `destroy` callback.
                 * @since 0.9.2
                 * @param font #hb_font_t to work upon
                 * @param destroy The function to call when `font_data` is not needed anymore
                 */
                font_set_funcs_data(font: never, destroy: never | null): void
                /**
                 * Sets the font-functions structure to use for a font, based on the
                 * specified name.
                 *
                 * If `name` is `NULL` or the empty string, the default (first) functioning font-functions
                 * are used.  This default can be changed by setting the `HB_FONT_FUNCS` environment
                 * variable to the name of the desired font-functions.
                 * @since 11.0.0
                 * @param font #hb_font_t to work upon
                 * @param name The name of the font-functions structure to use, or `NULL`
                 * @returns `true` if the font-functions was found and set, `false` otherwise
                 */
                font_set_funcs_using(font: never, name: string): never
                /**
                 * Sets the parent font of `font`.
                 * @since 1.0.5
                 * @param font #hb_font_t to work upon
                 * @param parent The parent font object to assign
                 */
                font_set_parent(font: never, parent: never): void
                /**
                 * Sets the horizontal and vertical pixels-per-em (PPEM) of a font.
                 *
                 * These values are used for pixel-size-specific adjustment to
                 * shaping and draw results, though for the most part they are
                 * unused and can be left unset.
                 * @since 0.9.2
                 * @param font #hb_font_t to work upon
                 * @param x_ppem Horizontal ppem value to assign
                 * @param y_ppem Vertical ppem value to assign
                 */
                font_set_ppem(font: never, x_ppem: number, y_ppem: number): void
                /**
                 * Sets the "point size" of a font. Set to zero to unset.
                 * Used in CoreText to implement optical sizing.
                 *
                 * <note>Note: There are 72 points in an inch.</note>
                 * @since 1.6.0
                 * @param font #hb_font_t to work upon
                 * @param ptem font size in points.
                 */
                font_set_ptem(font: never, ptem: number): void
                /**
                 * Sets the horizontal and vertical scale of a font.
                 *
                 * The font scale is a number related to, but not the same as,
                 * font size. Typically the client establishes a scale factor
                 * to be used between the two. For example, 64, or 256, which
                 * would be the fractional-precision part of the font scale.
                 * This is necessary because #hb_position_t values are integer
                 * types and you need to leave room for fractional values
                 * in there.
                 *
                 * For example, to set the font size to 20, with 64
                 * levels of fractional precision you would call
                 * `hb_font_set_scale(font, 20 * 64, 20 * 64)`.
                 *
                 * In the example above, even what font size 20 means is up to
                 * you. It might be 20 pixels, or 20 points, or 20 millimeters.
                 * HarfBuzz does not care about that.  You can set the point
                 * size of the font using hb_font_set_ptem(), and the pixel
                 * size using hb_font_set_ppem().
                 *
                 * The choice of scale is yours but needs to be consistent between
                 * what you set here, and what you expect out of #hb_position_t
                 * as well has draw / paint API output values.
                 *
                 * Fonts default to a scale equal to the UPEM value of their face.
                 * A font with this setting is sometimes called an "unscaled" font.
                 * @since 0.9.2
                 * @param font #hb_font_t to work upon
                 * @param x_scale Horizontal scale value to assign
                 * @param y_scale Vertical scale value to assign
                 */
                font_set_scale(font: never, x_scale: number, y_scale: number): void
                /**
                 * Sets the "synthetic boldness" of a font.
                 *
                 * Positive values for `x_embolden` / `y_embolden` make a font
                 * bolder, negative values thinner. Typical values are in the
                 * 0.01 to 0.05 range. The default value is zero.
                 *
                 * Synthetic boldness is applied by offsetting the contour
                 * points of the glyph shape.
                 *
                 * Synthetic boldness is applied when rendering a glyph via
                 * hb_font_draw_glyph_or_fail().
                 *
                 * If `in_place` is `false`, then glyph advance-widths are also
                 * adjusted, otherwise they are not.  The in-place mode is
                 * useful for simulating [font grading](https://fonts.google.com/knowledge/glossary/grade).
                 * @since 7.0.0
                 * @param font #hb_font_t to work upon
                 * @param x_embolden the amount to embolden horizontally
                 * @param y_embolden the amount to embolden vertically
                 * @param in_place whether to embolden glyphs in-place
                 */
                font_set_synthetic_bold(font: never, x_embolden: number, y_embolden: number, in_place: never): void
                /**
                 * Sets the "synthetic slant" of a font.  By default is zero.
                 * Synthetic slant is the graphical skew applied to the font
                 * at rendering time.
                 *
                 * HarfBuzz needs to know this value to adjust shaping results,
                 * metrics, and style values to match the slanted rendering.
                 *
                 * <note>Note: The glyph shape fetched via the hb_font_draw_glyph_or_fail()
                 * function is slanted to reflect this value as well.</note>
                 *
                 * <note>Note: The slant value is a ratio.  For example, a
                 * 20% slant would be represented as a 0.2 value.</note>
                 * @since 3.3.0
                 * @param font #hb_font_t to work upon
                 * @param slant synthetic slant value.
                 */
                font_set_synthetic_slant(font: never, slant: number): void
                /**
                 * Applies a list of variation coordinates (in design-space units)
                 * to a font.
                 *
                 * Note that this overrides all existing variations set on `font`.
                 * Axes not included in `coords` will be effectively set to their
                 * default values.
                 * @since 1.4.2
                 * @param font #hb_font_t to work upon
                 * @param coords Array of variation coordinates to apply
                 */
                font_set_var_coords_design(font: never, coords: number[]): void
                /**
                 * Applies a list of variation coordinates (in normalized units)
                 * to a font.
                 *
                 * Note that this overrides all existing variations set on `font`.
                 * Axes not included in `coords` will be effectively set to their
                 * default values.
                 *
                 * <note>Note: Coordinates should be normalized to 2.14.</note>
                 * @since 1.4.2
                 * @param font #hb_font_t to work upon
                 * @param coords Array of variation coordinates to apply
                 */
                font_set_var_coords_normalized(font: never, coords: number[]): void
                /**
                 * Sets design coords of a font from a named-instance index.
                 * @since 2.6.0
                 * @param font a font.
                 * @param instance_index named instance index.
                 */
                font_set_var_named_instance(font: never, instance_index: number): void
                /**
                 * Change the value of one variation axis on the font.
                 *
                 * Note: This function is expensive to be called repeatedly.
                 *   If you want to set multiple variation axes at the same time,
                 *   use hb_font_set_variations() instead.
                 * @since 7.1.0
                 * @param font #hb_font_t to work upon
                 * @param tag The #hb_tag_t tag of the variation-axis name
                 * @param value The value of the variation axis
                 */
                font_set_variation(font: never, tag: never, value: number): void
                /**
                 * Applies a list of font-variation settings to a font.
                 *
                 * Note that this overrides all existing variations set on `font`.
                 * Axes not included in `variations` will be effectively set to their
                 * default values.
                 * @since 1.4.2
                 * @param font #hb_font_t to work upon
                 * @param variations Array of variation settings to apply
                 */
                font_set_variations(font: never, variations: never[]): void
                /**
                 * Subtracts the origin coordinates from an (X,Y) point coordinate,
                 * in the specified glyph ID in the specified font.
                 *
                 * Calls the appropriate direction-specific variant (horizontal
                 * or vertical) depending on the value of `direction`.
                 * @since 0.9.2
                 * @param font #hb_font_t to work upon
                 * @param glyph The glyph ID to query
                 * @param direction The direction of the text segment
                 * @returns , Input = The original X coordinate     Output = The X coordinate minus the X-coordinate of the origin, Input = The original Y coordinate     Output = The Y coordinate minus the Y-coordinate of the origin
                 */
                font_subtract_glyph_origin_for_direction(font: never, glyph: never, direction: never, x: never, y: never): [never, never]
                /**
                 * Frees the memory pointed to by `ptr`, using the allocator set at
                 * compile-time. Typically just free().
                 * @since 11.0.0
                 * @param ptr The pointer to the memory to free.
                 */
                free(ptr: never | null): void
                /**
                 * Creates an #hb_face_t face object from the specified FT_Face.
                 *
                 * Note that this is using the FT_Face object just to get at the underlying
                 * font data, and fonts created from the returned #hb_face_t will use the native
                 * HarfBuzz font implementation, unless you call hb_ft_font_set_funcs() on them.
                 *
                 * This variant of the function does not provide any life-cycle management.
                 *
                 * Most client programs should use hb_ft_face_create_referenced()
                 * (or, perhaps, hb_ft_face_create_cached()) instead.
                 *
                 * If you know you have valid reasons not to use hb_ft_face_create_referenced(),
                 * then it is the client program's responsibility to destroy `ft_face`
                 * after the #hb_face_t face object has been destroyed.
                 * @since 0.9.2
                 * @param ft_face FT_Face to work upon
                 * @param destroy A callback to call when the face object is not needed anymore
                 * @returns the new #hb_face_t face object
                 */
                ft_face_create(ft_face: freetype2.Face, destroy: never | null): never
                /**
                 * Creates an #hb_face_t face object from the specified FT_Face.
                 *
                 * Note that this is using the FT_Face object just to get at the underlying
                 * font data, and fonts created from the returned #hb_face_t will use the native
                 * HarfBuzz font implementation, unless you call hb_ft_font_set_funcs() on them.
                 *
                 * This variant of the function caches the newly created #hb_face_t
                 * face object, using the `generic` pointer of `ft_face`. Subsequent function
                 * calls that are passed the same `ft_face` parameter will have the same
                 * #hb_face_t returned to them, and that #hb_face_t will be correctly
                 * reference counted.
                 *
                 * However, client programs are still responsible for destroying
                 *  `ft_face` after the last #hb_face_t face object has been destroyed.
                 * @since 0.9.2
                 * @param ft_face FT_Face to work upon
                 * @returns the new #hb_face_t face object
                 */
                ft_face_create_cached(ft_face: freetype2.Face): never
                /**
                 * Creates an #hb_face_t face object from the specified
                 * font blob and face index.
                 *
                 * This is similar in functionality to hb_face_create_or_fail(),
                 * but uses the FreeType library for loading the font blob. This can
                 * be useful, for example, to load WOFF and WOFF2 font data.
                 * @since 11.0.0
                 * @param blob A blob
                 * @param index The index of the face within the blob
                 * @returns The new face object, or `NULL` if loading fails (eg. blob does not contain valid font data).
                 */
                ft_face_create_from_blob_or_fail(blob: never, index: number): never
                /**
                 * Creates an #hb_face_t face object from the specified
                 * font file and face index.
                 *
                 * This is similar in functionality to hb_face_create_from_file_or_fail(),
                 * but uses the FreeType library for loading the font file. This can
                 * be useful, for example, to load WOFF and WOFF2 font data.
                 * @since 10.1.0
                 * @param file_name A font filename
                 * @param index The index of the face within the file
                 * @returns The new face object, or `NULL` if no face is found at the specified index or the file cannot be read.
                 */
                ft_face_create_from_file_or_fail(file_name: string, index: number): never
                /**
                 * Creates an #hb_face_t face object from the specified FT_Face.
                 *
                 * Note that this is using the FT_Face object just to get at the underlying
                 * font data, and fonts created from the returned #hb_face_t will use the native
                 * HarfBuzz font implementation, unless you call hb_ft_font_set_funcs() on them.
                 *
                 * This is the preferred variant of the hb_ft_face_create*
                 * function family, because it calls FT_Reference_Face() on `ft_face`,
                 * ensuring that `ft_face` remains alive as long as the resulting
                 * #hb_face_t face object remains alive. Also calls FT_Done_Face()
                 * when the #hb_face_t face object is destroyed.
                 *
                 * Use this version unless you know you have good reasons not to.
                 * @since 0.9.38
                 * @param ft_face FT_Face to work upon
                 * @returns the new #hb_face_t face object
                 */
                ft_face_create_referenced(ft_face: freetype2.Face): never
                /**
                 * Refreshes the state of `font` when the underlying FT_Face has changed.
                 * This function should be called after changing the size or
                 * variation-axis settings on the FT_Face.
                 * @since 1.0.5
                 * @param font #hb_font_t to work upon
                 */
                ft_font_changed(font: never): void
                /**
                 * Creates an #hb_font_t font object from the specified FT_Face.
                 *
                 * <note>Note: You must set the face size on `ft_face` before calling
                 * hb_ft_font_create() on it. HarfBuzz assumes size is always set and will
                 * access `size` member of FT_Face unconditionally.</note>
                 *
                 * This variant of the function does not provide any life-cycle management.
                 *
                 * Most client programs should use hb_ft_font_create_referenced()
                 * instead.
                 *
                 * If you know you have valid reasons not to use hb_ft_font_create_referenced(),
                 * then it is the client program's responsibility to destroy `ft_face`
                 * only after the #hb_font_t font object has been destroyed.
                 *
                 * HarfBuzz will use the `destroy` callback on the #hb_font_t font object
                 * if it is supplied when you use this function. However, even if `destroy`
                 * is provided, it is the client program's responsibility to destroy `ft_face`,
                 * and it is the client program's responsibility to ensure that `ft_face` is
                 * destroyed only after the #hb_font_t font object has been destroyed.
                 * @since 0.9.2
                 * @param ft_face FT_Face to work upon
                 * @param destroy A callback to call when the font object is not needed anymore
                 * @returns the new #hb_font_t font object
                 */
                ft_font_create(ft_face: freetype2.Face, destroy: never | null): never
                /**
                 * Creates an #hb_font_t font object from the specified FT_Face.
                 *
                 * <note>Note: You must set the face size on `ft_face` before calling
                 * hb_ft_font_create_referenced() on it. HarfBuzz assumes size is always set
                 * and will access `size` member of FT_Face unconditionally.</note>
                 *
                 * This is the preferred variant of the hb_ft_font_create*
                 * function family, because it calls FT_Reference_Face() on `ft_face`,
                 * ensuring that `ft_face` remains alive as long as the resulting
                 * #hb_font_t font object remains alive.
                 *
                 * Use this version unless you know you have good reasons not to.
                 * @since 0.9.38
                 * @param ft_face FT_Face to work upon
                 * @returns the new #hb_font_t font object
                 */
                ft_font_create_referenced(ft_face: freetype2.Face): never
                /**
                 * Fetches the FT_Load_Glyph load flags of the specified #hb_font_t.
                 *
                 * For more information, see
                 * <https://freetype.org/freetype2/docs/reference/ft2-glyph_retrieval.html#ft_load_xxx>
                 *
                 * This function works with #hb_font_t objects created by
                 * hb_ft_font_create() or hb_ft_font_create_referenced().
                 * @since 1.0.5
                 * @param font #hb_font_t to work upon
                 * @returns FT_Load_Glyph flags found, or 0
                 */
                ft_font_get_load_flags(font: never): number
                /**
                 * Configures the font-functions structure of the specified
                 * #hb_font_t font object to use FreeType font functions.
                 *
                 * In particular, you can use this function to configure an
                 * existing #hb_face_t face object for use with FreeType font
                 * functions even if that #hb_face_t face object was initially
                 * created with hb_face_create(), and therefore was not
                 * initially configured to use FreeType font functions.
                 *
                 * An #hb_font_t object created with hb_ft_font_create()
                 * is preconfigured for FreeType font functions and does not
                 * require this function to be used.
                 *
                 * Note that if you modify the underlying #hb_font_t after
                 * calling this function, you need to call hb_ft_hb_font_changed()
                 * to update the underlying FT_Face.
                 *
                 * <note>Note: Internally, this function creates an FT_Face.
                 * </note>
                 * @since 1.0.5
                 * @param font #hb_font_t to work upon
                 */
                ft_font_set_funcs(font: never): void
                /**
                 * Sets the FT_Load_Glyph load flags for the specified #hb_font_t.
                 *
                 * For more information, see
                 * <https://freetype.org/freetype2/docs/reference/ft2-glyph_retrieval.html#ft_load_xxx>
                 *
                 * This function works with #hb_font_t objects created by
                 * hb_ft_font_create() or hb_ft_font_create_referenced().
                 * @since 1.0.5
                 * @param font #hb_font_t to work upon
                 * @param load_flags The FreeType load flags to set
                 */
                ft_font_set_load_flags(font: never, load_flags: number): void
                /**
                 * Refreshes the state of the underlying FT_Face of `font` when the hb_font_t
                 *  `font` has changed.
                 * This function should be called after changing the size or
                 * variation-axis settings on the `font`.
                 * This call is fast if nothing has changed on `font`.
                 *
                 * Note that as of version 11.0.0, calling this function is not necessary,
                 * as HarfBuzz will automatically detect changes to the font and update
                 * the underlying FT_Face as needed.
                 * @since 4.4.0
                 * @param font #hb_font_t to work upon
                 * @returns true if changed, false otherwise
                 */
                ft_hb_font_changed(font: never): never
                /**
                 * Creates an #hb_blob_t blob from the specified
                 * GBytes data structure.
                 * @since 0.9.38
                 * @param gbytes the GBytes structure to work upon
                 * @returns the new #hb_blob_t blob object
                 */
                glib_blob_create(gbytes: (GLib.Bytes | Uint8Array)): never
                /**
                 * Fetches a Unicode-functions structure that is populated
                 * with the appropriate GLib function for each method.
                 * @since 0.9.38
                 * @returns a pointer to the #hb_unicode_funcs_t Unicode-functions structure
                 */
                glib_get_unicode_funcs(): never
                /**
                 * Fetches the GUnicodeScript identifier that corresponds to the
                 * specified #hb_script_t script.
                 * @since 0.9.38
                 * @param script The #hb_script_t to query
                 * @returns the GUnicodeScript identifier found
                 */
                glib_script_from_script(script: never): GLib.UnicodeScript
                /**
                 * Fetches the #hb_script_t script that corresponds to the
                 * specified GUnicodeScript identifier.
                 * @since 0.9.38
                 * @param script The GUnicodeScript identifier to query
                 * @returns the #hb_script_t script found
                 */
                glib_script_to_script(script: GLib.UnicodeScript): never
                /**
                 * Returns glyph flags encoded within a #hb_glyph_info_t.
                 * @since 1.5.0
                 * @param info a #hb_glyph_info_t
                 * @returns The #hb_glyph_flags_t encoded within `info`
                 */
                glyph_info_get_glyph_flags(info: never): never
                /**
                 * Converts `str` representing a BCP 47 language tag to the corresponding
                 * #hb_language_t.
                 * @since 0.9.2
                 * @param str a string representing       a BCP 47 language tag
                 * @returns  The #hb_language_t corresponding to the BCP 47 language tag.
                 */
                language_from_string(str: Uint8Array): never
                /**
                 * Fetch the default language from current locale.
                 *
                 * <note>Note that the first time this function is called, it calls
                 * "setlocale (LC_CTYPE, nullptr)" to fetch current locale.  The underlying
                 * setlocale function is, in many implementations, NOT threadsafe.  To avoid
                 * problems, call this function once before multiple threads can call it.
                 * This function is only used from hb_buffer_guess_segment_properties() by
                 * HarfBuzz itself.</note>
                 * @since 0.9.2
                 * @returns The default language of the locale as an #hb_language_t
                 */
                language_get_default(): never
                /**
                 * Check whether a second language tag is the same or a more
                 * specific version of the provided language tag.  For example,
                 * "fa_IR.utf8" is a more specific tag for "fa" or for "fa_IR".
                 * @since 5.0.0
                 * @param language The #hb_language_t to work on
                 * @param specific Another #hb_language_t
                 * @returns `true` if languages match, `false` otherwise.
                 */
                language_matches(language: never, specific: never): never
                /**
                 * Converts an #hb_language_t to a string.
                 * @since 0.9.2
                 * @param language The #hb_language_t to convert
                 * @returns  A `NULL`-terminated string representing the `language`. Must not be freed by the caller.
                 */
                language_to_string(language: never): string
                /**
                 * Allocates `size` bytes of memory, using the allocator set at
                 * compile-time. Typically just malloc().
                 * @since 11.0.0
                 * @param size The size of the memory to allocate.
                 * @returns A pointer to the allocated memory.
                 */
                malloc(size: number): never | null
                /**
                 * Tests whether memory allocation for a set was successful.
                 * @since 1.7.7
                 * @param map A map
                 * @returns `true` if allocation succeeded, `false` otherwise
                 */
                map_allocation_successful(map: never): never
                /**
                 * Clears out the contents of `map`.
                 * @since 1.7.7
                 * @param map A map
                 */
                map_clear(map: never): void
                /**
                 * Allocate a copy of `map`.
                 * @since 4.4.0
                 * @param map A map
                 * @returns Newly-allocated map.
                 */
                map_copy(map: never): never
                /**
                 * Creates a new, initially empty map.
                 * @since 1.7.7
                 * @returns The new #hb_map_t
                 */
                map_create(): never
                /**
                 * Removes `key` and its stored value from `map`.
                 * @since 1.7.7
                 * @param map A map
                 * @param key The key to delete
                 */
                map_del(map: never, key: never): void
                /**
                 * Fetches the value stored for `key` in `map`.
                 * @since 1.7.7
                 * @param map A map
                 * @param key The key to query
                 */
                map_get(map: never, key: never): never
                /**
                 * Fetches the singleton empty #hb_map_t.
                 * @since 1.7.7
                 * @returns The empty #hb_map_t
                 */
                map_get_empty(): never
                /**
                 * Returns the number of key-value pairs in the map.
                 * @since 1.7.7
                 * @param map A map
                 * @returns The population of `map`
                 */
                map_get_population(map: never): number
                /**
                 * Tests whether `key` is an element of `map`.
                 * @since 1.7.7
                 * @param map A map
                 * @param key The key to query
                 * @returns `true` if `key` is found in `map`, `false` otherwise
                 */
                map_has(map: never, key: never): never
                /**
                 * Creates a hash representing `map`.
                 * @since 4.4.0
                 * @param map A map
                 * @returns A hash of `map`.
                 */
                map_hash(map: never): number
                /**
                 * Tests whether `map` is empty (contains no elements).
                 * @since 1.7.7
                 * @param map A map
                 * @returns `true` if `map` is empty
                 */
                map_is_empty(map: never): never
                /**
                 * Tests whether `map` and `other` are equal (contain the same
                 * elements).
                 * @since 4.3.0
                 * @param map A map
                 * @param other Another map
                 * @returns `true` if the two maps are equal, `false` otherwise.
                 */
                map_is_equal(map: never, other: never): never
                /**
                 * Add the keys of `map` to `keys`.
                 * @since 7.0.0
                 * @param map A map
                 * @param keys A set
                 */
                map_keys(map: never, keys: never): void
                /**
                 * Fetches the next key/value pair in `map`.
                 *
                 * Set `idx` to -1 to get started.
                 *
                 * If the map is modified during iteration, the behavior is undefined.
                 *
                 * The order in which the key/values are returned is undefined.
                 * @since 7.0.0
                 * @param map A map
                 * @returns `true` if there was a next value, `false` otherwise, Iterator internal state, Key retrieved, Value retrieved
                 */
                map_next(map: never, idx: number): [never, number, never, never]
                /**
                 * Stores `key`:@value in the map.
                 * @since 1.7.7
                 * @param map A map
                 * @param key The key to store in the map
                 * @param value The value to store for `key`
                 */
                map_set(map: never, key: never, value: never): void
                /**
                 * Add the contents of `other` to `map`.
                 * @since 7.0.0
                 * @param map A map
                 * @param other Another map
                 */
                map_update(map: never, other: never): void
                /**
                 * Add the values of `map` to `values`.
                 * @since 7.0.0
                 * @param map A map
                 * @param values A set
                 */
                map_values(map: never, values: never): void
                /**
                 * Gets the number of SVG documents in the face `SVG` table.
                 * @since 12.1.0
                 * @param face #hb_face_t to work upon.
                 * @returns number of SVG documents in the face.
                 */
                ot_color_get_svg_document_count(face: never): number
                /**
                 * Gets the glyph range covered by an `SVG`-table document index.
                 * @since 13.0.0
                 * @param face #hb_face_t to work upon.
                 * @param svg_document_index SVG document index.
                 * @returns `true` if `svg_document_index` is valid, `false` otherwise., output start glyph ID., output end glyph ID.
                 */
                ot_color_get_svg_document_glyph_range(face: never, svg_document_index: number): [never, never | null, never | null]
                /**
                 * Fetches a list of all color layers for the specified glyph index in the specified
                 * face. The list returned will begin at the offset provided.
                 * @since 2.1.0
                 * @param face #hb_face_t to work upon
                 * @param glyph The glyph index to query
                 * @param start_offset offset of the first layer to retrieve
                 * @returns Total number of layers available for the glyph index queried, The array of layers found
                 */
                ot_color_glyph_get_layers(face: never, glyph: never, start_offset: number): [number, never[] | null]
                /**
                 * Gets the `SVG`-table document index associated with a glyph.
                 * @since 12.1.0
                 * @param face #hb_face_t to work upon.
                 * @param glyph glyph ID to query.
                 * @returns `true` if `glyph` maps to an SVG document, `false` otherwise., output SVG document index.
                 */
                ot_color_glyph_get_svg_document_index(face: never, glyph: never): [never, number | null]
                /**
                 * Tests where a face includes COLRv1 paint
                 * data for `glyph`.
                 * @since 7.0.0
                 * @param face #hb_face_t to work upon
                 * @param glyph The glyph index to query
                 * @returns `true` if data found, `false` otherwise
                 */
                ot_color_glyph_has_paint(face: never, glyph: never): never
                /**
                 * Fetches the PNG image for a glyph. This function takes a font object, not a face object,
                 * as input. To get an optimally sized PNG blob, the PPEM values must be set on the `font`
                 * object. If PPEM is unset, the blob returned will be the largest PNG available.
                 *
                 * If the glyph has no PNG image, the singleton empty blob is returned.
                 * @since 2.1.0
                 * @param font #hb_font_t to work upon
                 * @param glyph a glyph index
                 * @returns An #hb_blob_t containing the PNG image for the glyph, if available
                 */
                ot_color_glyph_reference_png(font: never, glyph: never): never
                /**
                 * Fetches the SVG document for a glyph. The blob may be either plain text or gzip-encoded.
                 *
                 * If the glyph has no SVG document, the singleton empty blob is returned.
                 * @since 2.1.0
                 * @param face #hb_face_t to work upon
                 * @param glyph a svg glyph index
                 * @returns An #hb_blob_t containing the SVG document of the glyph, if available
                 */
                ot_color_glyph_reference_svg(face: never, glyph: never): never
                /**
                 * Tests whether a face includes a `COLR` table
                 * with data according to COLRv0.
                 * @since 2.1.0
                 * @param face #hb_face_t to work upon
                 * @returns `true` if data found, `false` otherwise
                 */
                ot_color_has_layers(face: never): never
                /**
                 * Tests where a face includes a `COLR` table
                 * with data according to COLRv1.
                 * @since 7.0.0
                 * @param face #hb_face_t to work upon
                 * @returns `true` if data found, `false` otherwise
                 */
                ot_color_has_paint(face: never): never
                /**
                 * Tests whether a face includes a `CPAL` color-palette table.
                 * @since 2.1.0
                 * @param face #hb_face_t to work upon
                 * @returns `true` if data found, `false` otherwise
                 */
                ot_color_has_palettes(face: never): never
                /**
                 * Tests whether a face has PNG glyph images (either in `CBDT` or `sbix` tables).
                 * @since 2.1.0
                 * @param face #hb_face_t to work upon
                 * @returns `true` if data found, `false` otherwise
                 */
                ot_color_has_png(face: never): never
                /**
                 * Tests whether a face includes any `SVG` glyph images.
                 * @since 2.1.0
                 * @param face #hb_face_t to work upon.
                 * @returns `true` if data found, `false` otherwise.
                 */
                ot_color_has_svg(face: never): never
                /**
                 * Fetches the `name` table Name ID that provides display names for
                 * the specified color in a face's `CPAL` color palette.
                 *
                 * Display names can be generic (e.g., "Background") or specific
                 * (e.g., "Eye color").
                 * @since 2.1.0
                 * @param face #hb_face_t to work upon
                 * @param color_index The index of the color
                 * @returns the Name ID found for the color.
                 */
                ot_color_palette_color_get_name_id(face: never, color_index: number): never
                /**
                 * Fetches a list of the colors in a color palette.
                 *
                 * After calling this function, `colors` will be filled with the palette
                 * colors. If `colors` is NULL, the function will just return the number
                 * of total colors without storing any actual colors; this can be used
                 * for allocating a buffer of suitable size before calling
                 * hb_ot_color_palette_get_colors() a second time.
                 *
                 * The RGBA values in the palette are unpremultiplied. See the
                 * OpenType spec [CPAL](https://learn.microsoft.com/en-us/typography/opentype/spec/cpal)
                 * section for details.
                 * @since 2.1.0
                 * @param face #hb_face_t to work upon
                 * @param palette_index the index of the color palette to query
                 * @param start_offset offset of the first color to retrieve
                 * @returns the total number of colors in the palette, The array of #hb_color_t records found
                 */
                ot_color_palette_get_colors(face: never, palette_index: number, start_offset: number): [number, never[] | null]
                /**
                 * Fetches the number of color palettes in a face.
                 * @since 2.1.0
                 * @param face #hb_face_t to work upon
                 * @returns the number of palettes found
                 */
                ot_color_palette_get_count(face: never): number
                /**
                 * Fetches the flags defined for a color palette.
                 * @since 2.1.0
                 * @param face #hb_face_t to work upon
                 * @param palette_index The index of the color palette
                 * @returns the #hb_ot_color_palette_flags_t of the requested color palette
                 */
                ot_color_palette_get_flags(face: never, palette_index: number): never
                /**
                 * Fetches the `name` table Name ID that provides display names for
                 * a `CPAL` color palette.
                 *
                 * Palette display names can be generic (e.g., "Default") or provide
                 * specific, themed names (e.g., "Spring", "Summer", "Fall", and "Winter").
                 * @since 2.1.0
                 * @param face #hb_face_t to work upon
                 * @param palette_index The index of the color palette
                 * @returns the Named ID found for the palette. If the requested palette has no name the result is #HB_OT_NAME_ID_INVALID.
                 */
                ot_color_palette_get_name_id(face: never, palette_index: number): never
                /**
                 * Fetches a bit field of `face`.
                 * @since 14.3.0
                 * @param face #hb_face_t to work upon
                 * @param tag tag of the bit field to fetch
                 * @returns the bit field, or zero if the font does not have it.
                 */
                ot_fetch_bits(face: never, tag: never): number
                /**
                 * Fetches a number of `face` in font units.
                 * @since 14.3.0
                 * @param face #hb_face_t to work upon
                 * @param tag tag of the number to fetch
                 * @returns the number, or zero if the font does not have it.
                 */
                ot_fetch_number(face: never, tag: never): number
                /**
                 * Sets the font functions to use when working with `font` to
                 * the HarfBuzz's native implementation. This is the default
                 * for fonts newly created.
                 * @since 0.9.28
                 * @param font #hb_font_t to work upon
                 */
                ot_font_set_funcs(font: never): void
                /**
                 * Fetches a list of all feature indexes in the specified face's GSUB table
                 * or GPOS table, underneath the specified scripts, languages, and features.
                 * If no list of scripts is provided, all scripts will be queried. If no list
                 * of languages is provided, all languages will be queried. If no list of
                 * features is provided, all features will be queried.
                 * @since 1.8.5
                 * @param face #hb_face_t to work upon
                 * @param table_tag #HB_OT_TAG_GSUB or #HB_OT_TAG_GPOS
                 * @param scripts The array of scripts to collect features for,   terminated by %HB_TAG_NONE
                 * @param languages The array of languages to collect features for,   terminated by %HB_TAG_NONE
                 * @param features The array of features to collect,   terminated by %HB_TAG_NONE
                 * @returns , The set of feature indexes found for the query
                 */
                ot_layout_collect_features(face: never, table_tag: never, scripts: never[] | null, languages: never[] | null, features: never[] | null): never
                /**
                 * Fetches the mapping from feature tags to feature indexes for
                 * the specified script and language.
                 * @since 8.1.0
                 * @param face #hb_face_t to work upon
                 * @param table_tag #HB_OT_TAG_GSUB or #HB_OT_TAG_GPOS
                 * @param script_index The index of the requested script tag
                 * @param language_index The index of the requested language tag
                 * @returns , The map of feature tag to feature index.
                 */
                ot_layout_collect_features_map(face: never, table_tag: never, script_index: number, language_index: number): never
                /**
                 * Fetches a list of all feature-lookup indexes in the specified face's GSUB
                 * table or GPOS table, underneath the specified scripts, languages, and
                 * features. If no list of scripts is provided, all scripts will be queried.
                 * If no list of languages is provided, all languages will be queried. If no
                 * list of features is provided, all features will be queried.
                 * @since 0.9.8
                 * @param face #hb_face_t to work upon
                 * @param table_tag #HB_OT_TAG_GSUB or #HB_OT_TAG_GPOS
                 * @param scripts The array of scripts to collect lookups for,   terminated by %HB_TAG_NONE
                 * @param languages The array of languages to collect lookups for,   terminated by %HB_TAG_NONE
                 * @param features The array of features to collect lookups for,   terminated by %HB_TAG_NONE
                 * @returns , The array of lookup indexes found for the query
                 */
                ot_layout_collect_lookups(face: never, table_tag: never, scripts: never[] | null, languages: never[] | null, features: never[] | null): never
                /**
                 * Fetches a list of the characters defined as having a variant under the specified
                 * "Character Variant" ("cvXX") feature tag.
                 * @since 2.0.0
                 * @param face #hb_face_t to work upon
                 * @param table_tag table tag to query, "GSUB" or "GPOS".
                 * @param feature_index index of feature to query.
                 * @param start_offset offset of the first character to retrieve
                 * @returns Number of total sample characters in the cvXX feature., A buffer pointer.              The Unicode codepoints of the characters for which this feature provides               glyph variants.
                 */
                ot_layout_feature_get_characters(face: never, table_tag: never, feature_index: number, start_offset: number): [number, never[] | null]
                /**
                 * Fetches a list of all lookups enumerated for the specified feature, in
                 * the specified face's GSUB table or GPOS table. The list returned will
                 * begin at the offset provided.
                 * @since 0.9.7
                 * @param face #hb_face_t to work upon
                 * @param table_tag #HB_OT_TAG_GSUB or #HB_OT_TAG_GPOS
                 * @param feature_index The index of the requested feature
                 * @param start_offset offset of the first lookup to retrieve
                 * @returns Total number of lookups., The array of lookup indexes found for the query
                 */
                ot_layout_feature_get_lookups(face: never, table_tag: never, feature_index: number, start_offset: number): [number, number[] | null]
                /**
                 * Fetches name indices from feature parameters for "Stylistic Set" ('ssXX') or
                 * "Character Variant" ('cvXX') features.
                 * @since 2.0.0
                 * @param face #hb_face_t to work upon
                 * @param table_tag table tag to query, "GSUB" or "GPOS".
                 * @param feature_index index of feature to query.
                 * @returns `true` if data found, `false` otherwise, The ‘name’ table name ID that specifies a string            for a user-interface label for this feature., The ‘name’ table name ID that specifies a string              that an application can use for tooltip text for this              feature., The ‘name’ table name ID that specifies sample text             that illustrates the effect of this feature., Number of named parameters., The first ‘name’ table name ID used to specify                  strings for user-interface labels for the feature                  parameters. (Must be zero if numParameters is zero.)
                 */
                ot_layout_feature_get_name_ids(face: never, table_tag: never, feature_index: number): [never, never | null, never | null, never | null, number | null, never | null]
                /**
                 * Fetches a list of all lookups enumerated for the specified feature, in
                 * the specified face's GSUB table or GPOS table, enabled at the specified
                 * variations index. The list returned will begin at the offset provided.
                 * @since 1.4.0
                 * @param face #hb_face_t to work upon
                 * @param table_tag #HB_OT_TAG_GSUB or #HB_OT_TAG_GPOS
                 * @param feature_index The index of the feature to query
                 * @param variations_index The index of the feature variation to query
                 * @param start_offset offset of the first lookup to retrieve
                 * @returns Total number of lookups., The array of lookups found for the query
                 */
                ot_layout_feature_with_variations_get_lookups(face: never, table_tag: never, feature_index: number, variations_index: number, start_offset: number): [number, number[] | null]
                /**
                 * Fetches a list of all attachment points for the specified glyph in the GDEF
                 * table of the face. The list returned will begin at the offset provided.
                 *
                 * Useful if the client program wishes to cache the list.
                 * @param face The #hb_face_t to work on
                 * @param glyph The #hb_codepoint_t code point to query
                 * @param start_offset offset of the first attachment point to retrieve
                 * @returns Total number of attachment points for `glyph`., The array of attachment points found for the query
                 */
                ot_layout_get_attach_points(face: never, glyph: never, start_offset: number): [number, number[] | null]
                /**
                 * Fetches a baseline value from the font.
                 * @since 2.6.0
                 * @param font a font
                 * @param baseline_tag a baseline tag
                 * @param direction text direction.
                 * @param script_tag script tag.
                 * @param language_tag language tag, currently unused.
                 * @returns `true` if found baseline value in the font., baseline value if found.
                 */
                ot_layout_get_baseline(font: never, baseline_tag: never, direction: never, script_tag: never, language_tag: never): [never, never | null]
                /**
                 * Fetches a baseline value from the font.
                 *
                 * This function is like hb_ot_layout_get_baseline() but takes
                 * #hb_script_t and #hb_language_t instead of OpenType #hb_tag_t.
                 * @since 8.0.0
                 * @param font a font
                 * @param baseline_tag a baseline tag
                 * @param direction text direction.
                 * @param script script.
                 * @param language language, currently unused.
                 * @returns `true` if found baseline value in the font., baseline value if found.
                 */
                ot_layout_get_baseline2(font: never, baseline_tag: never, direction: never, script: never, language: never | null): [never, never | null]
                /**
                 * Fetches a baseline value from the font, and synthesizes
                 * it if the font does not have it.
                 * @since 4.0.0
                 * @param font a font
                 * @param baseline_tag a baseline tag
                 * @param direction text direction.
                 * @param script_tag script tag.
                 * @param language_tag language tag, currently unused.
                 * @returns , baseline value if found.
                 */
                ot_layout_get_baseline_with_fallback(font: never, baseline_tag: never, direction: never, script_tag: never, language_tag: never): never
                /**
                 * Fetches a baseline value from the font, and synthesizes
                 * it if the font does not have it.
                 *
                 * This function is like hb_ot_layout_get_baseline_with_fallback() but takes
                 * #hb_script_t and #hb_language_t instead of OpenType #hb_tag_t.
                 * @since 8.0.0
                 * @param font a font
                 * @param baseline_tag a baseline tag
                 * @param direction text direction.
                 * @param script script.
                 * @param language language, currently unused.
                 * @returns , baseline value if found.
                 */
                ot_layout_get_baseline_with_fallback2(font: never, baseline_tag: never, direction: never, script: never, language: never | null): never
                /**
                 * Fetches script/language-specific font extents.  These values are
                 * looked up in the `BASE` table's `MinMax` records.
                 *
                 * If no such extents are found, the default extents for the font are
                 * fetched. As such, the return value of this function can for the
                 * most part be ignored.  Note that the per-script/language extents
                 * do not have a line-gap value, and the line-gap is set to zero in
                 * that case.
                 * @since 8.0.0
                 * @param font a font
                 * @param direction text direction.
                 * @param script_tag script tag.
                 * @param language_tag language tag.
                 * @returns `true` if found script/language-specific font extents., font extents if found.
                 */
                ot_layout_get_font_extents(font: never, direction: never, script_tag: never, language_tag: never): [never, never | null]
                /**
                 * Fetches script/language-specific font extents.  These values are
                 * looked up in the `BASE` table's `MinMax` records.
                 *
                 * If no such extents are found, the default extents for the font are
                 * fetched. As such, the return value of this function can for the
                 * most part be ignored.  Note that the per-script/language extents
                 * do not have a line-gap value, and the line-gap is set to zero in
                 * that case.
                 *
                 * This function is like hb_ot_layout_get_font_extents() but takes
                 * #hb_script_t and #hb_language_t instead of OpenType #hb_tag_t.
                 * @since 8.0.0
                 * @param font a font
                 * @param direction text direction.
                 * @param script script.
                 * @param language language.
                 * @returns `true` if found script/language-specific font extents., font extents if found.
                 */
                ot_layout_get_font_extents2(font: never, direction: never, script: never, language: never | null): [never, never | null]
                /**
                 * Fetches the GDEF class of the requested glyph in the specified face.
                 * @since 0.9.7
                 * @param face The #hb_face_t to work on
                 * @param glyph The #hb_codepoint_t code point to query
                 * @returns The #hb_ot_layout_glyph_class_t glyph class of the given code point in the GDEF table of the face.
                 */
                ot_layout_get_glyph_class(face: never, glyph: never): never
                /**
                 * Retrieves the set of all glyphs from the face that belong to the requested
                 * glyph class in the face's GDEF table.
                 * @since 0.9.7
                 * @param face The #hb_face_t to work on
                 * @param klass The #hb_ot_layout_glyph_class_t GDEF class to retrieve
                 * @returns , The #hb_set_t set of all glyphs belonging to the requested          class.
                 */
                ot_layout_get_glyphs_in_class(face: never, klass: never): never
                /**
                 * Fetches the dominant horizontal baseline tag used by `script`.
                 * @since 4.0.0
                 * @param script a script tag.
                 * @returns dominant baseline tag for the `script`.
                 */
                ot_layout_get_horizontal_baseline_tag_for_script(script: never): never
                /**
                 * Fetches a list of the caret positions defined for a ligature glyph in the GDEF
                 * table of the font. The list returned will begin at the offset provided.
                 *
                 * Note that a ligature that is formed from n characters will have n-1
                 * caret positions. The first character is not represented in the array,
                 * since its caret position is the glyph position.
                 *
                 * The positions returned by this function are 'unshaped', and will have to
                 * be fixed up for kerning that may be applied to the ligature glyph.
                 * @param font The #hb_font_t to work on
                 * @param direction The #hb_direction_t text direction to use
                 * @param glyph The #hb_codepoint_t code point to query
                 * @param start_offset offset of the first caret position to retrieve
                 * @returns Total number of ligature caret positions for `glyph`., The array of caret positions found for the query
                 */
                ot_layout_get_ligature_carets(font: never, direction: never, glyph: never, start_offset: number): [number, never[] | null]
                /**
                 * Fetches optical-size feature data (i.e., the `size` feature from GPOS). Note that
                 * the subfamily_id and the subfamily name string (accessible via the subfamily_name_id)
                 * as used here are defined as pertaining only to fonts within a font family that differ
                 * specifically in their respective size ranges; other ways to differentiate fonts within
                 * a subfamily are not covered by the `size` feature.
                 *
                 * For more information on this distinction, see the [`size` feature documentation](
                 * https://docs.microsoft.com/en-us/typography/opentype/spec/features_pt#tag-size).
                 * @since 0.9.10
                 * @param face #hb_face_t to work upon
                 * @returns `true` if data found, `false` otherwise, The design size of the face, The identifier of the face within the font subfamily, The ‘name’ table name ID of the face within the font subfamily, The minimum size of the recommended size range for the face, The maximum size of the recommended size range for the face
                 */
                ot_layout_get_size_params(face: never): [never, number, number, never, number, number]
                /**
                 * Tests whether a face has any glyph classes defined in its GDEF table.
                 * @param face #hb_face_t to work upon
                 * @returns `true` if data found, `false` otherwise
                 */
                ot_layout_has_glyph_classes(face: never): never
                /**
                 * Tests whether the specified face includes any GPOS positioning.
                 * @param face #hb_face_t to work upon
                 * @returns `true` if the face has GPOS data, `false` otherwise
                 */
                ot_layout_has_positioning(face: never): never
                /**
                 * Tests whether the specified face includes any GSUB substitutions.
                 * @since 0.6.0
                 * @param face #hb_face_t to work upon
                 * @returns `true` if data found, `false` otherwise
                 */
                ot_layout_has_substitution(face: never): never
                /**
                 * Fetches the index of a given feature tag in the specified face's GSUB table
                 * or GPOS table, underneath the specified script and language.
                 * @since 0.6.0
                 * @param face #hb_face_t to work upon
                 * @param table_tag #HB_OT_TAG_GSUB or #HB_OT_TAG_GPOS
                 * @param script_index The index of the requested script tag
                 * @param language_index The index of the requested language tag
                 * @param feature_tag #hb_tag_t of the feature tag requested
                 * @returns `true` if the feature is found, `false` otherwise, The index of the requested feature
                 */
                ot_layout_language_find_feature(face: never, table_tag: never, script_index: number, language_index: number, feature_tag: never): [never, number]
                /**
                 * Fetches a list of all features in the specified face's GSUB table
                 * or GPOS table, underneath the specified script and language. The list
                 * returned will begin at the offset provided.
                 * @since 0.6.0
                 * @param face #hb_face_t to work upon
                 * @param table_tag #HB_OT_TAG_GSUB or #HB_OT_TAG_GPOS
                 * @param script_index The index of the requested script tag
                 * @param language_index The index of the requested language tag
                 * @param start_offset offset of the first feature tag to retrieve
                 * @returns Total number of features., The array of feature indexes found for the query
                 */
                ot_layout_language_get_feature_indexes(face: never, table_tag: never, script_index: number, language_index: number, start_offset: number): [number, number[] | null]
                /**
                 * Fetches a list of all features in the specified face's GSUB table
                 * or GPOS table, underneath the specified script and language. The list
                 * returned will begin at the offset provided.
                 * @since 0.6.0
                 * @param face #hb_face_t to work upon
                 * @param table_tag #HB_OT_TAG_GSUB or #HB_OT_TAG_GPOS
                 * @param script_index The index of the requested script tag
                 * @param language_index The index of the requested language tag
                 * @param start_offset offset of the first feature tag to retrieve
                 * @returns Total number of feature tags., The array of #hb_tag_t feature tags found for the query
                 */
                ot_layout_language_get_feature_tags(face: never, table_tag: never, script_index: number, language_index: number, start_offset: number): [number, never[] | null]
                /**
                 * Fetches the tag of a requested feature index in the given face's GSUB or GPOS table,
                 * underneath the specified script and language.
                 * @since 0.9.30
                 * @param face #hb_face_t to work upon
                 * @param table_tag #HB_OT_TAG_GSUB or #HB_OT_TAG_GPOS
                 * @param script_index The index of the requested script tag
                 * @param language_index The index of the requested language tag
                 * @returns `true` if the feature is found, `false` otherwise, The index of the requested feature, The #hb_tag_t of the requested feature
                 */
                ot_layout_language_get_required_feature(face: never, table_tag: never, script_index: number, language_index: number): [never, number, never]
                /**
                 * Fetches the index of a requested feature in the given face's GSUB or GPOS table,
                 * underneath the specified script and language.
                 * @since 0.6.0
                 * @param face #hb_face_t to work upon
                 * @param table_tag #HB_OT_TAG_GSUB or #HB_OT_TAG_GPOS
                 * @param script_index The index of the requested script tag
                 * @param language_index The index of the requested language tag
                 * @returns `true` if the feature is found, `false` otherwise, The index of the requested feature
                 */
                ot_layout_language_get_required_feature_index(face: never, table_tag: never, script_index: number, language_index: number): [never, number]
                /**
                 * Collects alternates of glyphs from a given GSUB lookup index.
                 *
                 * For one-to-one GSUB glyph substitutions, this function collects the
                 * substituted glyph.
                 *
                 * For lookups that assign multiple alternates to a glyph, all alternate glyphs are collected.
                 *
                 * For other lookup types, nothing is performed and `false` is returned.
                 *
                 * The `alternate_count` mapping will contain the number of alternates for each glyph id.
                 * Upon entry, this mapping should contain the glyph ids as keys, and the number of alternates
                 * currently known for each glyph id as values.
                 *
                 * The `alternate_glyphs` mapping will contain the alternate glyph ids for each glyph id.
                 * The mapping is encoded in the following way, upon entry and after processing:
                 * If G is the glyph id, and A0, A1, ..., A(n-1) are the alternate glyph ids,
                 * the mapping will contain the following entries: (G + (i << 24)) -> A(i)
                 * for i = 0, 1, ..., n-1 where n is the number of alternates for G as per `alternate_count`.
                 * @since 12.1.0
                 * @param face a face.
                 * @param lookup_index index of the feature lookup to query.
                 * @returns `true` if alternates were collected, `false` otherwise., mapping from glyph index to number of alternates for that glyph., mapping from encoded glyph index and alternate index, to alternate glyph ids.
                 */
                ot_layout_lookup_collect_glyph_alternates(face: never, lookup_index: number, alternate_count: never, alternate_glyphs: never): [never, never, never]
                /**
                 * Fetches a list of all glyphs affected by the specified lookup in the
                 * specified face's GSUB table or GPOS table.
                 * @since 0.9.7
                 * @param face #hb_face_t to work upon
                 * @param table_tag #HB_OT_TAG_GSUB or #HB_OT_TAG_GPOS
                 * @param lookup_index The index of the feature lookup to query
                 * @returns , Array of glyphs preceding the substitution range, Array of input glyphs that would be substituted by the lookup, Array of glyphs following the substitution range, Array of glyphs that would be the substituted output of the lookup
                 */
                ot_layout_lookup_collect_glyphs(face: never, table_tag: never, lookup_index: number): [never | null, never | null, never | null, never | null]
                /**
                 * Fetches alternates of a glyph from a given GSUB lookup index. Note that for one-to-one GSUB
                 * glyph substitutions, this function fetches the substituted glyph.
                 * @since 2.6.8
                 * @param face a face.
                 * @param lookup_index index of the feature lookup to query.
                 * @param glyph a glyph id.
                 * @param start_offset starting offset.
                 * @returns Total number of alternates found in the specific lookup index for the given glyph id., A glyphs buffer.                    Alternate glyphs associated with the glyph id.
                 */
                ot_layout_lookup_get_glyph_alternates(face: never, lookup_index: number, glyph: never, start_offset: number): [number, never[] | null]
                /**
                 * Fetches the optical bound of a glyph positioned at the margin of text.
                 * The direction identifies which edge of the glyph to query.
                 * @since 5.3.0
                 * @param font a font.
                 * @param lookup_index index of the feature lookup to query.
                 * @param direction edge of the glyph to query.
                 * @param glyph a glyph id.
                 * @returns Adjustment value. Negative values mean the glyph will stick out of the margin.
                 */
                ot_layout_lookup_get_optical_bound(font: never, lookup_index: number, direction: never, glyph: never): never
                /**
                 * Compute the transitive closure of glyphs needed for a
                 * specified lookup.
                 * @since 0.9.7
                 * @param face #hb_face_t to work upon
                 * @param lookup_index index of the feature lookup to query
                 * @returns , Array of glyphs comprising the transitive closure of the lookup
                 */
                ot_layout_lookup_substitute_closure(face: never, lookup_index: number): never
                /**
                 * Tests whether a specified lookup in the specified face would
                 * trigger a substitution on the given glyph sequence.
                 * @since 0.9.7
                 * @param face #hb_face_t to work upon
                 * @param lookup_index The index of the lookup to query
                 * @param glyphs The sequence of glyphs to query for substitution
                 * @param glyphs_length The length of the glyph sequence
                 * @param zero_context #hb_bool_t indicating whether pre-/post-context are disallowed in substitutions
                 * @returns `true` if a substitution would be triggered, `false` otherwise
                 */
                ot_layout_lookup_would_substitute(face: never, lookup_index: number, glyphs: never, glyphs_length: number, zero_context: never): never
                /**
                 * Compute the transitive closure of glyphs needed for all of the
                 * provided lookups.
                 * @since 1.8.1
                 * @param face #hb_face_t to work upon
                 * @param lookups The set of lookups to query
                 * @returns , Array of glyphs comprising the transitive closure of the lookups
                 */
                ot_layout_lookups_substitute_closure(face: never, lookups: never): never
                /**
                 * Fetches the index of a given language tag in the specified face's GSUB table
                 * or GPOS table, underneath the specified script tag.
                 * @since 0.6.0
                 * @deprecated since 2.0.0
                 * @param face #hb_face_t to work upon
                 * @param table_tag #HB_OT_TAG_GSUB or #HB_OT_TAG_GPOS
                 * @param script_index The index of the requested script tag
                 * @param language_tag The #hb_tag_t of the requested language
                 * @param language_index The index of the requested language
                 * @returns `true` if the language tag is found, `false` otherwise
                 */
                ot_layout_script_find_language(face: never, table_tag: never, script_index: number, language_tag: never, language_index: number): never
                /**
                 * Fetches a list of language tags in the given face's GSUB or GPOS table, underneath
                 * the specified script index. The list returned will begin at the offset provided.
                 * @since 0.6.0
                 * @param face #hb_face_t to work upon
                 * @param table_tag #HB_OT_TAG_GSUB or #HB_OT_TAG_GPOS
                 * @param script_index The index of the requested script tag
                 * @param start_offset offset of the first language tag to retrieve
                 * @returns Total number of language tags., Array of language tags found in the table
                 */
                ot_layout_script_get_language_tags(face: never, table_tag: never, script_index: number, start_offset: number): [number, never[] | null]
                /**
                 * Fetches the index of the first language tag fom `language_tags` that is present
                 * in the specified face's GSUB or GPOS table, underneath the specified script
                 * index.
                 *
                 * If none of the given language tags is found, `false` is returned and
                 *  `language_index` is set to the default language index.
                 * @since 2.0.0
                 * @param face #hb_face_t to work upon
                 * @param table_tag #HB_OT_TAG_GSUB or #HB_OT_TAG_GPOS
                 * @param script_index The index of the requested script tag
                 * @param language_count The number of languages in the specified script
                 * @param language_tags The array of language tags
                 * @returns `true` if one of the given language tags is found, `false` otherwise, The index of the requested language
                 */
                ot_layout_script_select_language(face: never, table_tag: never, script_index: number, language_count: number, language_tags: never): [never, number]
                /**
                 * Fetches the index of the first language tag fom `language_tags` that is present
                 * in the specified face's GSUB or GPOS table, underneath the specified script
                 * index.
                 *
                 * If none of the given language tags is found, `false` is returned and
                 *  `language_index` is set to #HB_OT_LAYOUT_DEFAULT_LANGUAGE_INDEX and
                 *  `chosen_language` is set to #HB_TAG_NONE.
                 * @since 7.0.0
                 * @param face #hb_face_t to work upon
                 * @param table_tag #HB_OT_TAG_GSUB or #HB_OT_TAG_GPOS
                 * @param script_index The index of the requested script tag
                 * @param language_count The number of languages in the specified script
                 * @param language_tags The array of language tags
                 * @returns `true` if one of the given language tags is found, `false` otherwise, The index of the chosen language, #hb_tag_t of the chosen language
                 */
                ot_layout_script_select_language2(face: never, table_tag: never, script_index: number, language_count: number, language_tags: never): [never, number, never]
                /**
                 * Deprecated since 2.0.0
                 * @param face #hb_face_t to work upon
                 * @param table_tag #HB_OT_TAG_GSUB or #HB_OT_TAG_GPOS
                 * @param script_tags Array of #hb_tag_t script tags
                 * @returns , The index of the chosen script, #hb_tag_t of the chosen script
                 */
                ot_layout_table_choose_script(face: never, table_tag: never, script_tags: never): [never, number, never]
                /**
                 * Fetches a list of feature variations in the specified face's GSUB table
                 * or GPOS table, at the specified variation coordinates.
                 * @since 1.4.0
                 * @param face #hb_face_t to work upon
                 * @param table_tag #HB_OT_TAG_GSUB or #HB_OT_TAG_GPOS
                 * @param coords The variation coordinates to query
                 * @param num_coords The number of variation coordinates
                 * @returns `true` if feature variations were found, `false` otherwise., The array of feature variations found for the query
                 */
                ot_layout_table_find_feature_variations(face: never, table_tag: never, coords: number, num_coords: number): [never, number]
                /**
                 * Fetches the index if a given script tag in the specified face's GSUB table
                 * or GPOS table.
                 * @param face #hb_face_t to work upon
                 * @param table_tag #HB_OT_TAG_GSUB or #HB_OT_TAG_GPOS
                 * @param script_tag #hb_tag_t of the script tag requested
                 * @returns `true` if the script is found, `false` otherwise, The index of the requested script tag
                 */
                ot_layout_table_find_script(face: never, table_tag: never, script_tag: never): [never, number]
                /**
                 * Fetches a list of all feature tags in the given face's GSUB or GPOS table.
                 * Note that there might be duplicate feature tags, belonging to different
                 * script/language-system pairs of the table.
                 * @since 0.6.0
                 * @param face #hb_face_t to work upon
                 * @param table_tag #HB_OT_TAG_GSUB or #HB_OT_TAG_GPOS
                 * @param start_offset offset of the first feature tag to retrieve
                 * @returns Total number of feature tags., Array of feature tags found in the table
                 */
                ot_layout_table_get_feature_tags(face: never, table_tag: never, start_offset: number): [number, never[] | null]
                /**
                 * Fetches the total number of lookups enumerated in the specified
                 * face's GSUB table or GPOS table.
                 * @since 0.9.22
                 * @param face #hb_face_t to work upon
                 * @param table_tag #HB_OT_TAG_GSUB or #HB_OT_TAG_GPOS
                 * @returns Total number of lookups.
                 */
                ot_layout_table_get_lookup_count(face: never, table_tag: never): number
                /**
                 * Fetches a list of all scripts enumerated in the specified face's GSUB table
                 * or GPOS table. The list returned will begin at the offset provided.
                 * @param face #hb_face_t to work upon
                 * @param table_tag #HB_OT_TAG_GSUB or #HB_OT_TAG_GPOS
                 * @param start_offset offset of the first script tag to retrieve
                 * @returns Total number of script tags., The array of #hb_tag_t script tags found for the query
                 */
                ot_layout_table_get_script_tags(face: never, table_tag: never, start_offset: number): [number, never[] | null]
                /**
                 * Selects an OpenType script for `table_tag` from the `script_tags` array.
                 *
                 * If the table does not have any of the requested scripts, then `DFLT`,
                 * `dflt`, and `latn` tags are tried in that order. If the table still does not
                 * have any of these scripts, `script_index` is set to
                 * #HB_OT_LAYOUT_NO_SCRIPT_INDEX and `chosen_script` is set to #HB_TAG_NONE.
                 * @since 2.0.0
                 * @param face #hb_face_t to work upon
                 * @param table_tag #HB_OT_TAG_GSUB or #HB_OT_TAG_GPOS
                 * @param script_count Number of script tags in the array
                 * @param script_tags Array of #hb_tag_t script tags
                 * @returns `true` if one of the requested scripts is selected, `false` if a fallback script is selected or if no scripts are selected., The index of the requested script, #hb_tag_t of the requested script
                 */
                ot_layout_table_select_script(face: never, table_tag: never, script_count: number, script_tags: never): [never, number | null, never | null]
                /**
                 * Fetches the specified math constant. For most constants, the value returned
                 * is an #hb_position_t.
                 *
                 * However, if the requested constant is #HB_OT_MATH_CONSTANT_SCRIPT_PERCENT_SCALE_DOWN,
                 * #HB_OT_MATH_CONSTANT_SCRIPT_SCRIPT_PERCENT_SCALE_DOWN or
                 * #HB_OT_MATH_CONSTANT_RADICAL_DEGREE_BOTTOM_RAISE_PERCENT, then the return value is
                 * an integer between 0 and 100 representing that percentage.
                 * @since 1.3.3
                 * @param font #hb_font_t to work upon
                 * @param constant #hb_ot_math_constant_t the constant to retrieve
                 * @returns the requested constant or zero
                 */
                ot_math_get_constant(font: never, constant: never): never
                /**
                 * Fetches the GlyphAssembly for the specified font, glyph index, and direction.
                 * Returned are a list of #hb_ot_math_glyph_part_t glyph parts that can be
                 * used to draw the glyph and an italics-correction value (if one is defined
                 * in the font).
                 *
                 * <note>The `direction` parameter is only used to select between horizontal
                 * or vertical directions for the construction. Even though all #hb_direction_t
                 * values are accepted, only the result of #HB_DIRECTION_IS_HORIZONTAL is
                 * considered.</note>
                 * @since 1.3.3
                 * @param font #hb_font_t to work upon
                 * @param glyph The index of the glyph to stretch
                 * @param direction direction of the stretching (horizontal or vertical)
                 * @param start_offset offset of the first glyph part to retrieve
                 * @returns the total number of parts in the glyph assembly, the glyph parts returned, italics correction of the glyph assembly
                 */
                ot_math_get_glyph_assembly(font: never, glyph: never, direction: never, start_offset: number): [number, never[] | null, never]
                /**
                 * Fetches an italics-correction value (if one exists) for the specified
                 * glyph index.
                 * @since 1.3.3
                 * @param font #hb_font_t to work upon
                 * @param glyph The glyph index from which to retrieve the value
                 * @returns the italics correction of the glyph or zero
                 */
                ot_math_get_glyph_italics_correction(font: never, glyph: never): never
                /**
                 * Fetches the math kerning (cut-ins) value for the specified font, glyph index, and
                 *  `kern`.
                 *
                 * If the MathKern table is found, the function examines it to find a height
                 * value that is greater or equal to `correction_height`. If such a height
                 * value is found, corresponding kerning value from the table is returned. If
                 * no such height value is found, the last kerning value is returned.
                 * @since 1.3.3
                 * @param font #hb_font_t to work upon
                 * @param glyph The glyph index from which to retrieve the value
                 * @param kern The #hb_ot_math_kern_t from which to retrieve the value
                 * @param correction_height the correction height to use to determine the kerning.
                 * @returns requested kerning value or zero
                 */
                ot_math_get_glyph_kerning(font: never, glyph: never, kern: never, correction_height: never): never
                /**
                 * Fetches the raw MathKern (cut-in) data for the specified font, glyph index,
                 * and `kern`. The corresponding list of kern values and correction heights is
                 * returned as a list of #hb_ot_math_kern_entry_t structs.
                 *
                 * See also #hb_ot_math_get_glyph_kerning, which handles selecting the
                 * appropriate kern value for a given correction height.
                 *
                 * <note>For a glyph with `n` defined kern values (where `n` > 0), there are only
                 *  `n`−1 defined correction heights, as each correction height defines a boundary
                 * past which the next kern value should be selected. Therefore, only the
                 * #hb_ot_math_kern_entry_t.kern_value of the uppermost #hb_ot_math_kern_entry_t
                 * actually comes from the font; its corresponding
                 * #hb_ot_math_kern_entry_t.max_correction_height is always set to
                 * <code>INT32_MAX</code>.</note>
                 * @since 3.4.0
                 * @param font #hb_font_t to work upon
                 * @param glyph The glyph index from which to retrieve the kernings
                 * @param kern The #hb_ot_math_kern_t from which to retrieve the kernings
                 * @param start_offset offset of the first kern entry to retrieve
                 * @returns the total number of kern values available or zero, array of kern entries returned
                 */
                ot_math_get_glyph_kernings(font: never, glyph: never, kern: never, start_offset: number): [number, never[] | null]
                /**
                 * Fetches a top-accent-attachment value (if one exists) for the specified
                 * glyph index.
                 *
                 * For any glyph that does not have a top-accent-attachment value - that is,
                 * a glyph not covered by the `MathTopAccentAttachment` table (or, when
                 *  `font` has no `MathTopAccentAttachment` table or no `MATH` table, any
                 * glyph) - the function synthesizes a value, returning the position at
                 * one-half the glyph's advance width.
                 * @since 1.3.3
                 * @param font #hb_font_t to work upon
                 * @param glyph The glyph index from which to retrieve the value
                 * @returns the top accent attachment of the glyph or 0.5 * the advance               width of `glyph`
                 */
                ot_math_get_glyph_top_accent_attachment(font: never, glyph: never): never
                /**
                 * Fetches the MathGlyphConstruction for the specified font, glyph index, and
                 * direction. The corresponding list of size variants is returned as a list of
                 * #hb_ot_math_glyph_variant_t structs.
                 *
                 * <note>The `direction` parameter is only used to select between horizontal
                 * or vertical directions for the construction. Even though all #hb_direction_t
                 * values are accepted, only the result of #HB_DIRECTION_IS_HORIZONTAL is
                 * considered.</note>
                 * @since 1.3.3
                 * @param font #hb_font_t to work upon
                 * @param glyph The index of the glyph to stretch
                 * @param direction The direction of the stretching (horizontal or vertical)
                 * @param start_offset offset of the first variant to retrieve
                 * @returns the total number of size variants available or zero, array of variants returned
                 */
                ot_math_get_glyph_variants(font: never, glyph: never, direction: never, start_offset: number): [number, never[] | null]
                /**
                 * Fetches the MathVariants table for the specified font and returns the
                 * minimum overlap of connecting glyphs that are required to draw a glyph
                 * assembly in the specified direction.
                 *
                 * <note>The `direction` parameter is only used to select between horizontal
                 * or vertical directions for the construction. Even though all #hb_direction_t
                 * values are accepted, only the result of #HB_DIRECTION_IS_HORIZONTAL is
                 * considered.</note>
                 * @since 1.3.3
                 * @param font #hb_font_t to work upon
                 * @param direction direction of the stretching (horizontal or vertical)
                 * @returns requested minimum connector overlap or zero
                 */
                ot_math_get_min_connector_overlap(font: never, direction: never): never
                /**
                 * Tests whether a face has a `MATH` table.
                 * @since 1.3.3
                 * @param face #hb_face_t to test
                 * @returns `true` if the table is found, `false` otherwise
                 */
                ot_math_has_data(face: never): never
                /**
                 * Tests whether the given glyph index is an extended shape in the face.
                 * @since 1.3.3
                 * @param face #hb_face_t to work upon
                 * @param glyph The glyph index to test
                 * @returns `true` if the glyph is an extended shape, `false` otherwise
                 */
                ot_math_is_glyph_extended_shape(face: never, glyph: never): never
                /**
                 * Fetches all available feature types.
                 * @since 2.6.0
                 * @param face a face object
                 * @param start_offset iteration's start offset
                 * @returns Number of all available feature types., entries tags buffer
                 */
                ot_meta_get_entry_tags(face: never, start_offset: number): [number, never[] | null]
                /**
                 * It fetches metadata entry of a given tag from a font.
                 * @since 2.6.0
                 * @param face a #hb_face_t object.
                 * @param meta_tag tag of metadata you like to have.
                 * @returns A blob containing the blob.
                 */
                ot_meta_reference_entry(face: never, meta_tag: never): never
                /**
                 * Fetches metrics value corresponding to `metrics_tag` from `font`.
                 * @since 2.6.0
                 * @param font an #hb_font_t object.
                 * @param metrics_tag tag of metrics value you like to fetch.
                 * @returns Whether found the requested metrics in the font., result of metrics value from the font.
                 */
                ot_metrics_get_position(font: never, metrics_tag: never): [never, never]
                /**
                 * Fetches metrics value corresponding to `metrics_tag` from `font`,
                 * and synthesizes a value if it the value is missing in the font.
                 * @since 4.0.0
                 * @param font an #hb_font_t object.
                 * @param metrics_tag tag of metrics value you like to fetch.
                 * @returns , result of metrics value from the font.
                 */
                ot_metrics_get_position_with_fallback(font: never, metrics_tag: never): never
                /**
                 * Fetches metrics value corresponding to `metrics_tag` from `font` with the
                 * current font variation settings applied.
                 * @since 2.6.0
                 * @param font an #hb_font_t object.
                 * @param metrics_tag tag of metrics value you like to fetch.
                 * @returns The requested metric value.
                 */
                ot_metrics_get_variation(font: never, metrics_tag: never): number
                /**
                 * Fetches horizontal metrics value corresponding to `metrics_tag` from `font`
                 * with the current font variation settings applied.
                 * @since 2.6.0
                 * @param font an #hb_font_t object.
                 * @param metrics_tag tag of metrics value you like to fetch.
                 * @returns The requested metric value.
                 */
                ot_metrics_get_x_variation(font: never, metrics_tag: never): never
                /**
                 * Fetches vertical metrics value corresponding to `metrics_tag` from `font` with
                 * the current font variation settings applied.
                 * @since 2.6.0
                 * @param font an #hb_font_t object.
                 * @param metrics_tag tag of metrics value you like to fetch.
                 * @returns The requested metric value.
                 */
                ot_metrics_get_y_variation(font: never, metrics_tag: never): never
                /**
                 * Fetches a font name from the OpenType 'name' table.
                 * If `language` is #HB_LANGUAGE_INVALID, English ("en") is assumed.
                 * Returns string in UTF-16 encoding. A NUL terminator is always written
                 * for convenience, and isn't included in the output `text_size`.
                 * @since 2.1.0
                 * @param face font face.
                 * @param name_id OpenType name identifier to fetch.
                 * @param language language to fetch the name for.
                 * @returns full length of the requested string, or 0 if not found., buffer to write fetched name into.
                 */
                ot_name_get_utf16(face: never, name_id: never, language: never): [number, number[]]
                /**
                 * Fetches a font name from the OpenType 'name' table.
                 * If `language` is #HB_LANGUAGE_INVALID, English ("en") is assumed.
                 * Returns string in UTF-32 encoding. A NUL terminator is always written
                 * for convenience, and isn't included in the output `text_size`.
                 * @since 2.1.0
                 * @param face font face.
                 * @param name_id OpenType name identifier to fetch.
                 * @param language language to fetch the name for.
                 * @returns full length of the requested string, or 0 if not found., buffer to write fetched name into.
                 */
                ot_name_get_utf32(face: never, name_id: never, language: never): [number, number[]]
                /**
                 * Fetches a font name from the OpenType 'name' table.
                 * If `language` is #HB_LANGUAGE_INVALID, English ("en") is assumed.
                 * Returns string in UTF-8 encoding. A NUL terminator is always written
                 * for convenience, and isn't included in the output `text_size`.
                 * @since 2.1.0
                 * @param face font face.
                 * @param name_id OpenType name identifier to fetch.
                 * @param language language to fetch the name for.
                 * @returns full length of the requested string, or 0 if not found., buffer to write fetched name into.
                 */
                ot_name_get_utf8(face: never, name_id: never, language: never): [number, string[]]
                /**
                 * Enumerates all available name IDs and language combinations. Returned
                 * array is owned by the `face` and should not be modified.  It can be
                 * used as long as `face` is alive.
                 * @since 2.1.0
                 * @param face font face.
                 * @returns Array of available name entries.
                 */
                ot_name_list_names(face: never): never[]
                /**
                 * Returns the serial number of the current internal buffer format.
                 * See #HB_OT_SHAPE_BUFFER_FORMAT_SERIAL for more information.
                 * @since 13.2.0
                 * @returns The current buffer-format serial number.
                 */
                ot_shape_get_buffer_format_serial(): number
                /**
                 * Computes the transitive closure of glyphs needed for a specified
                 * input buffer under the given font and feature list. The closure is
                 * computed as a set, not as a list.
                 * @since 0.9.2
                 * @param font #hb_font_t to work upon
                 * @param buffer The input buffer to compute from
                 * @param features The features enabled on the buffer
                 * @returns , The #hb_set_t set of glyphs comprising the transitive closure of the query
                 */
                ot_shape_glyphs_closure(font: never, buffer: never, features: never[]): never
                /**
                 * Computes the complete set of GSUB or GPOS lookups that are applicable
                 * under a given `shape_plan`.
                 * @since 0.9.7
                 * @param shape_plan #hb_shape_plan_t to query
                 * @param table_tag GSUB or GPOS
                 * @returns , The #hb_set_t set of lookups returned
                 */
                ot_shape_plan_collect_lookups(shape_plan: never, table_tag: never): never
                /**
                 * Fetches the list of OpenType feature tags enabled for a shaping plan, if possible.
                 * @since 10.3.0
                 * @param shape_plan A shaping plan
                 * @param start_offset The index of first feature to retrieve
                 * @returns Total number of feature tagss., The array of enabled feature
                 */
                ot_shape_plan_get_feature_tags(shape_plan: never, start_offset: number): [number, never[]]
                /**
                 * Converts an #hb_language_t to an #hb_tag_t.
                 * @since 0.6.0
                 * @deprecated since 2.0.0 use hb_ot_tags_from_script_and_language() instead
                 * @param language an #hb_language_t to convert.
                 */
                ot_tag_from_language(language: never): never
                /**
                 * Converts a language tag to an #hb_language_t.
                 * @since 0.9.2
                 * @param tag an language tag
                 * @returns  The #hb_language_t corresponding to `tag`.
                 */
                ot_tag_to_language(tag: never): never | null
                /**
                 * Converts a script tag to an #hb_script_t.
                 * @param tag a script tag
                 * @returns The #hb_script_t corresponding to `tag`.
                 */
                ot_tag_to_script(tag: never): never
                /**
                 * Converts an #hb_script_t to script tags.
                 * @since 0.6.0
                 * @deprecated since 2.0.0 use hb_ot_tags_from_script_and_language() instead
                 * @param script an #hb_script_t to convert.
                 * @returns , output #hb_tag_t., output #hb_tag_t.
                 */
                ot_tags_from_script(script: never): [never, never]
                /**
                 * Converts an #hb_script_t and an #hb_language_t to script and language tags.
                 * @since 2.0.0
                 * @param script an #hb_script_t to convert.
                 * @param language an #hb_language_t to convert.
                 * @returns , maximum number of script tags to retrieve (IN) and actual number of script tags retrieved (OUT), array of size at least `script_count` to store the script tag results, maximum number of language tags to retrieve (IN) and actual number of language tags retrieved (OUT), array of size at least `language_count` to store the language tag results
                 */
                ot_tags_from_script_and_language(script: never, language: never | null, script_count: number, language_count: number): [number, never, number, never]
                /**
                 * Converts a script tag and a language tag to an #hb_script_t and an
                 * #hb_language_t.
                 * @since 2.0.0
                 * @param script_tag a script tag
                 * @param language_tag a language tag
                 * @returns , the #hb_script_t corresponding to `script_tag`., the #hb_language_t corresponding to `script_tag` and `language_tag`.
                 */
                ot_tags_to_script_and_language(script_tag: never, language_tag: never): [never, never]
                /**
                 * Fetches the variation-axis information corresponding to the specified axis tag
                 * in the specified face.
                 * @since 1.4.2
                 * @deprecated since 2.2.0 use hb_ot_var_find_axis_info() instead
                 * @param face #hb_face_t to work upon
                 * @param axis_tag The #hb_tag_t of the variation axis to query
                 * @param axis_index The index of the variation axis
                 * @returns , The #hb_ot_var_axis_info_t of the axis tag queried
                 */
                ot_var_find_axis(face: never, axis_tag: never, axis_index: number): [never, never]
                /**
                 * Fetches the variation-axis information corresponding to the specified axis tag
                 * in the specified face.
                 * @since 2.2.0
                 * @param face #hb_face_t to work upon
                 * @param axis_tag The #hb_tag_t of the variation axis to query
                 * @returns `true` if data found, `false` otherwise, The #hb_ot_var_axis_info_t of the axis tag queried
                 */
                ot_var_find_axis_info(face: never, axis_tag: never): [never, never]
                /**
                 * Fetches a list of all variation axes in the specified face. The list returned will begin
                 * at the offset provided.
                 * @since 1.4.2
                 * @deprecated since 2.2.0 use hb_ot_var_get_axis_infos() instead
                 * @param face #hb_face_t to work upon
                 * @param start_offset offset of the first lookup to retrieve
                 * @returns , The array of variation axes found
                 */
                ot_var_get_axes(face: never, start_offset: number): [number, never[] | null]
                /**
                 * Fetches the number of OpenType variation axes included in the face.
                 * @since 1.4.2
                 * @param face The #hb_face_t to work on
                 * @returns the number of variation axes defined
                 */
                ot_var_get_axis_count(face: never): number
                /**
                 * Fetches a list of all variation axes in the specified face. The list returned will begin
                 * at the offset provided.
                 * @since 2.2.0
                 * @param face #hb_face_t to work upon
                 * @param start_offset offset of the first lookup to retrieve
                 * @returns the number of variation axes in the face, The array of variation axes found
                 */
                ot_var_get_axis_infos(face: never, start_offset: number): [number, never[] | null]
                /**
                 * Fetches the number of named instances included in the face.
                 * @since 2.2.0
                 * @param face The #hb_face_t to work on
                 * @returns the number of named instances defined
                 */
                ot_var_get_named_instance_count(face: never): number
                /**
                 * Tests whether a face includes any OpenType variation data in the `fvar` table.
                 * @since 1.4.2
                 * @param face The #hb_face_t to work on
                 * @returns `true` if data found, `false` otherwise
                 */
                ot_var_has_data(face: never): never
                /**
                 * Fetches the design-space coordinates corresponding to the given
                 * named instance in the face.
                 * @since 2.2.0
                 * @param face The #hb_face_t to work on
                 * @param instance_index The index of the named instance to query
                 * @returns the number of variation axes in the face, The array of coordinates found for the query
                 */
                ot_var_named_instance_get_design_coords(face: never, instance_index: number): [number, number[] | null]
                /**
                 * Fetches the `name` table Name ID that provides display names for
                 * the "PostScript name" defined for the given named instance in the face.
                 * @since 2.2.0
                 * @param face The #hb_face_t to work on
                 * @param instance_index The index of the named instance to query
                 * @returns the Name ID found for the PostScript name
                 */
                ot_var_named_instance_get_postscript_name_id(face: never, instance_index: number): never
                /**
                 * Fetches the `name` table Name ID that provides display names for
                 * the "Subfamily name" defined for the given named instance in the face.
                 * @since 2.2.0
                 * @param face The #hb_face_t to work on
                 * @param instance_index The index of the named instance to query
                 * @returns the Name ID found for the Subfamily name
                 */
                ot_var_named_instance_get_subfamily_name_id(face: never, instance_index: number): never
                /**
                 * Normalizes the given design-space coordinates. The minimum and maximum
                 * values for the axis are mapped to the interval [-1,1], with the default
                 * axis value mapped to 0.
                 *
                 * The normalized values have 14 bits of fixed-point sub-integer precision as per
                 * OpenType specification.
                 *
                 * Any additional scaling defined in the face's `avar` table is also
                 * applied, as described at https://docs.microsoft.com/en-us/typography/opentype/spec/avar
                 *
                 * Note: `coords_length` must be the same as the number of axes in the face, as
                 * for example returned by hb_ot_var_get_axis_count().
                 * Otherwise, the behavior is undefined.
                 * @since 1.4.2
                 * @param face The #hb_face_t to work on
                 * @param coords_length The length of the coordinate array
                 * @param design_coords The design-space coordinates to normalize
                 * @returns , The normalized coordinates
                 */
                ot_var_normalize_coords(face: never, coords_length: number, design_coords: number): number
                /**
                 * Normalizes all of the coordinates in the given list of variation axes.
                 * @since 1.4.2
                 * @param face The #hb_face_t to work on
                 * @param variations The array of variations to normalize
                 * @param variations_length The number of variations to normalize
                 * @returns , The array of normalized coordinates
                 */
                ot_var_normalize_variations(face: never, variations: never, variations_length: number): number[]
                /**
                 * Perform a "color" paint operation.
                 * @since 7.0.0
                 * @param funcs paint functions
                 * @param paint_data associated data passed by the caller
                 * @param is_foreground whether the color is the foreground
                 * @param color The color to use
                 */
                paint_color(funcs: never, paint_data: never | null, is_foreground: never, color: never): void
                /**
                 * Perform a "color-glyph" paint operation.
                 * @since 8.2.0
                 * @param funcs paint functions
                 * @param paint_data associated data passed by the caller
                 * @param glyph the glyph ID
                 * @param font the font
                 */
                paint_color_glyph(funcs: never, paint_data: never | null, glyph: never, font: never): never
                /**
                 * Gets the custom palette override color for `color_index`.
                 * @since 7.0.0
                 * @param funcs paint functions.
                 * @param paint_data associated data passed by the caller.
                 * @param color_index color index to fetch.
                 * @returns `true` if a custom color is provided, `false` otherwise., fetched color.
                 */
                paint_custom_palette_color(funcs: never, paint_data: never | null, color_index: number): [never, never]
                /**
                 * Perform a "fill-glyph" paint operation: fill the glyph's shape
                 * with a solid color.
                 * @since 14.3.0
                 * @param funcs paint functions
                 * @param paint_data associated data passed by the caller
                 * @param glyph the glyph ID
                 * @param font the font
                 * @param is_foreground whether the color is the foreground
                 * @param color The color to use
                 */
                paint_fill_glyph(funcs: never, paint_data: never | null, glyph: never, font: never, is_foreground: never, color: never): void
                /**
                 * Creates a new #hb_paint_funcs_t structure of paint functions.
                 *
                 * The initial reference count of 1 should be released with hb_paint_funcs_destroy()
                 * when you are done using the #hb_paint_funcs_t. This function never returns
                 * `NULL`. If memory cannot be allocated, a special singleton #hb_paint_funcs_t
                 * object will be returned.
                 * @since 7.0.0
                 * @returns the paint-functions structure
                 */
                paint_funcs_create(): never
                /**
                 * Fetches the singleton empty paint-functions structure.
                 * @since 7.0.0
                 * @returns The empty paint-functions structure
                 */
                paint_funcs_get_empty(): never
                /**
                 * Tests whether a paint-functions structure is immutable.
                 * @since 7.0.0
                 * @param funcs The paint-functions structure
                 * @returns `true` if `funcs` is immutable, `false` otherwise
                 */
                paint_funcs_is_immutable(funcs: never): never
                /**
                 * Makes a paint-functions structure immutable.
                 *
                 * After this call, all attempts to set one of the callbacks
                 * on `funcs` will fail.
                 * @since 7.0.0
                 * @param funcs The paint-functions structure
                 */
                paint_funcs_make_immutable(funcs: never): void
                /**
                 * Sets the paint-color callback on the paint functions struct.
                 * @since 7.0.0
                 * @param funcs A paint functions struct
                 * @param func The paint-color callback
                 */
                paint_funcs_set_color_func(funcs: never, func: never): void
                /**
                 * Sets the color-glyph callback on the paint functions struct.
                 * @since 8.2.0
                 * @param funcs A paint functions struct
                 * @param func The color-glyph callback
                 */
                paint_funcs_set_color_glyph_func(funcs: never, func: never): void
                /**
                 * Sets the custom-palette-color callback on `funcs`.
                 * @since 7.0.0
                 * @param funcs a paint functions struct.
                 * @param func custom-palette-color callback.
                 */
                paint_funcs_set_custom_palette_color_func(funcs: never, func: never): void
                /**
                 * Sets the fill-glyph callback on the paint functions struct.
                 * @since 14.3.0
                 * @param funcs A paint functions struct
                 * @param func The fill-glyph callback
                 */
                paint_funcs_set_fill_glyph_func(funcs: never, func: never): void
                /**
                 * Sets the paint-image callback on the paint functions struct.
                 * @since 7.0.0
                 * @param funcs A paint functions struct
                 * @param func The paint-image callback
                 */
                paint_funcs_set_image_func(funcs: never, func: never): void
                /**
                 * Sets the linear-gradient callback on the paint functions struct.
                 * @since 7.0.0
                 * @param funcs A paint functions struct
                 * @param func The linear-gradient callback
                 */
                paint_funcs_set_linear_gradient_func(funcs: never, func: never): void
                /**
                 * Sets the pop-clip callback on the paint functions struct.
                 * @since 7.0.0
                 * @param funcs A paint functions struct
                 * @param func The pop-clip callback
                 */
                paint_funcs_set_pop_clip_func(funcs: never, func: never): void
                /**
                 * Sets the pop-group callback on the paint functions struct.
                 * @since 7.0.0
                 * @param funcs A paint functions struct
                 * @param func The pop-group callback
                 */
                paint_funcs_set_pop_group_func(funcs: never, func: never): void
                /**
                 * Sets the pop-transform callback on the paint functions struct.
                 * @since 7.0.0
                 * @param funcs A paint functions struct
                 * @param func The pop-transform callback
                 */
                paint_funcs_set_pop_transform_func(funcs: never, func: never): void
                /**
                 * Sets the push-clip-glyph callback on the paint functions struct.
                 * @since 7.0.0
                 * @param funcs A paint functions struct
                 * @param func The push-clip-glyph callback
                 */
                paint_funcs_set_push_clip_glyph_func(funcs: never, func: never): void
                /**
                 * Sets the push-clip-path-end callback on the paint functions struct.
                 * @since 14.2.0
                 * @param funcs A paint functions struct
                 * @param func The push-clip-path-end callback
                 */
                paint_funcs_set_push_clip_path_end_func(funcs: never, func: never): void
                /**
                 * Sets the push-clip-path-start callback on the paint functions struct.
                 * @since 14.2.0
                 * @param funcs A paint functions struct
                 * @param func The push-clip-path-start callback
                 */
                paint_funcs_set_push_clip_path_start_func(funcs: never, func: never): void
                /**
                 * Sets the push-clip-rect callback on the paint functions struct.
                 * @since 7.0.0
                 * @param funcs A paint functions struct
                 * @param func The push-clip-rectangle callback
                 */
                paint_funcs_set_push_clip_rectangle_func(funcs: never, func: never): void
                /**
                 * Sets the push-group-for callback on the paint functions struct.
                 * @since 14.2.0
                 * @param funcs A paint functions struct
                 * @param func The push-group-for callback
                 */
                paint_funcs_set_push_group_for_func(funcs: never, func: never): void
                /**
                 * Sets the push-group callback on the paint functions struct.
                 * @since 7.0.0
                 * @param funcs A paint functions struct
                 * @param func The push-group callback
                 */
                paint_funcs_set_push_group_func(funcs: never, func: never): void
                /**
                 * Sets the push-transform callback on the paint functions struct.
                 * @since 7.0.0
                 * @param funcs A paint functions struct
                 * @param func The push-transform callback
                 */
                paint_funcs_set_push_transform_func(funcs: never, func: never): void
                /**
                 * Sets the radial-gradient callback on the paint functions struct.
                 * @since 7.0.0
                 * @param funcs A paint functions struct
                 * @param func The radial-gradient callback
                 */
                paint_funcs_set_radial_gradient_func(funcs: never, func: never): void
                /**
                 * Sets the sweep-gradient callback on the paint functions struct.
                 * @since 7.0.0
                 * @param funcs A paint functions struct
                 * @param func The sweep-gradient callback
                 */
                paint_funcs_set_sweep_gradient_func(funcs: never, func: never): void
                /**
                 * Perform a "image" paint operation.
                 * @since 7.0.0
                 * @param funcs paint functions
                 * @param paint_data associated data passed by the caller
                 * @param image image data
                 * @param width width of the raster image in pixels, or 0
                 * @param height height of the raster image in pixels, or 0
                 * @param format the image format as a tag
                 * @param slant Deprecated. set to 0.0
                 * @param extents the extents of the glyph
                 */
                paint_image(funcs: never, paint_data: never | null, image: never, width: number, height: number, format: never, slant: number, extents: never | null): void
                /**
                 * Perform a "linear-gradient" paint operation.
                 * @since 7.0.0
                 * @param funcs paint functions
                 * @param paint_data associated data passed by the caller
                 * @param color_line Color information for the gradient
                 * @param x0 X coordinate of the first point
                 * @param y0 Y coordinate of the first point
                 * @param x1 X coordinate of the second point
                 * @param y1 Y coordinate of the second point
                 * @param x2 X coordinate of the third point
                 * @param y2 Y coordinate of the third point
                 */
                paint_linear_gradient(funcs: never, paint_data: never | null, color_line: never, x0: number, y0: number, x1: number, y1: number, x2: number, y2: number): void
                /**
                 * Sorts `stops` by offset and rescales offsets into [0, 1] in
                 * place.  Writes the original (min, max) to `min` / `max` so the
                 * caller can shift the gradient geometry (axis endpoints for
                 * linear, centers+radii for radial, start+end angles for sweep)
                 * to keep the rendered gradient visually unchanged after the
                 * rescale.  Empty input is safe: both out-parameters set to 0.
                 * @since 14.2.0
                 * @returns , color stops., original minimum offset., original maximum offset.
                 */
                paint_normalize_color_line(stops: never[]): [never[], number, number]
                /**
                 * Perform a "pop-clip" paint operation.
                 * @since 7.0.0
                 * @param funcs paint functions
                 * @param paint_data associated data passed by the caller
                 */
                paint_pop_clip(funcs: never, paint_data: never | null): void
                /**
                 * Perform a "pop-group" paint operation.
                 * @since 7.0.0
                 * @param funcs paint functions
                 * @param paint_data associated data passed by the caller
                 * @param mode the compositing mode to use
                 */
                paint_pop_group(funcs: never, paint_data: never | null, mode: never): void
                /**
                 * Perform a "pop-transform" paint operation.
                 * @since 7.0.0
                 * @param funcs paint functions
                 * @param paint_data associated data passed by the caller
                 */
                paint_pop_transform(funcs: never, paint_data: never | null): void
                /**
                 * Perform a "push-clip-glyph" paint operation.
                 * @since 7.0.0
                 * @param funcs paint functions
                 * @param paint_data associated data passed by the caller
                 * @param glyph the glyph ID
                 * @param font the font
                 */
                paint_push_clip_glyph(funcs: never, paint_data: never | null, glyph: never, font: never): void
                /**
                 * Signal that the arbitrary-clip path started by
                 * hb_paint_push_clip_path_start() is fully drawn.  The
                 * accumulated path now acts as a clip on the paint context
                 * until a matching hb_paint_pop_clip() call.
                 * @since 14.2.0
                 * @param funcs paint functions
                 * @param paint_data associated data passed by the caller
                 */
                paint_push_clip_path_end(funcs: never, paint_data: never | null): void
                /**
                 * Begin clipping to an arbitrary path.  Returns an
                 * #hb_draw_funcs_t owned by the backend (the caller must not
                 * free it) that the caller uses to emit the clip outline via
                 * hb_draw_*() calls, using the returned `draw_data` as the
                 * draw data.  The returned draw funcs and draw data are only
                 * valid until the matching hb_paint_push_clip_path_end() call;
                 * no other paint calls should be made between start and end
                 * except hb_draw_*() on the returned funcs.  Finish the path
                 * with hb_paint_push_clip_path_end(); pop the clip later
                 * with hb_paint_pop_clip().
                 *
                 * Usage:
                 *
                 * |[<!-- language="plain" -->
                 * hb_draw_funcs_t *df = hb_paint_push_clip_path_start (pf, pd, &dd);
                 * hb_draw_move_to (df, dd, NULL, ...);
                 * hb_draw_line_to (df, dd, NULL, ...);
                 * ...
                 * hb_draw_close_path (df, dd, NULL);
                 * hb_paint_push_clip_path_end (pf, pd);
                 * /&ast; paint ops here are clipped to the emitted path &ast;/
                 * hb_paint_pop_clip (pf, pd);
                 * ]|
                 * @since 14.2.0
                 * @param funcs paint functions
                 * @param paint_data associated data passed by the caller
                 * @returns draw funcs that accumulate   the clip path, or `NULL` if the backend does not implement   arbitrary-path clipping., location to receive the draw data   the caller should pass alongside the returned draw funcs.
                 */
                paint_push_clip_path_start(funcs: never, paint_data: never | null): [never, never | null]
                /**
                 * Perform a "push-clip-rect" paint operation.
                 * @since 7.0.0
                 * @param funcs paint functions
                 * @param paint_data associated data passed by the caller
                 * @param xmin min X for the rectangle
                 * @param ymin min Y for the rectangle
                 * @param xmax max X for the rectangle
                 * @param ymax max Y for the rectangle
                 */
                paint_push_clip_rectangle(funcs: never, paint_data: never | null, xmin: number, ymin: number, xmax: number, ymax: number): void
                /**
                 * Push the transform reflecting the font's scale and slant
                 * settings onto the paint functions.
                 * @since 11.0.0
                 * @param funcs paint functions
                 * @param paint_data associated data passed by the caller
                 * @param font a font
                 */
                paint_push_font_transform(funcs: never, paint_data: never | null, font: never): void
                /**
                 * Perform a "push-group" paint operation.
                 * @since 7.0.0
                 * @param funcs paint functions
                 * @param paint_data associated data passed by the caller
                 */
                paint_push_group(funcs: never, paint_data: never | null): void
                /**
                 * Perform a "push-group" paint operation, with the compositing
                 * mode known in advance.  By default, this calls
                 * hb_paint_push_group().
                 * @since 14.2.0
                 * @param funcs paint functions
                 * @param paint_data associated data passed by the caller
                 * @param mode the compositing mode that will be used when the group is popped
                 */
                paint_push_group_for(funcs: never, paint_data: never | null, mode: never): void
                /**
                 * Push the inverse of the transform reflecting the font's
                 * scale and slant settings onto the paint functions.
                 * @since 11.0.0
                 * @param funcs paint functions
                 * @param paint_data associated data passed by the caller
                 * @param font a font
                 */
                paint_push_inverse_font_transform(funcs: never, paint_data: never | null, font: never): void
                /**
                 * Perform a "push-transform" paint operation.
                 * @since 7.0.0
                 * @param funcs paint functions
                 * @param paint_data associated data passed by the caller
                 * @param xx xx component of the transform matrix
                 * @param yx yx component of the transform matrix
                 * @param xy xy component of the transform matrix
                 * @param yy yy component of the transform matrix
                 * @param dx dx component of the transform matrix
                 * @param dy dy component of the transform matrix
                 */
                paint_push_transform(funcs: never, paint_data: never | null, xx: number, yx: number, xy: number, yy: number, dx: number, dy: number): void
                /**
                 * Perform a "radial-gradient" paint operation.
                 * @since 7.0.0
                 * @param funcs paint functions
                 * @param paint_data associated data passed by the caller
                 * @param color_line Color information for the gradient
                 * @param x0 X coordinate of the first circle's center
                 * @param y0 Y coordinate of the first circle's center
                 * @param r0 radius of the first circle
                 * @param x1 X coordinate of the second circle's center
                 * @param y1 Y coordinate of the second circle's center
                 * @param r1 radius of the second circle
                 */
                paint_radial_gradient(funcs: never, paint_data: never | null, color_line: never, x0: number, y0: number, r0: number, x1: number, y1: number, r1: number): void
                /**
                 * Reduces a COLRv1 linear gradient's 3-anchor spec (P0=color
                 * stop 0, P1=color stop 1, P2=rotation reference) to the
                 * 2-point axis (P0, P1') used by SVG / cairo / most software
                 * renderers.  P1' is the foot of P1 on the line through P0
                 * perpendicular to (P2 - P0); the resulting axis is the
                 * gradient's actual direction (perpendicular to the rotation
                 * line).  Degenerate (P0 == P2) passes through unchanged.
                 * @since 14.2.0
                 * @param x0 x coordinate of P0 (color stop 0).
                 * @param y0 y coordinate of P0 (color stop 0).
                 * @param x1 x coordinate of P1 (color stop 1).
                 * @param y1 y coordinate of P1 (color stop 1).
                 * @param x2 x coordinate of P2 (rotation reference).
                 * @param y2 y coordinate of P2 (rotation reference).
                 * @returns , x coordinate of the resulting axis start., y coordinate of the resulting axis start., x coordinate of the resulting axis end., y coordinate of the resulting axis end.
                 */
                paint_reduce_linear_anchors(x0: number, y0: number, x1: number, y1: number, x2: number, y2: number): [number, number, number, number]
                /**
                 * Perform a "sweep-gradient" paint operation.
                 * @since 7.0.0
                 * @param funcs paint functions
                 * @param paint_data associated data passed by the caller
                 * @param color_line Color information for the gradient
                 * @param x0 X coordinate of the circle's center
                 * @param y0 Y coordinate of the circle's center
                 * @param start_angle the start angle
                 * @param end_angle the end angle
                 */
                paint_sweep_gradient(funcs: never, paint_data: never | null, color_line: never, x0: number, y0: number, start_angle: number, end_angle: number): void
                /**
                 * Iterates the full 0..2π sweep produced by a color-stop list,
                 * invoking `emit_patch` once per (start, end) angular segment.
                 * Handles #HB_PAINT_EXTEND_PAD, #HB_PAINT_EXTEND_REPEAT, and
                 * #HB_PAINT_EXTEND_REFLECT.  Stops must be pre-sorted by
                 * offset; use hb_paint_normalize_color_line() first if they
                 * aren't.
                 * @since 14.2.0
                 * @param extend extend mode.
                 * @param start_angle sweep start angle, in radians.
                 * @param end_angle sweep end angle, in radians.
                 * @param emit_patch callback invoked once per tile.
                 * @returns , color stops (sorted, offsets in [0,1]).
                 */
                paint_sweep_gradient_tiles(stops: never[], extend: never, start_angle: number, end_angle: number, emit_patch: never): never[]
                /**
                 * Reallocates the memory pointed to by `ptr` to `size` bytes, using the
                 * allocator set at compile-time. Typically just realloc().
                 * @since 11.0.0
                 * @param ptr The pointer to the memory to reallocate.
                 * @param size The new size of the memory.
                 * @returns A pointer to the reallocated memory.
                 */
                realloc(ptr: never | null, size: number): never | null
                /**
                 * Converts an ISO 15924 script tag to a corresponding #hb_script_t.
                 * @since 0.9.2
                 * @param tag an #hb_tag_t representing an ISO 15924 tag.
                 * @returns An #hb_script_t corresponding to the ISO 15924 tag.
                 */
                script_from_iso15924_tag(tag: never): never
                /**
                 * Converts a string `str` representing an ISO 15924 script tag to a
                 * corresponding #hb_script_t. Shorthand for hb_tag_from_string() then
                 * hb_script_from_iso15924_tag().
                 * @since 0.9.2
                 * @param str a string representing an       ISO 15924 tag.
                 * @returns An #hb_script_t corresponding to the ISO 15924 tag.
                 */
                script_from_string(str: Uint8Array): never
                /**
                 * Fetches the #hb_direction_t of a script when it is
                 * set horizontally. All right-to-left scripts will return
                 * #HB_DIRECTION_RTL. All left-to-right scripts will return
                 * #HB_DIRECTION_LTR.
                 *
                 * Scripts that can be written either right-to-left or
                 * left-to-right will return #HB_DIRECTION_INVALID.
                 *
                 * Unknown scripts will return #HB_DIRECTION_LTR.
                 * @since 0.9.2
                 * @param script The #hb_script_t to query
                 * @returns The horizontal #hb_direction_t of `script`
                 */
                script_get_horizontal_direction(script: never): never
                /**
                 * Converts an #hb_script_t to a corresponding ISO 15924 script tag.
                 * @since 0.9.2
                 * @param script an #hb_script_t to convert.
                 * @returns An #hb_tag_t representing an ISO 15924 script tag.
                 */
                script_to_iso15924_tag(script: never): never
                /**
                 * Checks the equality of two #hb_segment_properties_t's.
                 * @since 0.9.7
                 * @param a first #hb_segment_properties_t to compare.
                 * @param b second #hb_segment_properties_t to compare.
                 * @returns `true` if all properties of `a` equal those of `b`, `false` otherwise.
                 */
                segment_properties_equal(a: never, b: never): never
                /**
                 * Creates a hash representing `p`.
                 * @since 0.9.7
                 * @param p #hb_segment_properties_t to hash.
                 * @returns A hash of `p`.
                 */
                segment_properties_hash(p: never): number
                /**
                 * Fills in missing fields of `p` from `src` in a considered manner.
                 *
                 * First, if `p` does not have direction set, direction is copied from `src`.
                 *
                 * Next, if `p` and `src` have the same direction (which can be unset), if `p`
                 * does not have script set, script is copied from `src`.
                 *
                 * Finally, if `p` and `src` have the same direction and script (which either
                 * can be unset), if `p` does not have language set, language is copied from
                 *  `src`.
                 * @since 3.3.0
                 * @param p #hb_segment_properties_t to fill in.
                 * @param src #hb_segment_properties_t to fill in from.
                 */
                segment_properties_overlay(p: never, src: never): void
                /**
                 * Adds `codepoint` to `set`.
                 * @since 0.9.2
                 * @param set A set
                 * @param codepoint The element to add to `set`
                 */
                set_add(set: never, codepoint: never): void
                /**
                 * Adds all of the elements from `first` to `last`
                 * (inclusive) to `set`.
                 * @since 0.9.7
                 * @param set A set
                 * @param first The first element to add to `set`
                 * @param last The final element to add to `set`
                 */
                set_add_range(set: never, first: never, last: never): void
                /**
                 * Adds `num_codepoints` codepoints to a set at once.
                 * The codepoints array must be in increasing order,
                 * with size at least `num_codepoints`.
                 * @since 4.1.0
                 * @param set A set
                 * @param sorted_codepoints Array of codepoints to add
                 */
                set_add_sorted_array(set: never, sorted_codepoints: never[]): void
                /**
                 * Tests whether memory allocation for a set was successful.
                 * @since 0.9.2
                 * @param set A set
                 * @returns `true` if allocation succeeded, `false` otherwise
                 */
                set_allocation_successful(set: never): never
                /**
                 * Clears out the contents of a set.
                 * @since 0.9.2
                 * @param set A set
                 */
                set_clear(set: never): void
                /**
                 * Allocate a copy of `set`.
                 * @since 2.8.2
                 * @param set A set
                 * @returns Newly-allocated set.
                 */
                set_copy(set: never): never
                /**
                 * Creates a new, initially empty set.
                 * @since 0.9.2
                 * @returns The new #hb_set_t
                 */
                set_create(): never
                /**
                 * Removes `codepoint` from `set`.
                 * @since 0.9.2
                 * @param set A set
                 * @param codepoint Removes `codepoint` from `set`
                 */
                set_del(set: never, codepoint: never): void
                /**
                 * Removes all of the elements from `first` to `last`
                 * (inclusive) from `set`.
                 *
                 * If `last` is #HB_SET_VALUE_INVALID, then all values
                 * greater than or equal to `first` are removed.
                 * @since 0.9.7
                 * @param set A set
                 * @param first The first element to remove from `set`
                 * @param last The final element to remove from `set`
                 */
                set_del_range(set: never, first: never, last: never): void
                /**
                 * Fetches the singleton empty #hb_set_t.
                 * @since 0.9.2
                 * @returns The empty #hb_set_t
                 */
                set_get_empty(): never
                /**
                 * Finds the largest element in the set.
                 * @since 0.9.7
                 * @param set A set
                 * @returns maximum of `set`, or #HB_SET_VALUE_INVALID if `set` is empty.
                 */
                set_get_max(set: never): never
                /**
                 * Finds the smallest element in the set.
                 * @since 0.9.7
                 * @param set A set
                 * @returns minimum of `set`, or #HB_SET_VALUE_INVALID if `set` is empty.
                 */
                set_get_min(set: never): never
                /**
                 * Returns the number of elements in the set.
                 * @since 0.9.7
                 * @param set A set
                 * @returns The population of `set`
                 */
                set_get_population(set: never): number
                /**
                 * Tests whether `codepoint` belongs to `set`.
                 * @since 0.9.2
                 * @param set A set
                 * @param codepoint The element to query
                 * @returns `true` if `codepoint` is in `set`, `false` otherwise
                 */
                set_has(set: never, codepoint: never): never
                /**
                 * Creates a hash representing `set`.
                 * @since 4.4.0
                 * @param set A set
                 * @returns A hash of `set`.
                 */
                set_hash(set: never): number
                /**
                 * Makes `set` the intersection of `set` and `other`.
                 * @since 0.9.2
                 * @param set A set
                 * @param other Another set
                 */
                set_intersect(set: never, other: never): void
                /**
                 * Inverts the contents of `set`.
                 * @since 3.0.0
                 * @param set A set
                 */
                set_invert(set: never): void
                /**
                 * Tests whether a set is empty (contains no elements).
                 * @since 0.9.7
                 * @param set a set.
                 * @returns `true` if `set` is empty
                 */
                set_is_empty(set: never): never
                /**
                 * Tests whether `set` and `other` are equal (contain the same
                 * elements).
                 * @since 0.9.7
                 * @param set A set
                 * @param other Another set
                 * @returns `true` if the two sets are equal, `false` otherwise.
                 */
                set_is_equal(set: never, other: never): never
                /**
                 * Returns whether the set is inverted.
                 * @since 7.0.0
                 * @param set A set
                 * @returns `true` if the set is inverted, `false` otherwise
                 */
                set_is_inverted(set: never): never
                /**
                 * Tests whether `set` is a subset of `larger_set`.
                 * @since 1.8.1
                 * @param set A set
                 * @param larger_set Another set
                 * @returns `true` if the `set` is a subset of (or equal to) `larger_set`, `false` otherwise.
                 */
                set_is_subset(set: never, larger_set: never): never
                /**
                 * Fetches the next element in `set` that is greater than current value of `codepoint`.
                 *
                 * Set `codepoint` to #HB_SET_VALUE_INVALID to get started.
                 * @since 0.9.2
                 * @param set A set
                 * @returns `true` if there was a next value, `false` otherwise, Input = Code point to query             Output = Code point retrieved
                 */
                set_next(set: never, codepoint: never): [never, never]
                /**
                 * Finds the next element in `set` that is greater than `codepoint`. Writes out
                 * codepoints to `out`, until either the set runs out of elements, or `size`
                 * codepoints are written, whichever comes first.
                 * @since 4.2.0
                 * @param set A set
                 * @param codepoint Outputting codepoints starting after this one.             Use #HB_SET_VALUE_INVALID to get started.
                 * @param out An array of codepoints to write to.
                 * @returns the number of values written.
                 */
                set_next_many(set: never, codepoint: never, out: never[]): number
                /**
                 * Fetches the next consecutive range of elements in `set` that
                 * are greater than current value of `last`.
                 *
                 * Set `last` to #HB_SET_VALUE_INVALID to get started.
                 * @since 0.9.7
                 * @param set A set
                 * @returns `true` if there was a next range, `false` otherwise, The first code point in the range, Input = The current last code point in the range         Output = The last code point in the range
                 */
                set_next_range(set: never, last: never): [never, never, never]
                /**
                 * Fetches the previous element in `set` that is lower than current value of `codepoint`.
                 *
                 * Set `codepoint` to #HB_SET_VALUE_INVALID to get started.
                 * @since 1.8.0
                 * @param set A set
                 * @returns `true` if there was a previous value, `false` otherwise, Input = Code point to query             Output = Code point retrieved
                 */
                set_previous(set: never, codepoint: never): [never, never]
                /**
                 * Fetches the previous consecutive range of elements in `set` that
                 * are greater than current value of `last`.
                 *
                 * Set `first` to #HB_SET_VALUE_INVALID to get started.
                 * @since 1.8.0
                 * @param set A set
                 * @returns `true` if there was a previous range, `false` otherwise, Input = The current first code point in the range         Output = The first code point in the range, The last code point in the range
                 */
                set_previous_range(set: never, first: never): [never, never, never]
                /**
                 * Makes the contents of `set` equal to the contents of `other`.
                 * @since 0.9.2
                 * @param set A set
                 * @param other Another set
                 */
                set_set(set: never, other: never): void
                /**
                 * Subtracts the contents of `other` from `set`.
                 * @since 0.9.2
                 * @param set A set
                 * @param other Another set
                 */
                set_subtract(set: never, other: never): void
                /**
                 * Makes `set` the symmetric difference of `set`
                 * and `other`.
                 * @since 0.9.2
                 * @param set A set
                 * @param other Another set
                 */
                set_symmetric_difference(set: never, other: never): void
                /**
                 * Makes `set` the union of `set` and `other`.
                 * @since 0.9.2
                 * @param set A set
                 * @param other Another set
                 */
                set_union(set: never, other: never): void
                /**
                 * Shapes `buffer` using `font` turning its Unicode characters content to
                 * positioned glyphs. If `features` is not `NULL`, it will be used to control the
                 * features applied during shaping. If two `features` have the same tag but
                 * overlapping ranges the value of the feature with the higher index takes
                 * precedence.
                 * @since 0.9.2
                 * @param font an #hb_font_t to use for shaping
                 * @param buffer an #hb_buffer_t to shape
                 * @param features an array of user    specified #hb_feature_t or `NULL`
                 */
                shape(font: never, buffer: never, features: never[] | null): void
                /**
                 * See hb_shape() for details. If `shaper_list` is not `NULL`, the specified
                 * shapers will be used in the given order, otherwise the default shapers list
                 * will be used.
                 * @since 0.9.2
                 * @param font an #hb_font_t to use for shaping
                 * @param buffer an #hb_buffer_t to shape
                 * @param features an array of user    specified #hb_feature_t or `NULL`
                 * @param shaper_list a `NULL`-terminated    array of shapers to use or `NULL`
                 * @returns false if all shapers failed, true otherwise
                 */
                shape_full(font: never, buffer: never, features: never[] | null, shaper_list: string[] | null): never
                /**
                 * Retrieves the list of shapers supported by HarfBuzz.
                 * @since 0.9.2
                 * @returns a    `NULL`-terminated array of supported shapers constant string.    The returned array is owned by HarfBuzz and should not be    modified or freed.
                 */
                shape_list_shapers(): string[]
                /**
                 * Constructs a shaping plan for a combination of `face`, `user_features`, `props`,
                 * and `shaper_list`.
                 * @since 0.9.7
                 * @param face #hb_face_t to use
                 * @param props The #hb_segment_properties_t of the segment
                 * @param user_features The list of user-selected features
                 * @param shaper_list List of shapers to try
                 * @returns The shaping plan
                 */
                shape_plan_create(face: never, props: never, user_features: never[], shaper_list: string[]): never
                /**
                 * The variable-font version of #hb_shape_plan_create.
                 * Constructs a shaping plan for a combination of `face`, `user_features`, `props`,
                 * and `shaper_list`, plus the variation-space coordinates `coords`.
                 * @since 1.4.0
                 * @param face #hb_face_t to use
                 * @param props The #hb_segment_properties_t of the segment
                 * @param user_features The list of user-selected features
                 * @param coords The list of variation-space coordinates
                 * @param shaper_list List of shapers to try
                 * @returns The shaping plan
                 */
                shape_plan_create2(face: never, props: never, user_features: never[], coords: number[], shaper_list: string[]): never
                /**
                 * Creates a cached shaping plan suitable for reuse, for a combination
                 * of `face`, `user_features`, `props`, and `shaper_list`.
                 * @since 0.9.7
                 * @param face #hb_face_t to use
                 * @param props The #hb_segment_properties_t of the segment
                 * @param user_features The list of user-selected features
                 * @param shaper_list List of shapers to try
                 * @returns The shaping plan
                 */
                shape_plan_create_cached(face: never, props: never, user_features: never[], shaper_list: string[]): never
                /**
                 * The variable-font version of #hb_shape_plan_create_cached.
                 * Creates a cached shaping plan suitable for reuse, for a combination
                 * of `face`, `user_features`, `props`, and `shaper_list`, plus the
                 * variation-space coordinates `coords`.
                 * @since 1.4.0
                 * @param face #hb_face_t to use
                 * @param props The #hb_segment_properties_t of the segment
                 * @param user_features The list of user-selected features
                 * @param coords The list of variation-space coordinates
                 * @param shaper_list List of shapers to try
                 * @returns The shaping plan
                 */
                shape_plan_create_cached2(face: never, props: never, user_features: never[], coords: number[], shaper_list: string[]): never
                /**
                 * Executes the given shaping plan on the specified buffer, using
                 * the given `font` and `features`.
                 * @since 0.9.7
                 * @param shape_plan A shaping plan
                 * @param font The #hb_font_t to use
                 * @param buffer The #hb_buffer_t to work upon
                 * @param features Features to enable
                 * @returns `true` if success, `false` otherwise.
                 */
                shape_plan_execute(shape_plan: never, font: never, buffer: never, features: never[]): never
                /**
                 * Fetches the singleton empty shaping plan.
                 * @since 0.9.7
                 * @returns The empty shaping plan
                 */
                shape_plan_get_empty(): never
                /**
                 * Fetches the shaper from a given shaping plan.
                 * @since 0.9.7
                 * @param shape_plan A shaping plan
                 * @returns The shaper
                 */
                shape_plan_get_shaper(shape_plan: never): string
                /**
                 * Searches variation axes of a #hb_font_t object for a specific axis first,
                 * if not set, first tries to get default style values in `STAT` table
                 * then tries to polyfill from different tables of the font.
                 * @since 3.0.0
                 * @param font a #hb_font_t object.
                 * @param style_tag a style tag.
                 * @returns Corresponding axis or default value to a style tag.
                 */
                style_get_value(font: never, style_tag: never): number
                /**
                 * Converts a string into an #hb_tag_t. Valid tags
                 * are four characters. Shorter input strings will be
                 * padded with spaces. Longer input strings will be
                 * truncated.
                 * @since 0.9.2
                 * @param str String to convert
                 * @returns The #hb_tag_t corresponding to `str`
                 */
                tag_from_string(str: Uint8Array): never
                /**
                 * Converts an #hb_tag_t to a string and returns it in `buf`.
                 * Strings will be four characters long.
                 * @since 0.9.5
                 * @param tag #hb_tag_t to convert
                 * @returns , Converted string
                 */
                tag_to_string(tag: never): Uint8Array
                /**
                 * Retrieves the Canonical Combining Class (ccc) property
                 * of code point `unicode`.
                 * @since 0.9.2
                 * @param ufuncs The Unicode-functions structure
                 * @param unicode The code point to query
                 * @returns The #hb_unicode_combining_class_t of `unicode`
                 */
                unicode_combining_class(ufuncs: never, unicode: never): never
                /**
                 * Fetches the composition of a sequence of two Unicode
                 * code points.
                 *
                 * Calls the composition function of the specified
                 * Unicode-functions structure `ufuncs`.
                 * @since 0.9.2
                 * @param ufuncs The Unicode-functions structure
                 * @param a The first Unicode code point to compose
                 * @param b The second Unicode code point to compose
                 * @returns `true` if `a` and `b` composed, `false` otherwise, The composition of `a`, `b`
                 */
                unicode_compose(ufuncs: never, a: never, b: never): [never, never]
                /**
                 * Fetches the decomposition of a Unicode code point.
                 *
                 * Calls the decomposition function of the specified
                 * Unicode-functions structure `ufuncs`.
                 * @since 0.9.2
                 * @param ufuncs The Unicode-functions structure
                 * @param ab Unicode code point to decompose
                 * @returns `true` if `ab` was decomposed, `false` otherwise, The first code point of the decomposition of `ab`, The second code point of the decomposition of `ab`
                 */
                unicode_decompose(ufuncs: never, ab: never): [never, never, never]
                /**
                 * Fetches the compatibility decomposition of a Unicode
                 * code point. Deprecated.
                 * @since 0.9.2
                 * @deprecated since 2.0.0
                 * @param ufuncs The Unicode-functions structure
                 * @param u Code point to decompose
                 * @returns length of `decomposed`., Compatibility decomposition of `u`
                 */
                unicode_decompose_compatibility(ufuncs: never, u: never): [number, never]
                /**
                 * Don't use. Not used by HarfBuzz.
                 * @since 0.9.2
                 * @deprecated since 2.0.0
                 * @param ufuncs a Unicode-function structure
                 * @param unicode The code point to query
                 */
                unicode_eastasian_width(ufuncs: never, unicode: never): number
                /**
                 * Creates a new #hb_unicode_funcs_t structure of Unicode functions.
                 * @since 0.9.2
                 * @param parent Parent Unicode-functions structure
                 * @returns The Unicode-functions structure
                 */
                unicode_funcs_create(parent: never | null): never
                /**
                 * Fetches a pointer to the default Unicode-functions structure that is used
                 * when no functions are explicitly set on #hb_buffer_t.
                 * @since 0.9.2
                 * @returns a pointer to the #hb_unicode_funcs_t Unicode-functions structure
                 */
                unicode_funcs_get_default(): never
                /**
                 * Fetches the singleton empty Unicode-functions structure.
                 * @since 0.9.2
                 * @returns The empty Unicode-functions structure
                 */
                unicode_funcs_get_empty(): never
                /**
                 * Fetches the parent of the Unicode-functions structure
                 *  `ufuncs`.
                 * @since 0.9.2
                 * @param ufuncs The Unicode-functions structure
                 * @returns The parent Unicode-functions structure
                 */
                unicode_funcs_get_parent(ufuncs: never): never
                /**
                 * Tests whether the specified Unicode-functions structure
                 * is immutable.
                 * @since 0.9.2
                 * @param ufuncs The Unicode-functions structure
                 * @returns `true` if `ufuncs` is immutable, `false` otherwise
                 */
                unicode_funcs_is_immutable(ufuncs: never): never
                /**
                 * Makes the specified Unicode-functions structure
                 * immutable.
                 * @since 0.9.2
                 * @param ufuncs The Unicode-functions structure
                 */
                unicode_funcs_make_immutable(ufuncs: never): void
                /**
                 * Sets the implementation function for #hb_unicode_combining_class_func_t.
                 * @since 0.9.2
                 * @param ufuncs A Unicode-functions structure
                 * @param func The callback function to assign
                 */
                unicode_funcs_set_combining_class_func(ufuncs: never, func: never): void
                /**
                 * Sets the implementation function for #hb_unicode_compose_func_t.
                 * @since 0.9.2
                 * @param ufuncs A Unicode-functions structure
                 * @param func The callback function to assign
                 */
                unicode_funcs_set_compose_func(ufuncs: never, func: never): void
                /**
                 * Sets the implementation function for #hb_unicode_decompose_compatibility_func_t.
                 * @since 0.9.2
                 * @deprecated since 2.0.0
                 * @param ufuncs A Unicode-functions structure
                 * @param func The callback function to assign
                 */
                unicode_funcs_set_decompose_compatibility_func(ufuncs: never, func: never): void
                /**
                 * Sets the implementation function for #hb_unicode_decompose_func_t.
                 * @since 0.9.2
                 * @param ufuncs A Unicode-functions structure
                 * @param func The callback function to assign
                 */
                unicode_funcs_set_decompose_func(ufuncs: never, func: never): void
                /**
                 * Sets the implementation function for #hb_unicode_eastasian_width_func_t.
                 * @since 0.9.2
                 * @deprecated since 2.0.0
                 * @param ufuncs a Unicode-function structure
                 * @param func The callback function to assign
                 */
                unicode_funcs_set_eastasian_width_func(ufuncs: never, func: never): void
                /**
                 * Sets the implementation function for #hb_unicode_general_category_func_t.
                 * @since 0.9.2
                 * @param ufuncs A Unicode-functions structure
                 * @param func The callback function to assign
                 */
                unicode_funcs_set_general_category_func(ufuncs: never, func: never): void
                /**
                 * Sets the implementation function for #hb_unicode_mirroring_func_t.
                 * @since 0.9.2
                 * @param ufuncs A Unicode-functions structure
                 * @param func The callback function to assign
                 */
                unicode_funcs_set_mirroring_func(ufuncs: never, func: never): void
                /**
                 * Sets the implementation function for #hb_unicode_script_func_t.
                 * @since 0.9.2
                 * @param ufuncs A Unicode-functions structure
                 * @param func The callback function to assign
                 */
                unicode_funcs_set_script_func(ufuncs: never, func: never): void
                /**
                 * Retrieves the General Category (gc) property
                 * of code point `unicode`.
                 * @since 0.9.2
                 * @param ufuncs The Unicode-functions structure
                 * @param unicode The code point to query
                 * @returns The #hb_unicode_general_category_t of `unicode`
                 */
                unicode_general_category(ufuncs: never, unicode: never): never
                /**
                 * Retrieves the Bi-directional Mirroring Glyph code
                 * point defined for code point `unicode`.
                 * @since 0.9.2
                 * @param ufuncs The Unicode-functions structure
                 * @param unicode The code point to query
                 * @returns The #hb_codepoint_t of the Mirroring Glyph for `unicode`
                 */
                unicode_mirroring(ufuncs: never, unicode: never): never
                /**
                 * Retrieves the #hb_script_t script to which code
                 * point `unicode` belongs.
                 * @since 0.9.2
                 * @param ufuncs The Unicode-functions structure
                 * @param unicode The code point to query
                 * @returns The #hb_script_t of `unicode`
                 */
                unicode_script(ufuncs: never, unicode: never): never
                /**
                 * Parses a string into a #hb_variation_t.
                 *
                 * The format for specifying variation settings follows. All valid CSS
                 * font-variation-settings values other than 'normal' and 'inherited' are also
                 * accepted, though, not documented below.
                 *
                 * The format is a tag, optionally followed by an equals sign, followed by a
                 * number. For example `wght=500`, or `slnt=-7.5`.
                 * @since 1.4.2
                 * @param str a string to parse
                 * @returns `true` if `str` is successfully parsed, `false` otherwise, the #hb_variation_t to initialize with the parsed values
                 */
                variation_from_string(str: Uint8Array): [never, never]
                /**
                 * Converts an #hb_variation_t into a `NULL`-terminated string in the format
                 * understood by hb_variation_from_string(). The client in responsible for
                 * allocating big enough size for `buf`, 128 bytes is more than enough.
                 *
                 * Note that the string won't include any whitespace.
                 * @since 1.4.2
                 * @param variation an #hb_variation_t to convert
                 * @returns , output string
                 */
                variation_to_string(variation: never): string[]
            }
        }

        const HarfBuzz: HarfBuzz.$Exports
    }

    export default GI.HarfBuzz
}